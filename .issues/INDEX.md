# Issues index

`oms-react`'s local issue tracker. Conventions: `docs/agents/issue-tracker.md`.
One line per issue under its effort heading. Numbers start fresh at `001`.

<!-- Format:
- [NNN](NNN-short-slug.md) — <title> · **open**/**done** · blocked by: NNN, NNN | —
-->

## Active Sessions admin screen (map 001)

- [001](001-active-sessions-admin-screen.md) — Active Sessions admin screen · **open** · wayfinder map
- [002](002-active-sessions-endpoint-gap-and-contract.md) — Active-sessions endpoint gap & contract · **done** · blocked by: —
- [003](003-active-sessions-access-grant.md) — Which access grant gates the Active Sessions screen? · **done** · blocked by: —
- [004](004-active-sessions-design-mock.md) — Interactive design mock for the Active Sessions screen · **done** · blocked by: 002
- [005](005-active-sessions-screen-spec.md) — Lock the Active Sessions screen spec · **done** · blocked by: 003, 004
- [006](006-active-sessions-screen-spec.md) — Active Sessions admin screen (spec) · **ready** · spec — consumable by /to-tickets
- [007](007-active-sessions-screen-access-spine.md) — activeSessionsScreenGatesOnItsOwnGrant · **done** · blocked by: — · dep: BackOffice Sessions/Access + grant seed
- [008](008-active-sessions-search-list.md) — searchingLiveSessionsListsMatchingRowsCappedAt50 · **done** · blocked by: 007 · dep: BackOffice GET Sessions
- [009](009-active-sessions-chips-counts.md) — channelAndIdleChipsFilterWithServerCounts · **done** · blocked by: 008 · dep: BackOffice Sessions/Counts
- [010](010-active-sessions-revoke-one.md) — revokingASessionSignsTheDeviceOut · **done** · blocked by: 008 · reuses Sessions/Revoke
- [011](011-active-sessions-revoke-all-for-user.md) — revokeAllForUserSignsAPersonOutEverywhere · **done** · blocked by: 008, 010 · dep: BackOffice Sessions/RevokeAllForUser
- [012](012-active-sessions-freshness.md) — theMonitorAutoRefreshesAndShowsFreshness · **done** · blocked by: 008

## Web POS Simulation screen (BackOffice map 484 / spec 503)

Client port of the WPF POS Simulation pricing harness. Server slice is BackOffice 509 (`Pricing/Simulate`
+ `Pricing/Access` + `POS_SIMULATION_ADMIN` grant) — **done**. Moved here from BackOffice `.issues/510–513`.

- [013](013-web-sim-screen-tracer.md) — POS Simulation screen: enter a basket, Process, see priced results · **done** · blocked by: — · dep: BackOffice 509 Pricing/Simulate + Access (done)
- [014](014-web-sim-condition-cards.md) — Pricing detail with expandable condition cards + statistical toggle · **done** · blocked by: 013
- [015](015-web-sim-bonusbuy-elements.md) — Bonus-buy tabs + pricing elements · **done** · blocked by: 013
- [016](016-web-sim-editable-grids.md) — Editable items + manual-conditions grids · **open** · blocked by: 013

## POS Simulation stale-BBY cache reset (map 017)

Prevent false pricing feedback from SIS.Api's server-side `"Pricing"` FusionCache serving a stale
BBY after re-download. Destination is a decision/spec (server + client). WPF cleared it in-process;
React needs a new `Pricing/*` endpoint.

- [017](017-sim-stale-bby-cache-reset.md) — POS Simulation stale-BBY cache reset · **done** · wayfinder map — spec ready for /to-tickets
- [018](018-cache-bust-endpoint-feasibility.md) — SIS.Api cache-bust feasibility: endpoint + FusionCache API + download path · **done** · blocked by: —
- [019](019-cache-bust-scope.md) — Whole-cache clear vs targeted BBY eviction · **done** · blocked by: 018
- [020](020-cache-bust-trigger-surface.md) — Where the cache-bust is triggered · **done** · blocked by: 018
- [021](021-cache-bust-blast-radius-auth.md) — Shared-cache blast radius & who may clear it · **done** · blocked by: 019
- [022](022-cache-reset-spec-lock.md) — Lock the stale-BBY cache-reset spec · **done** · blocked by: 019, 020, 021 · [spec](022-cache-reset.SPEC.md)

## Cache reset — client build (spec 022)

Client slice 3 of spec 022 (the simulator Clear-cache button). Server slices 1–2 (`Pricing/ClearCache`,
the `PricingCache` cache-admin grant, `GET Pricing/CacheAccess`, BBY auto-evict) are **BackOffice**
work, tracked in `C:\Work\DMSCO\BackOffice\.issues\` — the tickets below consume those endpoints.

- [051](051-sim-clearcache-button-gated.md) — theSimulatorShowsAClearCacheButtonOnlyToCacheAdmins · **done** (typecheck/build green; drove gating 3/3 via mocked envelope — live-drive pending SIS.Api) · blocked by: — · dep: BackOffice GET Pricing/CacheAccess + PricingCache grant
- [052](052-sim-clearcache-confirm-clear-toast.md) — clickingClearCacheConfirmsClearsAndToasts · **done** (typecheck/build green; drove confirm/clear/toast + rate-limit 5/5 via mocked envelope — live-drive pending SIS.Api) · blocked by: 051 · dep: BackOffice POST Pricing/ClearCache

## Web Back-Office Notification Center (map 023)

Bell + badge + list + deep-link + banner + claim (receive) and a channel-targeted broadcast/compose
screen (send), over `SIS.Api Notifications/*` polling. Backend NC (issues 164–172) already ships the
contract; the web BO caller (no `registerid`) is already a `User+All` audience. Destination = ready spec.

- [023](023-web-notification-center.md) — Web Back-Office Notification Center · **done** · wayfinder map — destination reached (spec 031 ready)
- [024](024-nc-backend-contract-for-web.md) — Notification Center backend contract for the web client · **done** · blocked by: — · [research](024-nc-backend-contract-for-web.RESEARCH.md)
- [025](025-web-identity-session-fit.md) — Web app identity & session fit for the NC · **done** · blocked by: — · [research](025-web-identity-session-fit.RESEARCH.md)
- [026](026-receive-side-parity-scope.md) — Receive-side parity scope for the back-office · **done** · blocked by: 024, 025
- [027](027-broadcast-channel-model.md) — Broadcast channel/audience model · **done** · blocked by: 024
- [028](028-access-gating-and-grants.md) — Access gating & grants (bell + compose screen) · **done** · blocked by: 024
- [029](029-nc-bell-panel-compose-ux.md) — Bell / panel / compose UX prototype · **done** · blocked by: 026, 027 · [prototype](029-nc-bell-panel-compose-ux.PROTOTYPE.html)
- [030](030-nc-spec-shape-and-lock.md) — Lock the NC spec shape & hand off to /to-spec · **done** · blocked by: 026, 027, 028, 029
- [031](031-web-notification-center-spec.md) — Web Back-Office Notification Center (spec) · **ready** · spec — consumable by /to-tickets

## Web Notification Center — build (spec 031)

Tracer tickets sliced from spec 031. Two independent chains: **Receive** (032→033→{034,035}, layout/
chrome) and **Send** (036→{037,038}, features/admin). AFK plan: [NC-AFK-HANDOFF.md](NC-AFK-HANDOFF.md);
overnight blockers: [NC-MORNING-REPORT.md](NC-MORNING-REPORT.md).

- [032](032-nc-bell-poll-badge.md) — theBellPollsAndShowsAnUnreadBadge · **code-complete** (typecheck green; runtime-blocked, SIS.Api down) · blocked by: — · dep: GET Notifications/Poll (+ api.get header passthrough)
- [033](033-nc-panel-list.md) — theBellOpensAPanelListingAnnouncementsNewestFirst · **code-complete** (typecheck green; runtime-blocked) · blocked by: 032
- [034](034-nc-read-state.md) — readingAnItemDropsTheUnreadCount · **code-complete** (typecheck green; runtime-blocked) · blocked by: 033 · dep: POST Notifications/{id}/Read
- [035](035-nc-arrivals-sonner.md) — aFreshArrivalRaisesAToastAndBumpsTheBadge · **code-complete** (typecheck green; runtime-blocked) · blocked by: 033
- [036](036-nc-compose-send-store.md) — composingABroadcastSendsItToAStore · **code-complete** (typecheck green; runtime-blocked) · blocked by: — · dep: POST Notifications
- [037](037-nc-fleet-confirm.md) — sendingToTheWholeFleetAsksForConfirmation · **code-complete** (typecheck green; runtime-blocked) · blocked by: 036
- [038](038-nc-compose-access-gate.md) — theComposeScreenIsHiddenWithoutTheBroadcastGrant · **code-complete** (client + backend built; runtime-blocked) · blocked by: 036 · dep: GET Notifications/Access (backend ADDED — BackOffice pricing2 dc73ba1f)

## Simulation applied-promotion visibility rework (map 039)

Rework how the POS Simulation screen shows per-line results + the promotions that fired: promo
visible without clicking each line, buy→get shown as relationships (1+1 free, 50%-off-2nd), today's
full detail preserved for advanced users via progressive disclosure. Destination = **approved
sketches** (spec + build are a later effort). Buy→get data is present-but-unused; no backend work.

- [039](039-sim-promo-visibility-rework.md) — Simulation applied-promotion visibility rework · **done** · wayfinder map — destination reached (B+C hybrid approved)
- [040](040-sim-promo-shape-taxonomy.md) — Promo-shape taxonomy & response-field mapping · **done** · blocked by: — · [taxonomy](040-sim-promo-shape-taxonomy.TAXONOMY.md)
- [041](041-sim-results-promo-sketch.md) — Sketch the reworked results-and-promo surface · **done** · blocked by: 040 · [prototype](041-sim-results-promo-sketch.PROTOTYPE.html) · chose B+C hybrid
- [042](042-sim-promo-hybrid-lock.md) — Consolidate the B+C hybrid & lock the direction · **done** · blocked by: 041 · [sketch](042-sim-promo-hybrid-lock.PROTOTYPE.html) · APPROVED
- [043](043-sim-promo-visibility-spec.md) — Simulation applied-promotion visibility rework (spec) · **ready** · spec — consumable by /to-tickets

## Simulation promo-visibility — build (spec 043)

Tracer tickets sliced from spec 043. Two chains meeting at the pure view model: **backend projection**
(044, own repo) + **frontend surface** (045 pure `promoView` → 046 grid column / 047 blocks →
{048 missed, 049 disclosure} → 050 responsive). Slice 0 = 045 (pure, in-memory, degradation path first).

- [044](044-sim-applied-bby-projection.md) — appliedBonusBuysSplitBuyFromGetByConditionKey · **in-progress** (condition-projection half — IsPrerequisite/IsCondition/ConditionKey/BbyItemIndex — landed in BackOffice `SIS.Pricing.Services`, builds & sim tests pass; unblocks the client "applied N×" count after SIS.Api restart. Structural `Applications[]`/`DiscountKind` split still blocked on the SIS.Pricing.Core package repo) · blocked by: — · dep: SIS.Pricing/BackOffice projection pass-through
- [045](045-sim-promoview-model.md) — promoViewDerivesLinesBlocksAndMissedFromAResult · **done** · blocked by: — · Slice 0 (pure, graceful degradation) · harness 11/11
- [046](046-sim-grid-promo-column.md) — theResultsGridShowsPromoKindAndRolePerLine · **done** (typecheck/build/transform green; live-drive pending SIS.Api) · blocked by: 045
- [047](047-sim-promo-blocks.md) — firedPromotionsRenderAsBuyGetBlocks · **done** (typecheck/build green; drove real component via Playwright — live-drive of split path pending 044) · blocked by: 045
- [048](048-sim-could-have-applied.md) — aMissedPromotionShowsWhyItDidNotFire · **done** (typecheck/build green; drove real component via Playwright 13/13 — found-vs-required meter, would-save, reason, expand, absent-when-empty) · blocked by: 047
- [049](049-sim-progressive-disclosure.md) — aPromotionBlockRevealsTodaysConditionAndPricingDetail · **wontfix** (superseded by map 097 → [103](103-sim-deep-layers-placement.md)) · blocked by: 047
- [050](050-sim-responsive-hybrid.md) — theHybridLaysOutResponsivelyByWidth · **wontfix** (superseded by map 097 → [105](105-sim-responsive-arrangement.md)) · blocked by: 046, 047, 048, 049

## BBY Inquiry screen (map 053)

React rebuild of the WPF `Sartawi.Retail.Pricing.BbyInquiry` screen: read-only Bonus Buy inquiry —
summary grid defaulting to currently-active BBYs, searchable by number or validity-overlap date
range, with a per-row Details modal mirroring SAP "Display Bonus Buy". Destination = **ready spec**
(+ two prototypes). Two backend contracts (list-search + detail-by-number) designed here, built on
SIS.Api separately.

- [053](053-bby-inquiry-map.md) — BBY Inquiry screen · **done** · wayfinder map — destination reached (both prototypes approved; ready for /to-spec)
- [054](054-bby-domain-glossary.md) — Bonus Buy domain model & glossary · **done** · blocked by: —
- [055](055-bbymodel-detail-shape-research.md) — Research: full BbyModel detail shape for the Details modal · **done** · blocked by: —
- [056](056-bby-feature-placement-access.md) — Feature placement, nav & access gate · **done** · blocked by: —
- [057](057-bby-list-search-endpoint-contract.md) — List/search endpoint contract · **done** · blocked by: 054
- [058](058-bby-detail-endpoint-contract.md) — Detail-by-number endpoint contract · **done** · blocked by: 055
- [059](059-bby-list-search-ux-prototype.md) — List + search UX prototype · **done** · blocked by: 054, 057 · [prototype](assets/059-bby-list-search-prototype.html)
- [060](060-bby-detail-modal-prototype.md) — Details modal prototype (SAP "Display Bonus Buy" mirror) · **done** · blocked by: 055, 058 · [prototype](060-bby-detail-modal-prototype.PROTOTYPE.html)
- [061](061-bby-inquiry-spec.md) — Bonus Buy (BBY) Inquiry screen (spec) · **ready** · spec — consumable by /to-tickets

## BBY Inquiry — build (spec 061)

Tracer tickets sliced from spec 061. Tracer = 062 (gate + list-active grid); frontier after it is
{063, 064} (parallel). Every slice ships **code-complete / runtime-blocked** until the SIS.Api `Bby/*`
endpoints (`Bby/Access`, `Bby/List`, `Bby/Detail`, `Bby/GroupingMembers`) are built separately.

- [062](062-bby-inquiry-scaffold-gate-list.md) — bonusBuyInquiryGatesAndListsActiveBonusBuysByDefault · **done** (typecheck/build green; pure harness 10/10; drove gate + active-list + empty/fail-open/denied 14/14 via mocked `Bby/*` envelopes — live-drive pending SIS.Api) · blocked by: — · dep: SIS.Api Bby/Access + Bby/List
- [063](063-bby-inquiry-full-grid.md) — theGridShowsAllTwentyEightHeaderFieldsGroupedWithChipsStickyIdentityAndDetailsAction · **done** (pure harness 23/23; drove full grid 23/23 via mocked `Bby/*` — live-drive pending SIS.Api) · blocked by: 062
- [064](064-bby-inquiry-search-toolbar.md) — searchingByNumberOrDateClearsActiveOnlyAndFiltersByValidityOverlap · **done** (typecheck/build green; pure harness 8/8; drove search toolbar — chip, cap banner, date-error title, Reset — 35/35 via mocked `Bby/*`; live-drive pending SIS.Api) · blocked by: 062
- [065](065-bby-inquiry-csv-export.md) — exportingTheGridWritesAllTwentyEightRawFieldsToCsv · **done** (typecheck/build green; drove Export CSV — 28-col header + raw code/date cells — via mocked `Bby/List`; live-drive pending SIS.Api) · blocked by: 063
- [066](066-bby-inquiry-details-modal.md) — openingDetailsShowsTheHeaderRecapBuyGetOrTotalDiscount · **done** · blocked by: 063 · dep: SIS.Api Bby/Detail
- [067](067-bby-inquiry-grouping-drilldown.md) — groupingRowsOpenAPagedMembersDrilldown · **done** · blocked by: 066 · dep: SIS.Api Bby/GroupingMembers

## POS palette + Document Details rework — wayfinder map 068

Adopt the `Sartawi.POS/View/Themed/PosTheme.xaml` palette as oms-react's standard (retiring the
claude.ai warm neutrals), then rebuild Document Details to match the POS "OMS Detail — reworked"
prototype. Destination is two ready specs. Palette first, screen second — owner ruling.
Reference capture: [assets/068-pos-detail-reference.html](assets/068-pos-detail-reference.html).
All 13 tickets done — wayfinding complete. **Both specs published: 082 (design system) and 083
(Document Details rework).** The map is reached; 083 consumes 082 and must not start until it lands.

- [068](068-pos-palette-and-document-detail-rework.md) — POS palette as the app standard + Document Details rework · **done** · wayfinder map — destination reached, both specs ready
- [069](069-token-surface-inventory.md) — Token surface & call-site inventory · **done** · blocked by: —
- [070](070-pos-token-remap-light.md) — The POS token remap (light) · **done** · blocked by: 069 · [prototype](assets/070-pos-token-remap.PROTOTYPE.html)
- [071](071-pos-token-dark-twin.md) — The derived dark twin · **done** · blocked by: 070 · [prototype](assets/071-pos-dark-twin.PROTOTYPE.html)
- [072](072-command-family-taxonomy.md) — The command-family taxonomy · **done** · blocked by: — · [prototype](assets/072-command-families.html)
- [073](073-detail-layout-with-our-data.md) — The reworked layout, filled with our real fields · **done** · blocked by: 070 · [prototype](assets/073-detail-layout.PROTOTYPE.html)
- [074](074-ag-grid-theme-mapping.md) — AG Grid theme mapping · **done** · blocked by: 070 · [research](assets/074-ag-grid-theme-mapping.RESEARCH.md)
- [075](075-brand-surfaces-reconciliation.md) — What survives of al-dawaa gold & navy · **done** · blocked by: 069
- [076](076-action-bar-grammar.md) — The action-bar grammar for our eight commands · **done** · blocked by: 072, 073
- [077](077-severity-colour-layer.md) — The severity colour layer and the raw-palette sweep · **done** · blocked by: 070, 071, 075
- [078](078-live-document-payload-capture.md) — Capture live document payloads · **done** · blocked by: — · [payloads](assets/078-document-payloads/)
- [079](079-status-severity-mapping.md) — Status value → severity mapping for the pill rail · **done** · blocked by: 078
- [080](080-rtl-mirroring-of-the-reworked-layout.md) — RTL mirroring of the reworked layout · **done** · blocked by: 073 · [prototype](assets/080-rtl-mirroring.PROTOTYPE.html)
- [081](081-rail-card-field-rules.md) — The rail cards' field rules against live data · **done** · blocked by: 079
- [082](082-pos-design-system-spec.md) — The POS design system · **ready** · spec (map 068, 1 of 2)
- [083](083-document-details-rework-spec.md) — The Document Details rework · **ready** · spec (map 068, 2 of 2) · consumes 082

### Design-system build — spec 082

Expand → migrate → contract. 084 expands (tokens; zero `.tsx` churn), 085–088 migrate the consumers,
089 contracts (the gates are green only when the sweep is complete).
**084 done** — the tokens are in and the contrast gate runs on every `npm run lint`; the app now
renders steel-blue neutrals against warm status badges, the expected intermediate state.
**085 done** — every grid now paints from the tokens out of one params block, so the grid can no
longer diverge from `global.css`.
**086 done** — the badge idiom is one component taking a severity; the two feature tone maps (and
the duplicate third) are gone, and `go` found its first consumer in the BBY validity marker.
**087 done** — al-dawaa gold/navy now live only inside the mark: the login Editorial Split swaps its
ground to `--brand-panel` (kickers lose their gold, marks stay gold), the home hero becomes a plain
`--card` tool (watermark + gold kicker dropped), both `text-[#FDC801]` classes and the four brand
`text-white` sites are gone, and the dead `auth.json` `subtitle` key is deleted. `-\[#` is zero across
`src/`.
**089 done — the design-system build is closed.** `npm run lint` runs three gates; the third,
`tools/check-palette.mjs`, fails on a raw palette class, an arbitrary colour value, a `white`/`black`
utility or a bare hex anywhere in `src/` outside `global.css` and the mark. The ticket's open question
was resolved as it recommended: the hex pattern is widened past D-12's `-\[#` to `#[0-9a-fA-F]{3,8}`,
so a colour hidden in a `.ts` string — the one place this app ever hid one — is caught by lint rather
than by review. The four deliberate literals (three `bg-black/50` scrims, the login QR quiet zone)
are file-scoped exclusions carrying their reason inline.

- [084](084-pos-tokens-both-themes.md) — theAppRendersOnThePosTokensInBothThemes · **done** · blocked by: — · slice 0 · + contrast gate
- [085](085-grid-theme-reads-tokens.md) — theGridReadsTheAppTokensInsteadOfItsOwnHexCopy · **done** · blocked by: 084 · one params block · + `tools/grid-theme-drive.mjs`
- [086](086-status-badge-takes-a-severity.md) — aStatusBadgeTakesASeverityRatherThanAClassString · **done** · blocked by: 084 · new `core/ui` primitive · + `tools/status-badge-drive.mjs`
- [087](087-brand-colour-lives-in-the-mark.md) — brandColourLivesOnlyInTheMark · **done** · blocked by: 084 · both kickers + 4 brand `text-white` retired, hero → card
- [088](088-raw-palette-sweep.md) — noScreenSpellsARawPaletteClass · **done** · one pass, 35 files
- [089](089-colour-literal-lint-gates.md) — lintFailsOnAReintroducedColourLiteral · **done** · blocked by: 085, 087, 088 · `tools/check-palette.mjs`, hex gate widened
- [107](107-identity-band-dark-separation.md) — theIdentityBandReadsAsASlabOnTheDarkPage · **open** · blocked by: — · follow-up from [096](096-document-detail-drive.md)'s manual pass · `--brand-panel` separates at 13.54:1 in light but 1.18:1 in dark and is the page's only borderless slab; D-9 sized that step deliberately, but for the login half, not for 091's full-width header

### Document Details build — spec 083

Region by region, each slice taking one region of Screen 2 the whole way down (payload → pure rule →
component → i18n → test). **090 is slice 0**: it bootstraps vitest against the five captured payloads
and cuts the pill rail, retiring the spec's biggest unknown — that rules derived from real data hold
against that data. 093 needs only the runner, so it runs in parallel with 091/092. The action bar is
094 rather than earlier because it is the only region whose change deletes state elsewhere in the
page (`pendingNote`). 095 lands the RTL respellings byte-identical under LTR; 096 is the acceptance
drive.
**096 done — the Document Details rework is closed, and with it map 068.** The rebuilt screen drives
end to end (39/39) with the six region drives still green beside it, and the both-theme pass across
all five captured documents found no arrangement defect. Its one recorded finding is a *design-system*
question, not a screen one: `--brand-panel` is fixed in both themes, so the identity band separates
from the page at 13.54:1 in light and 1.18:1 in dark.

- [090](090-pill-rail-and-vitest.md) — theRailRendersOnlyTheStatusesThatCarryAValue · **done** (vitest bootstrapped, `npm test` 17/17 on the five captured payloads; typecheck/lint/build green; drove the rendered rail 25/25 via `tools/document-rail-drive.mjs`) · blocked by: — · slice 0 · + Status tab removed, header Status group retired early
- [091](091-identity-band.md) — theDocumentNumberIsTheLargestThingOnTheScreen · **done** (`npm test` 28/28; typecheck/lint/build green; drove the rendered band 32/32 via `tools/document-band-drive.mjs` in both themes) · blocked by: 090 · `isExpressDelivery` binds — no contract bug; `DocumentHeader` retired, provenance moved into the disclosure
- [092](092-summary-rail-cards.md) — theScatteredFieldsBecomeFiveCardsOnASummaryRail · **done** (`npm test` 40/40; typecheck/lint/build green; drove the rendered rail 45/45 via `tools/document-cards-drive.mjs` in both themes and across the 900px unstack) · blocked by: 091 · `FieldGroup`/`ShippingAddress` retired; `courierCode` is set on 5/5 so the driver card does NOT collapse on pick-in-store — the rule ships as written, the gloss was wrong; the address panel's GPS + Google-Maps link left the screen with it
- [093](093-items-grid-and-jobs-count.md) — theItemsGridSumsItselfAndFlagsASignedDiscount · **done** (`npm test` 56/56; typecheck/lint/build green; drove the rendered grid 23/23 via `tools/document-items-drive.mjs`) · blocked by: 090 · parallel with 091/092 · counts land on ALL four tabs (a lone number on Jobs reads as an anomaly), no count until a deferred collection resolves; the footer label is two pluralised fragments — `1 lines` would have shipped on all five captures; totals round off float dust; the `cellClass` bidi wrap is 095's
- [094](094-action-bar-grammar.md) — theActionBarReadsAsThreeClustersAndATerminalTier · **done** (`npm test` 68/68; typecheck/lint/build green; drove the rendered bar 38/38 via `tools/document-actions-drive.mjs`) · blocked by: 092 · `pendingNote` dies with the standing textarea; the note is captured in each command's own dialog (`NoteDialog` + `NoteField`, `ChangeStoreResult.note`); a reasoned command carries `aria-disabled` (not `disabled`) so it stays focusable enough to explain itself; four new `core/ui/Button` variants carry 072's families; "command cluster" added to `CONTEXT.md`
- [095](095-rtl-mirroring-and-bidi.md) — theLayoutMirrorsCorrectlyBeforeAnyDirSwitchExists · **done** (`npm test` 68/68 unchanged; typecheck/lint/build green; drove both directions 33/33 via `tools/document-rtl-drive.mjs`, and the four region drives re-run green untouched) · blocked by: 091, 092, 093, 094 · new `core/ui/Ltr` on six fields; F1/F2/F4 were already correct from 085/091/093 and are now measured rather than assumed; **F5 has no counterpart in this build** — 073's notched frame became an underline tablist, so there is no notch to respell; `↗` flips to `↖`; `Undo2`/`Reply` on the action bar were never ruled on and stay unflipped for the `dir`-switch effort
- [096](096-document-detail-drive.md) — theRebuiltScreenDrivesEndToEndInBothThemes · **done** (`tools/document-detail-drive.mjs` 39/39; the six region drives re-run green untouched — rail 25, band 32, cards 45, items 23, actions 38, rtl 33; `npm test` 68/68, typecheck/lint/build green) · blocked by: 095 · **no source change was needed** — the pass found no defect; the one finding is recorded, not fixed: `--brand-panel` separates from the page at 13.54:1 in light but **1.18:1 in dark**, and the band is the page's only borderless surface, so the "one dark band" reads as a faint slab in dark — an 082 token decision, not a one-line correction · 090's fold-in note answered: the corpus table folded in, `document-rail-drive.mjs` stays as region evidence

## POS Simulation screen rework — wayfinder map 097

The Simulation twin of map 068: the whole screen — input side and results side — rearranged on the
shipped POS design system (082) into a denser, chip-led device that reclaims the space eleven
bordered cards spend on chrome. **Fresh design, no external reference**; evidence-first (098 before
every design ticket); **arrangement only**, no behaviour change. Destination = one **ready spec**.
Supersedes the two open 043 build tickets (049, 050).

- [097](097-simulation-screen-rework.md) — The POS Simulation screen rework · **done** · wayfinder map · all 11 tickets resolved, fog clear → hand off to `/to-spec`
- [098](098-simulate-payload-capture.md) — Capture live Simulate payloads · **done** · blocked by: — · task · 11 payloads under assets/098-simulate-payloads/
- [099](099-sim-region-question-inventory.md) — What the analyst reads the screen for, region by region · **done** · blocked by: — · grilling · arrangement D approved; 9 frames → 3
- [100](100-sim-chip-vocabulary.md) — The chip vocabulary: what earns a chip · **done** · blocked by: 098, 099 · grilling · two kinds, two hues, chips never act
- [101](101-sim-screen-device-prototype.md) — The reworked screen as one device · **done** · blocked by: 098, 099, 100 · prototype · quiet strip, 3 frames, near-misses in
- [102](102-sim-input-chip-bar.md) — The input chip bar: collapse, expand, edit, stale run · **done** · blocked by: 101 · prototype · one status slot, three states; defaults chip
- [103](103-sim-deep-layers-placement.md) — Where the deep layers live · **done** · blocked by: 101 · supersedes 049 · one idiom + the modal; only trace hides
- [104](104-sim-results-line-anatomy.md) — The anatomy of a result line · **done** · blocked by: 098, 101
- [105](105-sim-responsive-arrangement.md) — How the arrangement behaves across widths · **done** · blocked by: 101, 103, 104 · supersedes 050 · one breakpoint at 900 of work area; floor 1024
- [106](106-sim-rtl-mirroring.md) — Mirroring and bidi for the reworked arrangement · **done** · blocked by: 101, 104 · mirroring clean, 13 Ltr wrappers, 2 icons to flip
- [108](108-sim-bby-details-affordance.md) — The bonus-buy details affordance on a promo card · **done** · blocked by: 101, 103 · both cards · modal in place · ships dark until Bby/Detail lands
- [109](109-sim-i18n-churn-and-test-seams.md) — The i18n churn and the testing seams · **done** · blocked by: 108 · research · 22 retired / 6 renamed / 17 new keys, 6 pure seams, 5 drives
- [110](110-sim-screen-rework-spec.md) — The POS Simulation screen rework · **ready** · spec · synthesized from map [097](097-simulation-screen-rework.md); 80 stories, 6 pure seams, 4 drives to build; sliced into 111–122
- [111](111-sim-aggregate-conditions-under-test.md) — `aggregateConditions` holds its grouping contract under test · **done** · blocked by: — · spec 110 · prefactor · safety net before 116 grows its blast radius
- [112](112-bby-detail-modal-to-core.md) — The bonus-buy detail modal answers from `@/core/` · **done** · blocked by: — · spec 110 · prefactor · file move, zero behaviour; i18n namespace unchanged
- [113](113-sim-run-strip.md) — The run strip collapses the determination into chips and processes from there · **done** · blocked by: 123 · spec 110 · **slice 0** · + the work-area `@container` · 9 pure + 29 drive assertions
- [114](114-sim-status-slot.md) — The status slot marks a stale run and an in-flight one · **done** · blocked by: 113 · spec 110 · the rework's only new component · 30 pure + 22 drive assertions; no new i18n key
- [115](115-sim-result-line.md) — The result line reads its money in the corrected order · **done** · blocked by: 113, 123 · spec 110 · 7 columns; blank not 0.00; W ⇒ not priced · 20 pure; rendered check throwaway (the drive is 119’s)
- [116](116-sim-line-expansion.md) — A result line expands its rules and elements in place · **done** · blocked by: 111, 115, 123 · spec 110 · dissolved 2 components, the last AG Grid and 11 keys · 33 drive assertions on port 5200
- [117](117-sim-promotions-rail.md) — The promotions rail shows fires and near-misses beside the lines · **done** · blocked by: 115, 123 · spec 110 · near-misses reinstated; card prints its line list · 6 pure + 31 drive assertions (port 5201) · the split is between Results and the rail, so Items goes full-width above it and each frame is as tall as its own content; the near-miss's hue correction is the **meter**, not the tile (088 had already neutralised `KIND_CHIP`), so `○` marks the card and the condition type is named in words; no capture carries a `prerequisites[]` array, so the meter is unprovable on the corpus and the drive asserts the fallback; 114's stale-note check re-based on geometry
- [118](118-sim-bby-details-affordance.md) — A promotion card opens its bonus buy details · **done** · blocked by: 112, 117, 123 · spec 110 · gate `probed && screenAllowed`; ships dark · 22 drive assertions (port 5202) · the probe had to MOVE to `@/core/` to be consumed at all (112's spillover), exported cache key with it; four probe states driven, the fourth HELD so “the rail never waits” is proven rather than inferred; the control is a full-width card footer, never a chip, and the drive now checks that rule both ways
- [119](119-sim-responsive-arrangement.md) — The arrangement stacks the rail above the results below 900 px · **done** · blocked by: 116, 117 · spec 110 · container queries; pure shed order · 10 pure + 47 drive assertions (port 5203) · the work-area container is NAMED (`@container/work`) so the rail's card row can query the same 900 px breakpoint that stacks it — beside, a card still fills its column; the `order` lives on the RESULTS so source order stays results-then-rail (the reading and tab order at the width the screen is worked at); the trace measures its own wrapper because a pure shed order needs a number, and at 780 stacked it is WIDER than at 960 inside the 66% column; the two-row rule is measured across the strip's leaf parts, which caught a real three-row fragmentation at 780 — fixed by tightening spacing, hiding nothing
- [120](120-sim-non-result-states.md) — The screen's non-result states: pre-run, manual conditions, and the whole-run failure · **done** · blocked by: 113, 123 · spec 110 · manual conditions self-open; 400 replaces the work area · `fault-route.ts` reads the route off the code · drive port 5204, 27/27
- [121](121-sim-rtl-mirroring.md) — The reworked screen mirrors, spends two hues, and carries no retired key · **done** · blocked by: 115, 116, 117, 119 · spec 110 · absorbs 122 · `tools/sim-rtl-drive.mjs` re-pointed from the prototypes at the REAL app (29/29, 8 states × both directions, port 5205) + new `tools/check-sim-keys.mjs` (8/8) · the mirroring half stayed empty (zero physical utilities, asserted by source sweep); the bidi half landed on **9 runs + 5 composed values**, not 13 sites — two audit sites don't exist in the build (the strip's money has no literal space; `Rate: … | Base:` is W7-immune) and the raw server title turned up in THREE places, including the fired card's own title, because the classified branch never renders on the corpus; the audit's own measurement had to be **sharpened** (a leading/trailing neutral relocates per N1/N2 without scrambling the value, so runs are measured first-to-last ALPHANUMERIC — otherwise English prose reads as broken) and its composed pass **narrowed** to figure+unit (a label and its amount SHOULD swap under RTL — that is the mirroring); a Tailwind v4 flip is invisible to `transform` (it lives on the `scale`/`rotate` longhands), which is what makes the double-mirror check meaningful; the `buy→get` arrow is unprovable on the corpus (no split path until 044) so the drive asserts the spelling and says so; hue: the flag cell's `success` went neutral, 122's dead `met` branch had already left with 116, and the 400's `danger` is core `ErrorBanner`'s own (120's ruling), asserted by name rather than folded into the census; the ledger swept 11 held-back keys + one orphan the ledger never named (`summary.placeholder`)
- [122](122-sim-hue-and-key-audit.md) — The screen spends two hues and carries no retired key · **wontfix** · blocked by: 116, 121 · spec 110 · merged into [121](121-sim-rtl-mirroring.md) to remove a build wave
- [123](123-sim-i18n-key-expand.md) — The simulation namespace carries every new key before any slice needs it · **done** · blocked by: — · spec 110 · **expand half** · 27 keys minted, nothing retired; vitest key-ledger test

## OMS access gate + the empty-menu state

The one nav group with no probe (`menu-model.ts:23-29` names deliveries as *the* "always visible"
example) turns out to be an open **write** door: every `SdDocument/*` endpoint carries
`ApiKeyEndpointFilter` and no grant filter, so any authenticated session — including a store employee who
has just activated and holds no role — can reschedule or update a delivery document. Closing it makes the
empty menu common, so the empty state ships first. Server halves are BackOffice
[749](C:\Work\DMSCO\BackOffice\.issues\749-oms-web-screen-grant-seed.md) (grant seed) and
[750](C:\Work\DMSCO\BackOffice\.issues\750-sddocument-web-door.md) (the cookie-only `SdDocumentWeb/*` door).

- [124](124-no-visible-menu-empty-state.md) — A signed-in user with nothing to open is told so, not shown a blank page · **done** · blocked by: — · frontend-only, no backend dependency, ships first · the `noAccess` card in the `home` namespace (three keys: title / diagnosis / action — "contact IT support", never "your administrator") plus the part that is not cosmetic: `useVisibleMenu` grows a `settled` flag, because it hides a gated item while its probe is *pending* and the moment 125 gates the last ungated group the menu is empty on **first paint for everyone** — a naive `length === 0` check would flash the card on every load. Two call sites, so the return type changes rather than adding a parallel hook that can drift. Root cause it explains: `UpsertEmployeeAsync` writes `UaEmployee` only; the `UaUser` shell is minted by **first role assignment in Authz Admin**, and until then `AuthorizationSnapshotLoader` answers every probe with an empty list, silently
- [125](125-oms-behind-the-access-probe.md) — OMS goes behind an access probe, and its write doors stop being open · **code-complete** (client built, drive 28/28 on a stubbed wire; runtime-blocked on 750 being deployed + the server-refuses-a-reschedule check) · blocked by: 124 ✔, BackOffice 750 (**deployed**, not merely merged) · repoint `features/oms/*` to `SdDocumentWeb/*`, fold the bare `searchDeliveries` into a `deliveriesApi` object with `access()`, `accessProbe` on the leaf keyed `['oms','access']` (same key as the page guard ⇒ one call), and **two** guards — `DeliveriesPage` on `canOpenList`, `DocumentDetailsPage` on `canOpenDetail`. The second is the easy one to skip and the one that matters: `/oms/document/:no` is deep-linkable, `router.tsx` carries no per-route permission metadata, and a list-only guard leaves the write screen reachable to anyone who knows a document number. ⚠ this probe **fails closed** — no 404-tolerant catch like `Notifications/Access`/`Bby/Access`, because the endpoint ships with 750 and the thing behind it is a write door, which is exactly why it cannot merge before 750 is deployed. OQ1 is the likeliest way it goes wrong: the current OMS team must be *bound to the role* first, or the gate locks out the people it admits. Built: the probe landed in `@/core/oms/api` (`OMS_ACCESS_KEY` + `omsAccessApi.access()`) rather than on `deliveriesApi` — both OMS pages guard on it and a feature may not import a feature, the ticket-118 move. Two fixes beyond the letter: the guards carry the menu's `staleTime: Infinity` too (a mismatched one refetched on mount and could empty the OMS group under a working screen), and a FAILED probe now says the check is unavailable instead of telling an entitled operator to ask for a grant they hold. Eight sibling document/grid drives were repointed to the new door and re-run green

## The call center goes to the web (map 126)

Phase 1 replaces the WPF call center (CC1 `NewOrderController` + CC2) for the **plain CLCN cash
order only**, on an **engine-as-a-service** substrate: SIS.Api opens a real server-side
`CallCenterOrder` `PosTransaction` (it already ships `SIS.Pos 26.4.113`), resumed per request off
the persisted snapshot, and the client sends **intent, never money**. Backend halves are minted as
BackOffice issues (`C:\Work\DMSCO\BackOffice\.issues\`) — the engine and SIS.Api ship to the tills
too. Stands on BackOffice map [244](C:\Work\DMSCO\BackOffice\.issues\244-newpos-callcenter-order-entry.md)
+ spec [267](C:\Work\DMSCO\BackOffice\.issues\267-cc-order-entry-submission-spec.md), which already
shipped the doctype, the once-only `CallCenter/SubmitOrder` mint, and the submit orchestration.

- [126](126-web-call-center.md) — MAP: The call center goes to the web · **open**
- [127](127-engine-session-lifecycle.md) — What a web agent's engine session is, precisely · **done** · map 126 · blocked by: — · grilling · `register = WEB-<loginId>` (per agent), `Origin C000` a server-side constant, shiftless stays doc-type-keyed (no new flag), **strict claim** + 15 s lease + no heartbeats, second `open` refused with the existing id, sweeper at 12 h, **no `IPosEnvironment`**, live rows in the HQ DB → BackOffice [785](C:\Work\DMSCO\BackOffice\.issues\785-web-cc-engine-session.md)
- [128](128-origin-c000-and-coupon-parity.md) — Origin becomes a real field, and coupons move with it · **done** · map 126 · blocked by: — · grilling · **two permanent layers**: `TransactionOpenOptions.Origin` engine-**defaulted** (never required, so a till is right by construction), and the `BbyProcess` Plant-fallback **kept forever** — owner ruling — because it serves `PcHeader`s built outside the transaction engine (simulation/billing). `Origin` is **sticky**, restored from the snapshot, NOT claimant-derived. Coupons match `request.Origin ?? request.StoreCode` — the same expression as `BbyProcess` — with **one pairing test** proving same-filter template + BBY give both-or-neither. Physical CC device **out of scope** (already a `Store` row); **no blast-radius survey** (new engine not live); no `Origin` on the CLCN document. Server work minted as BackOffice [788](C:\Work\DMSCO\BackOffice\.issues\788-origin-seat-axis-and-coupon-parity.md)
- [129](129-rebind-store-door.md) — The plant-rebind door and its five re-derivations · **done** · map 126 · blocked by: — · grilling · 🚩 found a **till bug** on the way: `ResumeAsync` never sets `PcHeader.Plant`, so anything scanned after a suspend/resume takes the **national** price, silently (BackOffice 797, blocks the door). Door = `RebindPlantAsync` (BackOffice 798): takes the new plant's **whole identity**, re-prices with `"C"` `NewPricingAndKeepManual` (NOT `RecalculateAsync`'s rescale-only `"A"`), **refuses atomically** if a line no longer prices, **re-freezes ATP** (amends note 8), coupons **untouched**, promotion *selection* unchanged (BBY keys on Origin, not Plant). Derivation **pinned at the address act** ⇒ 132's ops-flip trigger out of scope. Preview is free: rebind and don't persist
- [130](130-potential-bby-prerequisites.md) — Can the server tell an agent *which items* would fire a missed promotion? · **done 2026-07-27 — yes for the buy side, no for the get side.** `prerequisites[]` is **not** dead (filled on every candidate); the captures read `[]` because both promotions carry **zero prereq rows**. BackOffice already shipped the guidance projection (spec 574/579 `AvailableOffersBuilder`), and `PricingContext.BuildSimulationResult` projects a **live** transaction with no re-price — that *is* `getState()`'s promotion seam. Eligible items are already exploded one-row-per-material and are the **same set** evaluation matches, but both builders **collapse to `[0]`** (also mis-typing a grouping as a single material — a live WPF defect). 🚩 **The real blocker is discovery**: BBY lookup keys on the *condition-side* access tables only, so a basket with only the buy-side item of "buy X get Y" **never loads that promotion**. ATP filtering → SIS.Api beside 131. 128 shifts which promos appear but doesn't block 136 (typed skip category). **No `wouldSave` on the wire** — this repo prints a percent as money today. → BackOffice [787](C:\Work\DMSCO\BackOffice\.issues\787-web-cc-promotion-guidance-engine.md), design → [138](138-near-miss-guidance-design.md) · map 126 · blocked by: — · research
- [131](131-item-search-endpoint.md) — The item search the agent actually types into (owner supplied the SQL) · **done** · map 126 · blocked by: — · task · **HITL** · contract minted as BackOffice [799](C:\Work\DMSCO\BackOffice\.issues\799-cc-item-search-endpoint.md) · `GET CallCenter/ItemSearch` behind door 137 · 🚩 supplied SQL is missing the composite key's `Client` predicate · ⚠ **amends note 9** — the row price is an **ex-VAT estimate** off the material master, not a store list price, and reads ~13% under the VAT-inclusive basket line (`MWST` is a separate 15% condition), so under-quoting is the failure mode; label lands on 135 · search gains **Arabic** (WPF never searched `Description2`) + item-number prefix, barcode out · eligibility = CC1's own whitelist, which the WPF lookup applies nowhere · **ATP needs no new contract** — `Stock/ItemPlant` already exists, folded in server-side at the order's plant, degrades to `atp: null` never non-200 · no paging, cap + `truncated` · 🚩 non-sargable match left deliberately unmeasured (p95 ≤ 500 ms target, full-text as measured fallback)
- [132](132-header-capture-inventory.md) — What header capture actually requires (CC1 + CC2 inventory) · **done** · map 126 · blocked by: — · research · **no new server data** — 11 of 12 header fields already have live endpoints, 5 already wired here; the one gap is a **door** (→ 137). Slot rule + `1283`/`1154` are Wasfaty-only, cut from CLCN. CC2's district→store rule wins and this repo already has it. Two new silent rebind triggers for 129
- [133](133-submission-path-server-side.md) — How the web submit reuses 278's orchestration, server-side · **done 2026-07-27 — in-process, not HTTP-to-self** (once-only lives entirely below the HTTP boundary; the till's endpoint stays byte-identical). The builder needs **no move** — its WPF-resident **input**, the issue-198 line mirror, is what moves, and it already exists server-side as a private test helper to promote. Once-only shipped as **(OrderNo, DocumentType), OrderNo := TransactionId**. Operator identity needs **nothing new**; `EntryStore` ruled = the agent's switcher store. 🚩 **`RecoverStaleAsync` has zero production callers**; 🚩 127's 12 h sweeper would **auto-void a transaction whose order was actually minted** ⇒ hosted reconciler + sweeper guard. 🚩 The delivery-fee **rule** is WPF-only and must become shared code (the `DFEE` condition itself is unchanged). Server work: BackOffice [786](C:\Work\DMSCO\BackOffice\.issues\786-web-cc-submission-path.md) · map 126 · blocked by: — · research
- [134](134-access-and-authorization.md) — Who may open the call center, and what the door refuses · **done** · map 126 · blocked by: — · grilling · **one grant admits the whole console**: `CallCenterConsoleView` = `BackOfficeScreen[CallCenter,03]`, role `CALL_CENTER_AGENT`, covering every verb incl. submit (749's own ruling reused; note 4 already removed every price-affecting power, so no dangerous verb is left to single out) ⇒ the probe answers **one boolean**, `{ canOpenConsole }`, and **fails closed**. **No store dimension** — the store is derived and pinned at the address act, 129's atomic refusal constrains an override materially, and there is nothing to constrain against (`StoreDetails` is documented broader than real permissions; no per-user `Auth/Stores`). 🚩 The filter must require the **cookie branch explicitly** — `ApiKeyEndpointFilter` stamps a never-empty *service* `UserId`, so an `IsNullOrEmpty`-only guard grant-checks a service account; that check is what keeps the WPF `CallCenter/SubmitOrder` alive and **unedited** beside the web door. Both Bby-gated surfaces dissolved instead of minting a second grant: grouping members ship **resolved + ATP-filtered inside `getState()`** (a 136 contract ruling), the SAP detail modal is **out of phase 1** ⇒ the console touches no `Bby/*` route. 🚩 Chrome-less console ⇒ its refusal is a **dead end** (`ProtectedLayout` renders `AppShell` unconditionally), so it carries its own way home in the five-key copy form — a state 135 must draw. 🚩 **Cutover**: seeds bind no holder and the only path minting the `UaUser` shell is first role assignment in Authz Admin, so an activated agent is refused **silently** until bound — per user, query-verified, before the SIS.Api deploy. Server work minted as BackOffice [800](C:\Work\DMSCO\BackOffice\.issues\800-call-center-console-grant.md) (the 749 analogue; deploys before 137's door)
- [135](135-agent-console-prototype.md) — What the agent console looks like · **done 2026-07-27** · map 126 · blocked by: — · prototype · **variant A, three fixed columns** — rails never move; ex-VAT estimate leaves the money column (`SAR` = engine money only); offer strip wraps, count mirrored in the top bar; both confirmations stay modal; keyboard grammar graduates to 153. Variants on branch `prototype/135-callcenter-console`
- [136](136-session-api-contract.md) — The session API contract that lets both tracks run free · **done** · map 126 · blocked by: 127, 129, 130, 133 · grilling · **frozen v1.0** → [CONTRACT.md](assets/136-cc-contract/CONTRACT.md) + 8 provisional fixtures. Every mutating verb returns the **whole `SessionState`** (the server has just resumed and persisted — the projection is in hand); **`requestId` on every mutation** makes an ambiguous retry safe; **explicit, ownership-validated `transactionId`** stops a forgotten tab writing onto the agent's *new* order; **"are you sure" rides the success path** as `pendingConfirmation` + a token that **pins** the previewed diff — one pattern for both the rebind and the below-ATP ack, whose token *is* the fraud-flag audit record; **near-misses inline, eligible items on demand** (`ResolvePrereq`, off `Bby/*`); `SESSION_BUSY` retried in `features/callcenter/api.ts`, **never** `core/api.ts`; header fields live in a SIS.Api **`CallCenterSession` sidecar** and `SessionState` is the join. 🚩 The two-store **write ordering** is the fragile part (reserve requestId *with the pre-mutation version* before the engine mutation, or a crash makes the retry double-apply). Server work minted as BackOffice [804](C:\Work\DMSCO\BackOffice\.issues\804-cc-session-contract-server-obligations.md). **Both tracks now unblocked to `/to-spec`**
- [137](137-callcenter-web-door.md) — The header-capture routes need a web door · **done** · map 126 · blocked by: 134 · grilling · 🚩 **132's premise was wrong**: `ApiKeyEndpointFilter` is *cookie-session OR api-key* (`:40-79`), not api-key-only, so all ~15 routes are **already browser-reachable today** (`lookups.ts` drives five live). The door is about **gating**, not reachability ⇒ **eight leave it entirely** (Cities · Districts · AddressLabels · DocumentSources · DocumentTypes · StoreDetails · AvailableSlots · SlotIsActive — reference data, zero server work; 750 OQ2's slot ruling re-confirmed on this door's own terms) and **nine stay**, gated for PII + writes. **One tag `CallCenterWeb/*` hosts everything** — header siblings, 131's `ItemSearch`, 136's session verbs — behind one filter and one `{ canOpenConsole }` probe, so **136 inherits it**. 🚩 **'Delegates verbatim' breaks twice, deliberately**: `DocumentSourceUsers/{userId}` → session-derived `MyDocumentSources` (no path param), and the five `CustomerAddresses` routes → **scoped to the session's attached customer**, because the originals are unscoped (`DeleteCustomerAddress` takes **only an `addressNumber`**, no customer at all) ⇒ a verbatim sibling would let any agent delete any address in the estate. ⚠ Address book now **unreachable before customer attach** — a new ordering constraint for 135 + 136. `Touch` confirmed off the door. 🚩 Exposed a **live PII hole predating the call center**, **ruled out of scope** → BackOffice [802](C:\Work\DMSCO\BackOffice\.issues\802-callcenter-pii-routes-ungated.md). Door minted as BackOffice [801](C:\Work\DMSCO\BackOffice\.issues\801-callcenter-web-door.md)
- [138](138-near-miss-guidance-design.md) — What actionable promotion guidance looks like, and what it promises · **done 2026-07-27** · map 126 · blocked by: 135 · prototype · **HITL (ruling taken)** · graduated from fog by 130: three near-miss classes in one list, a grouping is a set not an item, and the card may promise a discount **definition** — never a savings total. Ruled: **variant 1, the wrapping strip** — against the write-up's recommendation, which stays recorded; it costs 72 px over variant 2 and buys **scanning**, every actionable offer a card with no stepper. Safe only because both its corrections were already measured in (open card spans both columns; strip body clamped at 18rem with the outcome banner pinned outside). **Qualifying rows carry Arabic** (131's reason applies harder — this is the row read aloud), fixed at the shared `ItemRow`, whose doc comment had promised Arabic while rendering none. Two findings from landing it: 🚩 **`<bdi>` implies `dir="auto"`** and flips the block RTL, detaching the Arabic from its English line — `<bdi dir="ltr">` keeps the isolation without the flip, the mirror case of 121's `core/ui/Ltr` and a **build-level** constraint; 🚩 **a clamped region hides the cost of new content** — all nine heights stayed identical while the third item and `Search the other 37` (part 1's required route to the rest) went below the fold, and the 45%-of-centre budget left no room to raise the clamp, so Arabic moved to the **meta line** and every height came back byte-identical: **the ruling costs zero pixels**. Drive 91/91, typecheck + 3 lint gates green
- [153](153-console-keyboard-grammar.md) — The console's keyboard grammar · **done 2026-07-29** · map 126 · blocked by: — · prototype · **four keys and a palette**: `Ctrl+K` · `↑↓` · `Enter` · `Esc`, all armed from inside a text box because the resting focus IS one, twice over — **no single letters, no `Alt` chords, no `?`, no `F1`**. 🚩 Found the map's headline feature **mouse-only in the built console**: neither a search row's *Add* nor a guidance card's had any keyboard path. `Enter` is armed only after an arrow (131's non-sargable `LIKE` makes the top row a guess); **nothing on the keyboard can end a call** — submit/abandon are palette rows, sorted last and never auto-highlighted; a refused verb is a **disabled row carrying its reason**, the one deliberate exception to *no control beats a refused control*. No contract change, no server work

### Owner gap review, 2026-07-27 — six features the charting missed (154–159)

Raised on reviewing 138's prototype: delivery type · payment type · delivery fees · stock in other
stores · price check · redeem coupon · add new loyalty. They split three ways — **already in scope
but never drawn** (159), **missing from the frozen contract** (154, 155), and **never charted at all**
(156's waiver half, 157, 158). Two owner rulings taken at review time: the delivery fee is
**rule-driven with no manual waiver**, and price check + stock-in-other-stores are **both phase 1**.

- [154](154-fulfilment-mode-and-store-choice.md) — Fulfilment mode, and how the store gets chosen when nothing derives it · **done 2026-07-27** · map 126 · blocked by: — · grilling · **HITL (7 rulings taken)** · **both modes in phase 1** — owner: "these are the bare minimum". Contract → **v1.1, additive** ([§2.2](assets/136-cc-contract/CONTRACT.md) + new fixture [09](assets/136-cc-contract/09-fulfilment-flip.json) + dated §10 entry); server half BackOffice [826](C:\Work\DMSCO\BackOffice\.issues\826-cc-fulfilment-mode.md). **Three facts dissolved most of the ticket**: 🚩 CC2 holds **no line entry at all** (the header is captured, then POS rings the items), so "can the mode change mid-basket?" has **no WPF precedent** — the web *creates* the question by making the engine session live from the first keystroke; 🚩 the gate the ticket cited (`CanEditOrderType`, `IsFulfillmentEnabled`) is **edit-mode, not basket state**, so it argues for no lines-exist gate; 🚩 `open` **already** seeds the plant from `entryStore` with `canAddItem: true` (fixture 01), so map note 6 holds under either mode and pickup needs **no new opening move** — 135's caret-on-phone ruling is untouched. Ruled: flip allowed whenever `status: open`; **the flip never moves the plant** (which is what keeps this additive — pickup→delivery re-derives through `setAddress`'s existing path, so 129's door is reached the way it already was); pickup store = **whole estate unfiltered** via existing `setStore` over off-door `StoreDetails`, **134's no-store-dimension ruling stands** (129's atomic refusal is the material constraint); address **hidden and retained** (shown-but-optional rejected — the builder discards it, so the agent would watch keyed data vanish), slot hidden, both dropping from `submitBlockers`. **1.1 not 2.0**: `address` was already nullable, §5.1 already defined `setStore` as an operator override, `submitBlockers` was already a varying list — a v1.0 client renders a pickup order wrongly but never *mis-renders money*, the hazard the hard stop exists for. 🚩 **Corrects [156](156-delivery-fee-shared-rule.md)**: the threshold is **not** missing and the contract invented nothing — `POSController.NewPos.cs:8951` computes the whole CallCenterOrder fee rule and already keys on the mode; `thresholdGross` quotes `POSCommon.ShippingMinimumAmount`. Two open items recorded not invented: a pickup order has **no collection time**, and `plantSource` now does double duty
- [155](155-payment-type-cod-or-online.md) — Payment type: cash on delivery, or paid online · **done 2026-07-29** · map 126 · blocked by: — · grilling · **HITL** · **contract v1.4, additive** ([§2.4](assets/136-cc-contract/CONTRACT.md)): `header.paymentType` (`CashOnDelivery | Online | Receivable`), `header.paymentTypeForcedReason`, `setPaymentType`, `capabilities.canChangePaymentType`, codes `PAYMENT_TYPE_FORCED`/`PAYMENT_TYPE_INVALID`, fixture 11 specified-and-owed. Owner rulings: default **`CashOnDelivery`** (WPF parity — `NewOrderController.cs:50`), and it draws as a **chip, settled and collapsed** (CC2's own `ShowPaymentChip`) with **no `submitBlocker`** — it always holds a valid value and the agent's whole act is one spoken question. 🚩 **The forcing rule the ticket was handed is not on this axis**: 175 carried *"P2E forces online payment"* here as a **source** rule, but CC2 forces on the **kind** (`IsPaymentForced => DocumentType.SelectedType.ForcesOnlinePayment`) and CC1 says it louder from the other side — `OnlinePayment`'s setter opens `if (!IsCash && value) return`, so insurance and Wasfaty **refuse** online and re-assert COD, making **the cash kind the one where the operator is free**. Phase 1 is the cash kind ⇒ **nothing can force this field**; `DocumentSourcePolicyService` forces only *delivery-only* (WSFD/P2E/DKSW), which is **176's** axis and was handed there. The confusion is in the codebase: CC1 calls the *kind* axis `OrderSource`, and `P2E` is simultaneously a kind strategy and a `DocumentSourceConstants` code. The flag ships **with no rule behind it** so a later one is a server data change (`DocumentSourceCategory` membership is table data — it cannot be settled in code); hard-coding P2E/WSFD/DKSW ⇒ online was rejected as a rule this map would be *inventing*. **Receivable (`R`) is named and refused** — reserved ≠ accepted, and freezing a two-value enum would make the third a §9 major. 🚩 **Deletes** `Submit.cs:196`'s `CashOnDelivery = isDelivery` rather than adjusting it — an unrelated axis, wrong both ways, on real documents today. **Note 4 untouched on the owner's own grounding** (*"nothing will be paid there, no tender"* — `Online` instructs OMS to send a gateway link), and the delivery-fee predicate carries no payment term, so the axis interacts with **nothing on this map**; `CreateOmsOrder.cs:109` is the only downstream consumer and it maps rather than branches. One console rule rides out: under `PickInStore` the chip reads **Pay on collection** while the wire stays `CashOnDelivery` — so the chip and 176's mode control cannot be drawn independently. ⚠ The forced rendering is an **unreachable client path, named as such** (177's lesson) and proved from a stubbed drive state. Server half folded into BackOffice [871](C:\Work\DMSCO\BackOffice\.issues\871-cc-opening-gate-and-plant-source.md)
- [156](156-delivery-fee-shared-rule.md) — The delivery fee stops living in WPF · **done 2026-07-29** · map 126 · blocked by: — · research · **it already had** — 786 §2 (minted by 133) extracted `CallCenterDeliveryFeePolicy`, the ONE copy the till, the live quote and the submit all call, so §8.3's WPF-resident flag is cleared; the campaign window became `PosConfig` rows, not a recompile; `thresholdGross` is real (100 SAR); pick-in-store is the first predicate with `waived` deliberately FALSE. 🚩 Both real findings were in the CONSOLE: `waived` carries no reason and the "free over …" line is gated on `!waived`, so it vanishes exactly when it would explain itself ⇒ contract **v1.5** `deliveryFee.waivedReason` (client must NOT derive it from `gross` vs `thresholdGross`); and capture 09's pickup state draws `Delivery SAR 0.00` + a free-delivery promise on a collection order ⇒ **absent, not zero**, handed to 176. Residual: quote and submit recompute rather than pin. Server work → BackOffice 874
- [157](157-price-check.md) — Price check: what an item costs, without adding it · **done 2026-07-29** · map 126 · blocked by: — · grilling · **engine truth, and the engine already gives it without a transaction** — contract **v1.6 §3.4**, additive; server work BackOffice [875](C:\Work\DMSCO\BackOffice\.issues\875-cc-price-check-endpoint.md). `SimulationService.Simulate` prices in a throwaway `SIM_<guid>` context and calls `RemoveContext` — **no claim, no resume, no persist** — so a price check cannot collide with the agent's own basket and never queues behind the 15 s lease; and `EnrichItem` overwrites `TaxClassificationMaterial` from the master, so the answer is **VAT-inclusive engine money** equal to the basket line, quotable as `SAR`. The ex-VAT estimate never has to be read out loud. Rulings: item alone at **qty 1**; the number **and** the offers on it in 138's promise language (no `wouldSave`, no figure formatted as money); the surface is the deliberate **expansion of a search row**, shared with [158](158-stock-in-other-stores.md); **refused** before a caller and a chosen store (`canPriceCheck` = `canAddItem`'s predicate) because quoting at a seeded store is 797's silent wrong price said aloud; the `≈` estimate stays exactly where 168 put it. 🚩 **`Pricing/Simulate` must not be reused, route or body** — grant-gated on `BackOfficeScreen[PosSimulation,03]`, and `ManualConditions` + *fill-only-when-empty* header enrichment would hand an agent map note 4's removed power on the one number spoken aloud. 🚩 **`EnrichItem` does not fill `MaterialGroup`**, which is a BBY grouping key the engine's scan path DOES set — omit it and an offer shows on the basket line and not on the price check, failing quietly and only on the offers half. Offers ship blind and say so (`offersComplete: false` until 787-C). **Original framing:** A till feature, not a port: `ItemInfoLookupController` (UnitPrice + Stock) is constructed in exactly one place, `POSController.cs:16403`, and no CC1/CC2 path reaches it. 🚩 the whole difficulty is **131's note 9 made worse** — an ex-VAT estimate is tolerable on a search row where the agent is *choosing*, and intolerable here, because a price check exists to be **read out loud** with no basket line beside it to contradict it. So the question is not where the button goes but **what number it may return**: engine truth or estimate, VAT-inclusive always, does it see promotions (a number that ignores the offer 138 is simultaneously advertising is a contradiction on one screen), and what it says before a plant is bound
- [158](158-stock-in-other-stores.md) — Stock for an item in other stores · **done 2026-07-29** · map 126 · blocked by: — · research · **read-only, and the read already exists** — contract **v1.7 §3.5**, additive; server work BackOffice [876](C:\Work\DMSCO\BackOffice\.issues\876-cc-stock-elsewhere-endpoint.md). SIS.Api already registers the typed client and **calls it from nowhere** (`StockV2HttpService.GetPlant` + `.GetCurrentStockByDistance`), so the endpoint is two existing calls onto the till's exact read — the two screens cannot quote different availability or different distance. ⚠ **The ticket's two-way choice was not one**: the OMS `MaterialPlantStockModel` path takes no location and returns on-hand, filling a different grid. SIS.Api's own geo stack (`Stores/Nearby` over the pure, tested `NearestStoreFinder`) rejected as a **second definition of distance** — 156's lesson — but it supplied the degrade rule the SQL lacks. 🚩 **The dangerous coordinate is the ORIGIN, not the row**: nothing refuses `(0,0)`, so an unlocatable order plant returns a fully-populated, plausible, entirely wrong ranking measured from the Gulf of Guinea — the fiction `NearestStoreFinder.cs:65-70` already refuses **by name** ⇒ `distanceKnown: false`, never an omitted store. 🚩 **Two estate geo derivations from one table** — SIS.Stock off `StoreArea.StoreLatitude/Longitude`, SIS.Api off the free-text `StoreGPS` *because* its read-model documents those columns as existing "in some environments but not others" ⇒ a query-verification becomes a deploy step. **ATP proved identical** to 131's `atp` (same formula, table, 11-day window) and only ATP ships — the till shows on-hand beside it, which is how the larger number gets promised. Read-only stands on the till's own precedent: the only action on its grid **SMSes the customer a map link** (now Out of scope), never moves the order; plus an item's panel cannot host an act that re-prices every line, and a rebind invalidates the list it was clicked from. Separate call from `priceCheck` because it is the **only remote HTTP hop on the contract**. [Research note](assets/158-stock-elsewhere/RESEARCH.md). **Original framing:** `StockInOtherStoresCommand` → `StockByDistanceController` → `StockV2Service.GetCurrentStockByDistance(item, lat, long)` — the first thing on this map needing **geo**, and the first item read deliberately **not** scoped to the order's plant (131 folded `Stock/ItemPlant` in *at* that plant — the opposite reach). 🚩 the dangerous half is not the read, it is **what the agent may do with it**: seeing stock at 1204 invites moving the order there, which is 129's plant rebind with its atomic refusal and whole-basket re-price. Rule read-only vs rebind-entry explicitly; if the latter it rides 129's confirm path and nothing else
- [159](159-coupon-and-loyalty-signup-drawn.md) — The console draws its coupon and its loyalty signup · **done** · map 126 · blocked by: — · prototype · a **drawing** job, no contract question open: both are already settled and simply absent from 135/138. `applyCoupon` is verb #10 with `COUPON_REJECTED`/`COUPON_ALREADY_APPLIED` in the taxonomy, yet `grep -i coupon` over `VariantA.tsx` returns nothing; loyalty signup is 132's ruled-in two-step OTP (country + mobile → OTP, no name/email, `CustomerCreateService`) with *"nothing to build server-side"*, and the console has no answer to "this caller isn't in the system". Draw the coupon beside a fired promotion without it reading as a fourth near-miss class, draw the OTP wait as a **spoken** one, and 🚩 check that the **signup** routes are on 137's door — it gated loyalty *lookup*, and a console that can find a caller it cannot create is the wrong half · **ANSWERED**: contract **v1.10**, additive — and the ticket's *no contract question is open* was wrong four times. `applyCoupon` was the one verb with **no field to show its result** (the projection's only coupon-aware line is the one that HIDES the COUP voucher) ⇒ `header.coupons[]`, an array, derivable from `TransactionLine` data the projection already holds. Owner ruled **remove with a real reversal** — which turned out to be **already built for the till** (issue 211: reverse first, void only if it landed; a transport fault is a refusal), so `COUPON_REVERSAL_REFUSED` means **NOTHING CHANGED**, the opposite of what a failed remove usually means. `canApplyCoupon` = `canAddItem`'s predicate because the redemption stamps the ORDER'S PLANT in the coupon ledger permanently and 129's rebind does not move it. 🚩 **Capture 02 already held a money hole**: its near-miss prereq is a **coupon SKU** (`COUPT173`) drawn as *add 1 more*, and prerequisite matching has no line-type filter — a plain add qualifies the bonus buy while burning nothing ⇒ a fourth `prereq.kind`, `coupon`, **stated never offered**. Coupon draws as the **last chip** (owner: chip over receipt), code never amount. Signup: both routes **already on the door** ✅, drawn **inline in the rail** because the OTP wait is spoken; 🚩 `BranchId` must leave the wire (server-stamped) — 137's verbatim law breaking a third time — and the dialling-code rule stays a display preview so it is not a second copy of a loyalty key. Built on the REAL ConsoleShell, drive 84/84 ⚠ every state a STUB. Server half: BackOffice 879
- [160](160-callcenter-console-spec.md) — The web call-center console, phase 1 (spec) · **ready** · spec — consumable by /to-tickets · map 126 · the **client track's** destination, covering the **frozen core only**: 135's three fixed columns + 138's wrapping guidance strip over 136's frozen v1.0 contract, behind 134/137's one-grant door. 65 stories. Key client rulings: the **query cache is the store of record** (every mutating verb returns the whole `SessionState`, so a guarded `applyState` — discard a lower `version` — replaces any reducer or delta protocol); `SESSION_BUSY` backoff lives in `features/callcenter/api.ts` and **never** in `core/api.ts`; one `requestId` per user action, reused across retries **and** the confirm re-send; both `pendingConfirmation` kinds **modal**; `capabilities`/`submitBlockers` drive enablement so no server predicate is re-implemented; the **guidance region holds no engine money at all**, so its rule is *no figure formatted as money* (never *no `SAR`* — server descriptions carry currency words); `promo-view.ts` graduates to `@/core/` **with its percent-as-money defect fixed**; a `dir`-pinned bidi wrapper, never a bare `<bdi>`; the client **does not re-derive the fulfilment store** (the server does, at `setAddress`). Testing: **RTL stays uninstalled** (083's ruling, owner-confirmed) — pure modules + Playwright drives over 135's thirteen and 138's nine states, stubbing 136's eight **provisional** fixtures (shape only, never values). 🚩 Carve-outs are named, not silent: **154/155 are holes in the frozen contract** and need a contract revision, not a ticket; 153/156/157/158/159 each carry what would unblock them
- [161](161-percent-not-printed-as-money.md) — aPercentStopsBeingPrintedAsMoney · **done** · map 126 · blocked by: — · **prefactor** · spec 160 · 🚩 fixed a **live bug on a shipping screen**: `wouldSave = discount.value` rendered through `formatMoney`, so the capture's `70% 2nd PCS` promo read **`35.00 SAR`**. The card now states what the offer *gives* (`GIVES 35% off`), never a saving it cannot know (US26). The discount-**definition wording** graduated to `src/core/promotions/discount-definition.ts` — `{ kind, value, quantity?, nthFree? }` → `{ key, params }` for `t()`, keys namespace-qualified `common:discount.*`, ONE `numeral()` formatter so `35.00` cannot be produced at all, ordinal left to i18next. **Narrow by ruling** — `DISCOUNT_VALUE_UNIT` was drafted and cut for want of a caller. Proof: 16 pure + the corpus-wide no-numeric-field guard + `sim-rail-drive` 35/35 (extended with the `%` regression capture) + `sim-rtl-drive` 29/29
- [162](162-console-opens-an-order.md) — openingTheConsoleOpensAnOrderBehindItsOwnGrant · **done** · map 126 · blocked by: — · **Slice 0** · spec 160 · the whole spine, thin: `/callcenter` renders its **own full-viewport layout** outside `AppShell` (note 13), the probe **fails closed** on one boolean, `open` returns a whole `SessionState` that lands in the **query cache as store of record** through a guarded apply, and the three columns render from it. 🚩 The denial **carries its own way home** — a chrome-less refusal has no nav to leave by. Bootstraps the `callcenter` i18n namespace, `__fixtures__/payloads.ts` off 136's `01` (**shape only, never values**) and `tools/callcenter-drive.mjs`. dep: BackOffice 800/801/804 · **built**: feature lands at `features/callcenter/console/` (area/feature, what the boundary lint requires); `Open` is modelled as a **query** keyed on one console-life `requestId` so a retry re-sends the same id (law 3) and concurrent mounts dedupe; `CONSOLE_NOT_GRANTED` branches on `apiErrorCode()` so a real refusal never reads as "try again shortly"; `signOut` graduated to `@/core/auth/` — the chrome-less refusal needs it and a feature may not import `features/auth`. Proof: 11 pure + `callcenter-drive` 35/35
- [163](163-order-already-open.md) — anOrderAlreadyOpenOffersResumeOrStartFresh · **done** · map 126 · blocked by: 162 · spec 160 · `refusedExisting` is a **choice on the success path**, not an error: the previous caller's name, line count and opened-at, then resume (`getState`) or abandon-and-open-fresh. 🚩 **Never a silent auto-resume** — a new caller must not inherit the last caller's basket (127). Doubles as the reconnect story after refresh, crash or closed tab · **built**: `open-outcome.ts` reads `OpenResult`'s discriminant + two nullable payloads ONCE into a closed set of four (the malformed pair is a named card, not an eternal spinner); `requestId` became state so *start fresh* mints a genuinely new open action while a retry still reuses its id (law 3); **one** `AbandonConfirm` serves both the choice screen and the live order — same act, same sentence naming the lines thrown away; a **failed resume stays on the choice screen** so *abandon and start fresh* is still reachable; `ConsoleCard` now owns the two ways home so no non-console state can be built without them. `SESSION_CLOSED` deliberately left to 164. Proof: 13 pure + `callcenter-drive` 76/76
- [164](164-busy-collision-and-staleness.md) — aBusyCollisionLooksRoutineAndTheBasketNeverGoesBackwards · **done** · map 126 · blocked by: 162 · spec 160 · the resilience spine, **early on purpose** so every later verb inherits it. Backoff `0·400·800·1600·3200` (~15 s ceiling) 🚩 **in `features/callcenter/api.ts`, never `core/api.ts`** — lease semantics stay out of the layer every grid shares. A non-blocking strip that says typing still works, then a manual retry; a **lower `version` is discarded** so a slow response can't rewind the basket; a stale tab is refused `SESSION_CLOSED` instead of landing on the new order; a **major `contractVersion` mismatch is a hard stop** rather than mis-rendered money · **built**: one seam `runGuarded` that every call goes through, so tickets 165–174 inherit the spine rather than each remembering it — it rides the collision out, publishes it as a strip (in the flow, never over the basket) and reads a dead-order refusal ONCE, so return-to-start cannot become a branch per verb; `ApiError` now carries a refusal's envelope `data` (the contract answers refusals with `reason` / `retryAfterMs` / `unpriceableLines[]`, and unwrapping the envelope is core's job on both paths, not one feature's); `getState` gained the agent's hand on it — §6.1's universal recovery action, and the still-busy strip's manual retry; the strip is two facts not one slot, so an overlapping call cannot wipe the only handle on an unfinished action; 🚩 **an ABSENT `contractVersion` degrades rather than stopping** — silence is a server defect but not evidence of a major change, and refusing would brick the console against the very server (804) this slice is waiting on. Proof: 18 pure + `callcenter-drive` 113/113
- [165](165-attach-caller-fills-the-rail.md) — attachingTheCallerFillsTheRailAndOpensTheAddressBook · **done** · map 126 · blocked by: 162 · spec 160 · caret in the phone field **at open**; a six-field compact card (the seventh turns a rail into a form); the address book is **server-side unreachable before attach** (137's scoping), so the empty dashed slot + *Pick an address* is a real state and `canOpenAddressBook` decides it — the agent never reaches a control that refuses. Removing the caller clears the address and **keeps the derived store**. ⚠ loyalty *signup* is 159's, undrawn — do not invent one · **built**: the rail moved out of `ConsoleShell` into its own `CustomerRail` with a pure `rail-view.ts` behind it — the six-field **cap and order live in the module**, so a seventh field cannot be added by widening JSX; finding is **two steps** (the lookup names who was found, the agent attaches) because a wrong number that attached itself would have to be removed from a real order; `addressSlot` reads `canOpenAddressBook` **only**, which is what produced the fourth state — *caller attached, book shut* (v1.1 `PickInStore`) — and `noCaller`/`unavailable` are drawn with **no control at all**, not a disabled one; attach and remove are **one mutation** (one act, two directions) whose fallback wording follows the direction that failed; 🚩 the search **clears when the attached caller changes** — a found card left standing offers *Attach* for the caller just removed over a box still holding their number, and US9's caret then lands in the previous call. *Pick an address* is drawn as words until 166 wires the book: a control that answers with silence is the same harm as one that refuses. `LoyaltyMember` is typed in `core/models/` as **not part of the session contract** — it precedes attach, which is why 801 could not scope it. Proof: 12 pure + `callcenter-drive` 150/150
- [166](166-address-derives-the-store.md) — pickingAnAddressDerivesTheStoreAndSaysSo · **done** · map 126 · blocked by: 165 · spec 160 · an address on an **empty basket** applies inline; the store chip says **derived** (a parenthetical off `plantSource`) so a branch the agent didn't choose reads as explained. 🚩 **The client never derives the store** — the district→store rule is server-side; a second derivation is how console and engine start disagreeing. Lands the **chip row** (settled / needs attention / derived) with *needs attention* computed from `capabilities.submitBlockers`, never a second client rule. `ADDRESS_NOT_FOR_CUSTOMER` is explained, never “not found” · **built**: `address-book.ts` is the pure module — default-first ordering, CC2's own line composition, and the two refusals mapped to **different** explanations; it composes **book rows only**, the order's own address arrives composed in the projection and is never re-composed. The picker applies on **one click** (an empty basket has nothing to re-price, so §5.1 raises no confirmation and a confirm step here would be a modal in front of a modal), reads the book **when it is opened** rather than at attach, and offers a retry on a failed read. 🚩 The negative is PROVEN, not asserted: the `SetAddress` body names no plant and **no district/city/store lookup is fetched at all** — a console that worked the branch out itself would have had to read one. `canOpenAddressBook` is read once on the way in (the page passes `onPickAddress` only while it holds), and a **change of caller closes the book** so the next call never inherits the last one's intent. ⚠ chips stay non-interactive: the only section behind one today is the store, whose re-open is 167's explicit override. Proof: 14 pure + `callcenter-drive` 192/192
- [167](167-store-move-shows-the-diff.md) — movingTheStoreShowsTheDiffAndRefusesWhole · **done** · map 126 · blocked by: 166 · spec 160 · **owns the confirmation pattern the console reuses**: “are you sure” on the **success path** — 200 + the *unchanged* state + a token that **pins** the previewed diff — drawn **modal** by ruling (an inline card in a scrolling flow can be scrolled past). Accept re-sends the same verb with the **same `requestId`** + token; `CONFIRM_TOKEN_STALE` re-previews rather than committing a diff the agent never saw; declining costs nothing (the door is run and not persisted). `REBIND_REFUSED` is atomic and names the line **in the banner and by tinting it** · **built**: the id discipline is a **module**, not a call-site convention — `store-move.ts` mints a rebind's id in exactly one place and carries it through the confirm and the re-preview, so “one action, one id” is a property a pure test holds. 🚩 **One mechanism, structurally**: `ConfirmSheet.tsx` owns the modal, the two buttons, the re-issue notice and the blocked-commit block; `StoreMoveConfirm` supplies only the store-change **body**, so 169 mounts the same sheet with a body of its own and the marker is `pendingConfirmation.kind`. The re-preview is **bounded by the thing that raises it** (only a send that carried a token can be answered stale, and the re-send carries none) — without that condition “it cannot loop” was a comment, not a guarantee, on a verb that re-prices a live basket. A preview that **already names an unpriceable line refuses the commit there**, before the round trip (fixture 06's own note: `unpriceableLines[]` rides preview *and* refusal); the 409 path remains and is driven, as the basket-moved-between case. The refusal reads its lines from `ApiError.data` and falls back to the preview — fixture 06's note that core drops `data` is out of date. The **delivery fee counts as money** in `moneyStandsStill`. The store chip becomes the way back in (gated on `canChangeStore`; a chip with nowhere to go stays a `<span>`, never a disabled control), and `StorePicker` reads the **unfiltered** estate only when opened, **stating** the count its 40-row cap does not show. Proof: 15 pure + `callcenter-drive` 265/265
- [168](168-search-in-arabic-no-estimate-as-money.md) — searchingFindsItemsInArabicAndNeverQuotesAnEstimateAsMoney · **done** · map 126 · blocked by: 162 · spec 160 · search by part of a name in **English or Arabic** (`Description2`, which WPF never searched) or item-number prefix; cap + `truncated`, no paging. 🚩 The row price is a **material-master ex-VAT estimate reading ~13% under the basket line**, so it renders on the item's **second line** with `≈`, muted, **no currency word** — `SAR` is engine money — and the end edge carries availability + *Add* only. **Availability has three states and `unknown` is not `zero`** (ground, ink *and* wording). dep: BackOffice 799 · **built**: `item-search.ts` holds both rulings, because both are properties of the ROW rather than of the panel — `SearchRowView` is four fields (`itemNumber`, `title`, `meta[]`, `availability`) and 🚩 **has no money-column field to assign**, so the estimate rides `meta[]` beside the item number with `≈` and no currency word, and an absent one is **dropped rather than drawn as `≈0.00`**. The drive proves it **spatially**, not by class name: the estimate's box ends 400 px before the availability pill starts, sits on the row's second line, and the end edge holds no money-formatted figure at all. `availabilityOf` returns `labelKey` **and** `tone` separately so ground, ink and wording differ **by construction** — one `tone` would let a later edit distinguish three opposite decisions by colour alone; `null`, an absent field and a negative count all answer honestly and nothing collapses onto zero. The estimate note is **standing above the input**, not the results box's caption (a rule that arrives with the rows is read after the agent is already looking at prices). Search is held until the term would be accepted (3 chars — 799 answers 400 below it), settles 250 ms, and is keyed by **order + term**: the same words on a different order are a different question. Arabic rides the meta line in the **`dir`-pinned** isolate (138), measured — the name stays at the start edge instead of flipping the block. `addItem` sends an item number and a quantity and **never a price** (asserted on the wire), on a `requestId` minted once outside `runGuarded`. ⚠ a `belowAtp` answer draws one interim sentence for now; **169 replaces it with the sheet and deletes the key**. Proof: 17 pure + `callcenter-drive` 301/301
- [169](169-below-availability-accepted.md) — addingBeyondAvailabilityIsAcceptedDeliberately · **done** · map 126 · blocked by: 167, 168 · spec 160 · availability is a **soft gate**: the confirm always succeeds and never blocks. 🚩 **The token *is* the audit record** — it proves the agent was shown the number they accepted, which a client-set boolean never could (285/286) — and the line + header carry the flags. 🚩 **Unknown availability raises no confirmation at all** (287); confirming there would turn a degraded stock service into a workflow. Basket lines wear the frozen *at add* pill so frozen and live never read alike. Reuses 167's modal — a second mechanism would be a defect · **built**: the two-phase discipline **graduated to `confirm-action.ts`** (`committing` / `repreviewing` / `isCommitting`), so the second action to take that path delegates rather than re-types it — one action, one `requestId` across the acceptance and the re-ask, proven on the wire. 🚩 `belowAtpAsk()` is the client-side predicate and exists so the console never draws an acceptance it cannot state truthfully: it answers `null` for 167's kind, a missing token, figures that are not both readable numbers (**which is how a degraded read arrives**) and anything within availability — so *unknown never asks, at any quantity* is a property a pure test holds. A block that cannot be stated is still an ask and therefore still not an add, so the panel says so rather than leaving the agent watching a basket that did not move. A failed **acceptance stays in the sheet** (same id, same token — pressing again is a retry of one action) and the panel behind it stays silent: one refusal, one voice. `AvailabilityPill` is now ONE component in two registers via `keyBase` — `search.atp.*` reads *12 in stock*, `line.atp.*` reads *12 at add*, and `frozenAvailability` sends `known:false` to *unknown* whatever quantity rides beside it; the drive measures that the two never read alike for the same item. The line's `belowAtpAtScan` and the header's `hasBelowAtp` are drawn from the server, never re-derived. The sheet draws **no money and no proposed quantity**. 168's interim key was deleted with its call site. Proof: 11 pure + `callcenter-drive` 326/326
- [170](170-basket-corrects-itself.md) — theBasketCorrectsItselfAndTheReceiptIsEngineTruth · **done** · map 126 · blocked by: 168 · spec 160 · quantity, unit of measure, void — each a verb returning the whole state, rendered not patched; lines show conditions (VAT is its own condition) and fired promotions. 🚩 **The console never sums lines** — `totals` is engine truth, and a client-side total is how the web starts quoting a different number from the till. The delivery fee **quotes live** as the basket moves, with `waived` an **outcome shown, never a control** (note 4's correction). ⚠ the fee *rule* is 156's, out of scope · **built**: two pure modules, because both rulings are rules rather than renderings — `basket-view.ts` is 🚩 **structurally incapable of summing** (`receiptView` takes `totals` and is *never given the lines*, proven by a drive whose totals deliberately disagree with the lines on screen), and `line-edit.ts` holds the three corrections as ONE action model with the id discipline in one place plus the two client-side rulings: **only a real correction is sent** (unchanged, unreadable, and zero-or-below — which is a *void*, and void has its own verb) and **what the console does not rule on stays the server's** (no client per-line cap, no unit the projection did not offer). A line says what it costs and why, with the condition run and the offer run told apart **in words** — the engine projects one discount twice, and two −8.40s in a row read as two deductions; a promotion's amount is the OFFER's and says so when it spans lines. The quantity field is 🚩 **rendered, not patched**: the draft lives only while that line's correction is unfinished, so a refusal, a decline and a `QTY_INVALID` all leave the engine's figure standing. The unit control tests *is there an alternative*, not list length. A raise beyond availability takes 169's path unchanged — same sheet, same `requestId`, same token — but the sheet's **words** are the verb's (*Raise it anyway*, never *Add it anyway*): one mechanism was never meant to mean one vocabulary, and there is exactly ONE acceptance mounted, fed by whichever verb raised it. §7's three codes are worded by the console and drawn **on the line**, on the acceptance's failure as well as the first send. The fee re-quotes live and states its threshold so the crossing is watchable; `waived` is an outcome with no control on it. Proof: 30 pure + `callcenter-drive` 390/390
- [171](171-guidance-strip-three-classes.md) — theGuidanceStripReadsThreeClassesAtAGlance · **done** · map 126 · blocked by: 161, 170 · spec 160 · the differentiator: a **wrapping** strip (never a sideways scroll), three classes told apart by rank, treatment **and words** — blocked offers say why in the agent's words, never the wire code. The **discount definition is headline-sized** (as a caption it disappears); no savings total exists. 🚩 **No figure *formatted as money* anywhere in the region** — the narrow rule, because server descriptions carry currency words nobody may edit (`“2 PC for 29.95 SR”` is in our own 098 captures). Load-bearing, not polish: an open card spans both columns, the body is clamped at 18rem with the outcome banner **outside** it, and the top-ranked actionable card opens **by construction**. 🚩 the drive must assert **what is visible**, not just region height · **built**: one pure module (`guidance-view.ts`) holding every rule the strip has — the three classes, the order, the definition wording (161's, from `@/core/`) and the skip words — and a component that arranges them and owns no vocabulary of its own. The classes are a rendering **rank**, never a re-sort: the list comes out in the order §3.1 sent it, and the **skip is asked first** (an offer the engine never evaluated is out of reach whatever its progress claims). 🚩 The **headline definition needs a wire field that does not exist yet**: `NearMiss.discount` is taken as an optional additive block (§9) and the card **degrades to the server's description** while it is absent — a BackOffice 787 dependency, live in the code and the drive but against no frozen fixture. The set statement rides EVERY card (a closed card stating a delta with no cardinality implies *buy this one item*), and it reads `any 1 from this selection · 42 qualify` because the wire carries a groupingId and no NAME. Open-by-construction is proven by re-ordering the server's list; the span is measured (826 vs 409px); the head is proven outside the clamp by scrolling the body to its end. The money scan excludes `data-cc-server-text` (pass-through descriptions) and **self-tests that it still sees the console's own figures**. Six of 138's nine states — `adding · didNotFire · firedOther` are 172's, in this same drive file. Proof: 19 pure + `callcenter-guidance-drive` 59/59, `callcenter-drive` still 390/390
- [172](172-one-click-add-closes-the-gap.md) — oneClickAddClosesTheGapAndSaysWhatHappened · **done 2026-07-28** · map 126 · blocked by: 171 · spec 160 · a grouping is a **set**: honest cardinality plus a ranked, ATP-filtered handful of **three — the server's `topN`, never a client slice** (at five, opening one offer pushed every other below the fold). Arabic on the **meta line**, which is what made the ruling cost zero pixels. `Search the other N` **hands off to the item search**, not a second list. 🚩 **Three outcomes because the re-price is the engine's**: fired · fired a *different* offer · **did not fire** — where the offer stays and only its meter moves, since silence reads as a broken button and a vanished card reads as a bug. Get-side absence acknowledged once at the region's edge, and it **disappears on its own** when 787-C lands. dep: BackOffice 787 → **103/103 drive** (171's 59 + 44) + 33 pure over `add-outcome.test.ts` and the new get-side block. `ResolvePrereq` is asked **on demand, for the open card only** and never on `Bby/*` (the drive records every request). 🚩 Two contract gaps found by building it: the hand-off needs an **optional `offerId` on `ItemSearch`** (§1.1 has none — the console sends it additively and the scope chip says *narrowed to*, never *filtered*, so an ignoring server produces no lie), and **`truncated`, not `eligibleCount − shown`, is what says there IS a rest** — the resolution is availability-filtered, so 42-minus-3 would route the agent after rows the server withheld. `prereqKey` omits `version` **by design**: the row that launched an add must not move under the cursor, and the outcome banner (pinned outside the clamp, 171's slot) is what reports the re-price instead. dep: BackOffice 787 must send `topN: 3` — the frozen fixture still says 25
- [173](173-header-complete-before-submit.md) — theHeaderIsCompleteBeforeSubmitIsOffered · **done 2026-07-28** · map 126 · blocked by: 166 · spec 160 · slot (a **soft** gate on CLCN — a lapsed slot warns, never blocks; the Wasfaty rule fell out with the non-CLCN kinds), document source + mandatory reference, each settling into a re-openable chip. 🚩 **Submit is never mysteriously dead** — the reason comes from `capabilities.submitBlockers`, so the console renders the server's own completeness answer instead of re-implementing it. `MyDocumentSources` is session-derived (no user-id path param — 137 broke “delegates verbatim” on purpose). ⚠ **fulfilment mode (154) and payment type (155) are NOT captured here** — they are holes in the frozen contract with no axis to write to · **built**: one pure module (`submit-blockers.ts`) holding the whole answer — code → phrase → owning chip — read by BOTH the receipt and the chip row, so the two can no longer disagree about which section is at fault; it replaced the chip row's private table and the receipt's `t(code, {defaultValue: code})`, which rendered a raw machine code to the agent for any blocker shipped server-first (§9). Codes that would print the same sentence collapse to one. 🚩 The soft gate is honoured **twice**: a held slot that has lapsed keeps its chip *settled*, marks itself and warns in the flow with submit still live, and a window that goes mid-call is drawn in the **attention** register inside the picker rather than as a failure. The mandatory-reference rule stays the door's — one verb carries both fields, *Save* is never disabled on an empty reference, and the drive proves the console **sent** it and worded the refusal instead of predicting it. `MyDocumentSources` carries no user id, asserted from the recorded request. `Slots/AvailableSlots/{storeCode}` **graduated to `@/core/services/lookups.ts`** (uncached by construction) — two features ask it now and a feature may never import a feature. Ruled while building: *re-open in place* is a modal, on 167's precedent, and there is **no clear-the-slot** path. Proof: 8 pure (the locale file asserted against — a `t()` with no backing key is this ticket's own failure mode) + `callcenter-drive` **416/416**
- [174](174-placing-the-order.md) — placingTheOrderHasNoOptimismAndSurvivesARetry · **done 2026-07-28** · map 126 · blocked by: 170, 173 · spec 160 · submit takes **only the transaction id** — the document is built server-side, which is what makes the browser unable to influence what the caller pays. 🚩 **No optimistic hand-off** (ruled against CC2's own recommendation): the receipt holds until a `documentNo` exists, because an optimistically confirmed order that then refuses is a call the agent can't take back. 🚩 `alreadySubmitted` is treated **identically** to `submitted` — once-only is keyed on the transaction id, and any branch that makes them look different is the defect. `SUBMIT_REFUSED` names the field and keeps the order; 🚩 `SUBMIT_UNAVAILABLE` is a **503 that must carry the envelope**, or `core/api.ts` maps it to `server` and a routine retryable outcome reads as “unexpected” — asserted in the drive, not noted. dep: BackOffice 786 · **built**: `submit-outcome.ts` makes *the two successes are the same news* STRUCTURAL — it returns `{ documentNo }` and drops the outcome word and `replayed`, so no surface downstream *can* tell a replay from a first submit (the pure test asserts the two EQUAL; the drive compares the rendered panel word for word). 🚩 The predicted defect was real and on the other side: `core/api.ts` mapped **every** 5xx to `kind:'server'` *before* it looked at the envelope, so an envelope-carrying 503 read as “unexpected” anyway — fixed generically (a refusal carrying `errors[0].errorCode` outranks its status; an uncoded 5xx is still a server fault) with `src/core/api.test.ts` new to pin it. One press = one `requestId`, reused across every retry of that press including an unclassifiable failure (the case where the submit may have landed); a press with no standing failure mints a new id. 🚩 A refusal carries the `version` it was made against and stops being said the moment the agent corrects the order. Ruled at review: **no client state for a numberless success** — §7 has no unknown submit outcome and it could only be paid for by pressing a verb the door just refused; `canSubmit` reading moved up to the page with every other capability gate. Proof: 11 pure + 4 core pure + `callcenter-drive` **445/445**

- [175](175-nothing-enters-an-unaddressed-order.md) — The order opens, and nothing may go into it yet · **done 2026-07-28** · map 126 · blocked by: — · grilling · **HITL** · **contract v1.3, additive** (not the major it was raised as — §2's own definition of `capabilities` is *"what the server will accept right now"*, so the ANSWER moved and the definition did not; a definition whose answer may not change is a constant, not a capability). `canAddItem = open && customer != null && plantSource != "seededAtOpen"`. `open` still seeds the plant — map note 6 and 129's premise untouched, the engine cannot hold a plant-less order — what changes is that the seeded value is **labelled unchosen** rather than reported settled, so the fix was a **missing vocabulary word**, not a rebind. `plantSource` gains `seededAtOpen` + `chosenForPickup` (four); the chip says *not chosen* via a `STORE_NOT_CHOSEN` **submitBlocker**, never a client rule (`header-chips.ts`'s one-table discipline); a shut gate makes the item command line **absent, not disabled**. The pickup one-click *Yes, this store* is `setStore` with the store already held — no plant move, no confirmation, but a choice now **on the record**, which is the whole difference from a chip that was already settled; under delivery there is no shortcut, so §6.3 makes **caller-first true by construction**. 🚩 **Session 1's hope was wrong and acting on it would have broken every `open`**: `IsCustomerRequired` is an OPEN-time check on `options.CustomerId` that *throws* (`PosTransaction.cs:916`), not a per-add gate, and the `CallCenterOrder` row sets it `false` with the reason written down (*"requiring one would brick `OpenAsync`"*) — a flag whose NAME matches your rule is not evidence, the enforcement site is; 871 carries a **negative** Done-when so the next reader does not "fix" it. Owner rulings: `removeCustomer` with lines **keeps the lines** (no WPF precedent — CC2 has no basket, the web creates the state), store-less district is a **hard block** (`NO_DELIVERY_STORE_FOR_DISTRICT`), **order note in**, P2E-forces-online + delivery-only sources **in** (source rules ≠ kind rules; *"we might need the payment type"* → 155), density toggle + launch seeds **out**. 🚩 **The store code is never saved on the address** — resolved from the district while the order is created, so the same address can derive a different store next week; that is why `plantSource` belongs to the ORDER, and it corrects 154's unfiltered-estate ruling to **collection only** (delivery reads the address book + district assignment). 🚩 **One answer outgrew the ticket** — the owner ruled the sidecar's fields into the `PosTransaction` snapshot (`Loyalty`/`Insurance`/`Order` are already 1:1 companions; `TransactionOrder` already holds `DeliveryType`), which would dissolve §6.4's two-store ordering — carried whole to **178** because v1.3 does not depend on which side wins. Server: BackOffice **871**; found on the way, **872** (`DocumentTypeCatalog.cs:739` never sets `IsGs1Required`, default `true` ⇒ the engine demands a scan for a serial-controlled article on an order captured over the phone). Also minted **179** (the address editor: nine fields, server label catalogue, SPL format-only check, one-box location search that is NOT a cascade, plus CC2's two-step OTP signup that is on no contract). 🚩 **Pattern**: the review found the console lying about a field it had already been told the truth about — 154 filed `plantSource`'s double duty as "contract hygiene" and it was the behaviour itself. A defect filed under hygiene reads as optional
- [178](178-the-transaction-absorbs-the-sidecar.md) — The transaction absorbs the sidecar · **done 2026-07-28** · map 126 · blocked by: — · grilling · **HITL** · **it does not** — raised on an owner ruling to move the header capture into `PosTransaction`, **withdrawn by the owner** once grounded (*"we can add columns there — any missing column — so we don't touch the engine"*), and the grounding is what settled it. 🚩 **The protocol state cannot move, and it is exactly what §6.4 protects**: `CallCenterEngineSession.ScopeAsync` deliberately **never flushes**, because §5's ask half runs the engine door and 798 requires the previewed instance be *"discarded without `SaveAsync`"* — writing a confirm token into the snapshot would commit a re-price the agent never authorised and make their confirmation read as a replay. So a sidecar survives holding the ledger + tokens under ANY absorption, and §6.4's two-store hazard survives with it: **engine risk paid, nothing removed.** 🚩 Second collision: `PosTransactionStore.SaveAsync` treats a null companion as *"don't touch"* not *"clear"* (a guard protecting migration-057 backfilled provenance), while 175 just ruled `removeCustomer` **clears** the customer and address — so absorption needed either a tombstone door or a relaxation of that exact guard. ⚖ Recorded fairly rather than stacked: the snapshot bump was **not** the expensive part (`PosSnapshotSchema` is v8 and **v4 was literally this pattern** — *"order metadata … additive/nullable"* — and `TransactionOrder` already holds `DeliveryType`), and absorption's real prize was **provenance** (the address visible to the sweeper, the reconciler, FindInvoice, a future web till) — worth naming, not worth the two findings; it returns as its own effort if wanted. 🚩 **804 had already written this reasoning into `CallCenterSession`'s class comment and it survived the re-test** — a design note that states its own reasoning wins a re-litigation a bare decision would lose. **The audit the owner asked for**: exactly **one new column** (`OrderNote`, `StringMaxType`), **one changed default** (`PlantSource`'s field initialiser is `OperatorOverride` — the 175 defect sitting in the initialiser, where a test that only drives `open` would miss it), **one deferred** (payment type → 155); the nine CC2 address-capture fields correctly stay out (the sidecar holds `AddressNumber`; the address book is the system of record → 179). Both live changes folded into BackOffice **871**; contract §1.2's under-review flag replaced by the ruling; **no** new BackOffice issue, no amendment, no snapshot bump. ⚠ Residue on the map's fog: §6.4's hazard is untouched, and one option removes it **without touching the engine** — move the sidecar ROW to the `CallCenterStore` connection, since `PosUnitOfWork` is already ambient-aware (029a) so both writes land in one db transaction; left as fog because the deploy/backup/dual-running cost is unexamined. **Owner addendum 2026-07-29**: the sidecar should hold **what the OMS document holds** so the submit builder copies rather than translates — add `DocumentType` (`CLCN`), add `PaymentType` (`C`/`O`/`R`), recode `DeliveryType` to `D`/`P`. 🚩 Corrected on grounding: **online is `"O"`, not `"P"`** (`"P"` is `PickInStore` on the delivery axis — crossed letters, and no downstream validation would catch it), and **the wire does not change** (frozen values ⇒ §9 major, and the console would render `"P"` at the agent). 🚩 And it exposed a live wrong value: `Submit.cs:196` derives `CashOnDelivery = isDelivery` — and the owner has since ruled the axes **independent** (*"any order could be paid online or cash on delivery"*), so it fails **both ways**: a pickup order is stamped online-paid, and a delivery caller cannot be offered online at all. ⚠ Not a tender — nothing is paid on the console; the field tells OMS to send a payment-gateway link, so note 4 is untouched. All on 871; 155 rules the default and the source-forcing
- [179](179-the-address-editor-and-its-capture-contract.md) — The address the agent creates, and the contract that carries it · **done 2026-07-29** · map 126 · blocked by: — · grilling · **HITL** · **contract v1.9, additive** ([§6.5](assets/136-cc-contract/CONTRACT.md)) — one new code, **no new verb, no new field, no new capability**. 🚩 **Three of the four questions were answered by things already built**: the payload by [801](C:\Work\DMSCO\BackOffice\.issues\801-callcenter-web-door.md)'s shipped `CallCenterWebAddressRequests.cs` (the body is `{LabelCode, Address: BusinessAddress}` — the WHOLE 25-field model, so the contract was never missing, it was **wide**); the ~1,000-district fetch by `lookups.ts:53`, which already pulls every district at `staleTime: Infinity` for Screen 2's Change Store picker and which 137 ruled OFF the door; and the undeliverable district by `SdDistrictModel` already carrying `StoreCode` AND `TempStoreCode` on the wire — so §2.3's *visible and unpickable* needs **no new read** and the ticket's either/or was a false choice. 178 killed the fourth (the sidecar stays, the book is the system of record). **So the session was about a seam nothing on the ticket asked about — the address book is a CUSTOMER store on a DIFFERENT door, and two of its writes reach into the order.** **(1) Edit re-pins**: the map had already ruled the district→store derivation pinned at *picks **or edits*** and `setAddress` carries only an `addressNumber`, so a `PUT` moving an address between districts leaves the order on a plant derived from a district that address has left, `header.address.line` still rendering the old composition — [797](C:\Work\DMSCO\BackOffice\.issues97-resume-drops-pcheader-plant.md)'s silent wrong price through an unwatched door. Ruled: **re-issue `setAddress` with the same number** — it already carries the re-derivation, §5.1's preview and `NO_DELIVERY_STORE_FOR_DISTRICT` — and the one server obligation is **NEGATIVE**: that call must not be short-circuited as a no-op, the only thing on this contract that looks idempotent and is not. ⚠ An edit can leave the book row **saved** and the order **refusing** it; the order keeps what it had and says why, rather than rolling back a correction the caller just made. **(2) Delete is refused** — `ADDRESS_IN_USE_BY_ORDER` (409): the sidecar holds a *number* while `Cc2DocumentHeaderBuilder.ApplyShippingAddress:81` copies *fields* and `GetCustomerAddresses` filters `IsDeleted = 0`, so the delete breaks the order at its **last** step. The console omits the control too, but **the refusal is the guard and the omission is the courtesy**. Clearing the order's address instead was rejected as cascading a book act into order state (it would silently shut §2.3's gate mid-call). **(3) The payload narrows** to CC2's nine + `LabelCode`, three constants server-stamped — 801's own *“a field the server silently discards is indistinguishable from one it honours”*, one field-set wider. ✅ Safe because `SdAddressService.UpdateCustomerAddress:486` is a **null-coalescing merge** (`existing.X = address.X ?? existing.X` ×25), so an omitted field is preserved and a narrow PUT cannot blank an SAP-synced column — the one risk that could have sunk it. 🚩 **Its flip side is a capture rule**: a field can never be emptied by omission, so the client sends `""` and **never** `null`, and the mapper must NOT normalise blanks to null — exactly the tidying reflex when writing one. **(4) The picker** is CC2's one box (not a cascade) over the already-cached list, pick commits both, snapshot-and-revert, current pick pinned above results rather than force-kept in the filtered array; Arabic inherits the no-folding limitation **knowingly**. The client **may** grey a store-less district: it asks *whether* there is a store, never *which*, so `address-book.ts`'s no-derivation rule stays literally true (predicate in a new module, not that file) and the server refusal stays authoritative — advisory-but-authoritative, the `capabilities` shape. ⚠ Residual: `staleTime: Infinity` makes an ops `TempStoreCode` flip invisible for the shift; the direction that matters is still caught server-side. **(5) SPL stays format-only** with **no verified affordance**, and live SPL verification is ruled onto the map's **Out of scope** — which is what makes the inheritance knowing rather than quiet. **(6) Surface**: one modal, two views (list ↔ form) — `AddressPicker`'s own comment already rejected modal-on-modal — and a **create auto-applies** via `setAddress`, since the new `AddressNumber` comes back on the create and the only reason to create mid-call is to deliver to it. Server work → BackOffice [878](C:\Work\DMSCO\BackOffice\.issues\878-cc-address-capture-and-order-acts.md). 🚩 **Pattern**: five tickets now (156, 157, 158, 176, 179) have become findings rather than restatements by reading what the ticket **inherited** before designing anything — at five it stops being a coincidence and becomes how a ticket written weeks before it is worked should be opened. Narrower: the merge was found by reading the **service**, not the endpoint, which de-risked the narrowing AND produced a rule a mapper written from the endpoint signature alone would have got backwards
- [176](176-fulfilment-mode-drawn.md) — Fulfilment mode, drawn where the agent asks the question · **done** · map 126 · blocked by: — (175 done) · prototype · **HITL** · owner gap review #2: no delivery/pick-in-store control anywhere. The wire is already settled (154, contract v1.1 `setFulfilment`) and spec 160 carved it out of the build — this is the undrawn surface, plus what the flip does to the address block, the slot chip and the fee line · **+155 (2026-07-29)**: the **payment chip** is drawn here too — settled-and-collapsed, but under `PickInStore` it reads **Pay on collection** while the wire value never changes, so it and the mode control cannot be drawn independently; and 🚩 the **delivery-only sources** (WSFD/P2E/DKSW, `DocumentSourcePolicyService`) turn out to be **this** ticket's half of 175's ruling — 155 owns only the payment half, and that half has no forcing rule in phase 1 · **DONE 2026-07-29 — contract v1.8, additive.** The arrangement was never the open question (175 ruling 9 chose it; spec 160's build settled that a chip opens to a MODAL), so the mode is the **first chip**, opening two full-sentence choices. 🚩 **The rail's two blocks are ONE block** — *Address* and *Collecting from* at the same pixels, so the flip moves no furniture (measured at 226 px both modes, not eyeballed), which dissolves the ticket's own collapse-or-mark question. Slot chip and the whole delivery region **absent, not zero** (156, capture-confirmed); the store chip drops its *(derived)* parenthetical where there is no address to derive from; and a collection order's missing collection time draws **nothing** (owner: no promise this map cannot keep). 🚩 **The client retained-address trace was BUILT and REJECTED** by the owner — blank after a refresh and in a second tab, so one order reads two ways ⇒ `header.retainedAddressLabel`, the label never the address. 🚩 **`capabilityReasons` minted here** for the delivery-only sources, over a per-capability field, because [153](153-console-keyboard-grammar.md) had already named it for the palette's identical problem — one field serves both. Found on the way: the **import-boundary gate is why every previous prototype was an illustration** (a prototype cannot import the feature it is about — this one lives at `console/__prototype__/` and mounts the real `ConsoleShell`), and **`STORE_NOT_CHOSEN` had never reached this client** (175's debt). Branch `prototype/176-fulfilment-mode`, 90/90 drive, 11 states captured, 5 of them named as stubs. Server half: BackOffice [877](C:\Work\DMSCO\BackOffice\.issues\877-cc-fulfilment-drawn-server-half.md)
- [177](177-v1.2-captures-land-on-the-client.md) — The v1.2 captures land on the client, and three of them are red · **done 2026-07-28** · map 126 · blocked by: — · task · note 15's budgeted revision arrived and the client half is landed: `payloads.ts` and both drives read the capture shape, and the corpus is now **two tiers, named as such** — the wire's own bytes, plus an `unreachable-v1_0.json` block (NOT in `.issues/assets/`, so nobody mistakes it for a capture) holding only what 859 blocks, read by the tests AND the drives so they cannot drift. 🚩 **07 ruled hold-both**: the client's *both successes are the same news* stays (it is a claim about this repo and must hold the day 860 lands), and the capture's real `409 SESSION_CLOSED` is asserted beside it — including that **no order number survives anywhere**, which is 860's harm as one assertion. 🚩 **Ship-blocked ruled detect-and-say-so** (owner): two of the four paths LIED to the agent — a below-ATP acceptance and a plant-rebind confirm both returned 200 and did nothing, silently (858) — so `commitWasSwallowed` draws a banner, 🚩 whose FIRST version was unsound and `/standards-review` caught it — `replayed: true` alone is not evidence, since §6.4's crash-resolution is by construction a replay answer over a commit that DID apply, and the captures killed both obvious repairs too (`version` advances on both swallowed commits because `SaveAsync` blind-increments it, and fixture 04 answers `hasBelowAtp: true` over ZERO lines). So the predicate now takes *did the accepted change actually happen in the returned projection*: the add asks the basket's own quantity, the rebind asks the plant against the one the token pinned. It offers **no retry** and disappears on its own when 858 lands. 859 and 860 ship as they are — both already say something honest. 🚩 **Two real client defects found by driving the real projection**: a blank `offerId` is a REAL wire answer, and the strip keyed on it — React de-duplicated two distinct offers into one card and opening one opened both (fixed with `GuidanceCard.cardId`, a render key that is never an offer identity, so 859 stays visible); `BasketPanel` and `StoreMoveConfirm` keyed promotion rows the same way. Also folded in v1.2's `addItem.uom?` and `setSlot.day/description/from/to` — ⚠ the four slot fields are mapped by LITERAL name and 804's `CreateDocument :18730-18734` is the authority. Proof: typecheck + lint + build clean, **537 pure** (was 8 typecheck errors and 11 files failing at import), `callcenter-drive` **461/461** and `callcenter-guidance-drive` **107/107** (both were unrunnable)

- [180](180-console-catches-up-with-the-contract-spec.md) — The console catches up with the contract (spec) · **ready** · spec — consumable by /to-tickets · map 126 · **phase 2 of the client track**: spec [160](160-callcenter-console-spec.md) specified the frozen **v1.0** core and it shipped (161–174), then carved out seven tickets by name — **all seven are now resolved**, plus four more the gap reviews found, and the contract has moved **v1.0 → v1.10**. This is what the console owes that movement. 109 stories, nine slices, **all additive, no new route**: the **fulfilment** (v1.8) and **payment** (v1.4) axes as first and last chip; `deliveryFee.waivedReason` (v1.5); one **about this item** panel holding **price check** (v1.6) and **stock elsewhere** (v1.7) as two independent reads that fail separately; the **address editor** and its two order-touching book acts (v1.9); the **coupon** chip, list and reversal plus the fourth `prereq.kind` (v1.10); the **loyalty signup** inline in the rail (v1.10); and the **keyboard grammar** — four keys and a palette, the only slice with **no server dependency at all**. 🚩 **Much of the drawing already exists and is unwired**: `FulfilmentPicker`, `PaymentPicker`, `CouponPicker`, `SignupPanel` and their view models were built by the prototype tickets and are proved by drives, but `CallCenterConsolePage` passes no handler — so most slices start by wiring an existing component to a new mutation, and `api.ts` is missing **eight verbs** the contract now carries. Only two surfaces are genuinely undrawn (the about-this-item panel, the palette). Testing: **RTL still uninstalled** (083's ruling, restated by 160 and re-confirmed here) — pure vitest modules + Playwright drives, with `fulfilment-176` (90/90) and `coupon-159` (84/84) **re-pointed at the wired console** rather than rewritten; three new drives, the keyboard one asserting its **negative** cases as hard as its positive ones. ⚠ Every slice ships against stubs and each drive says so (177's rule); server halves are BackOffice **804** + **879** (implementing now) · 871 · 875 · 876 · 877 · 878. 🚩 Known red before this spec starts: `tools/callcenter-drive.mjs` fails on a **clean tree** (175's opening gate vs a drive that predates it)

## The call center goes to the web — build, phase 2 (spec 180)

Tracer tickets sliced from spec 180 — everything the console owes contract **v1.1 → v1.10**, after
spec 160's frozen-v1.0 build (161–174) shipped and its seven carve-outs were all resolved. 🚩 The
slicing re-verified the spec's load-bearing claim and **corrected it**: `ConsoleShell` really does
already accept all seven handlers and `header-chips.ts` really does emit all seven chips with
`CallCenterConsolePage` supplying none — but the spec said *two* surfaces are undrawn and there are
**three**. `AddressPicker` is **pick-only**: no create form, no edit form, no district picker, because
179 was a decision session and not a prototype. Model gap is small (`PriceCheckResult`,
`StockElsewhereResult`, `canPriceCheck`), so no prefactor — each slice adds its own. Four chains:
**{181 → 191}** (repair, then the keyboard), **{182 → 184}**, **{185 → 186}**, **{187 → 188}**, with
183, 189, 190 free-standing and **192 last** behind five of them.

- [181](181-console-drive-green-on-clean-tree.md) — theConsoleDriveIsGreenOnACleanTree · **done** (461/461 on the committed fixtures, no page errors; guidance drive re-run 107/107 — it never depended on the gate; no production change) · blocked by: — · repair, not a feature. `tools/callcenter-drive.mjs` fails on a **clean tree** (verified by stashing) at `[data-cc-search-add="200145"]`: the row renders, its *Add* does not, because `ItemSearchPanel` draws the button only when `add.onAdd` is passed and the page passes it only while `canAddItem` holds — 175's opening gate meeting a drive written before it, failing since, unnoticed. **The gate is right and the drive is wrong**: fix the drive's setup (attach a caller, choose a store), never relax `canAddItem`. Check the guidance strip's *Add* for the same staleness. No server dep, no i18n, no production change expected — if one proves necessary that is a finding, not a silent fix. Two more stale assertions swept with it (the seeded store chip needs attention under 175; the chip row is seven, not four, after 176/159). 🚩 Finding for [189](189-coupon-names-itself-and-comes-off.md): under the *uncommitted* recapture of fixture 03 (`prereq.kind` material→coupon) the guidance strip draws no card at all for a coupon-kind near-miss
- [182](182-mode-flips-the-screen-payment-word-follows.md) — theModeFlipsTheScreenAndThePaymentWordFollowsIt · **done** · blocked by: — · **Slice 0** · widest spine reach in the spec, and it retires the shared unknown behind five others: *does an unwired prototype component mount in the real page against a real mutation?* Fulfilment becomes the **first** chip and payment near-last, both settled; `FulfilmentPicker`/`PaymentPicker`/`fulfilment-view`/the chips all exist and `ConsoleShell` accepts both handlers — **the page supplies neither**, which plus the two verbs is the work. The flip's consequences ARE the slice: the rail's second block is **one block with two faces at the same pixels** (226 px, measured not eyeballed), the slot chip and the receipt's whole delivery region **absent not zero**, the store chip dropping *(derived)* under collection **whatever `plantSource` says** (a capture keeps `derivedFromAddress` beside `address: null`), `retainedAddressLabel` server-supplied because a client that remembers is blank after a refresh, `capabilityReasons` wording a shut chip, **Pay on collection** under pickup while the wire value never moves (which is why the two chips cannot be built apart), and `STORE_NOT_CHOSEN` finally a real blocker rather than the *unknown blocker* phrase. Re-points `fulfilment-176-drive.mjs` at the wired console · dep: BackOffice **877** + **871** · **DONE 2026-07-29** — drive 108/108 on the real `/callcenter`. Two findings: the block's **height is NOT** identical (address 72 px, collection 50 px) — what is identical is where it **starts** (226 px, all eleven states), and it moves nothing only because it is the rail's **last child**, now asserted; and consequence 9 was a live defect — `paymentWordKey` worded `Receivable` as *On account*, so the branch and its key are gone. `STORE_NOT_CHOSEN` needed no code, only a test
- [183](183-order-note-reaches-the-order.md) — theOrderNoteReachesTheOrder · **done 2026-07-29** · blocked by: — · built: the smallest slice here and genuinely one field wide on both sides: 175 ruled the order note **in**, nobody built it, and `api.ts` has no `setOrderNote`. A chip like every other header field; clearing it (`null`) is a real act so a stale instruction never travels with the order; no submit blocker. The sidecar's `OrderNote` is 871's one new column · dep: BackOffice **871** · 🚩 the row now ends with the TWO chips an order need never fill (coupon, then note) and blank in any form reads *unset*, so a cleared note never draws as settled-and-blank · the drive's read-back goes the way an agent really does — reload meets *you have an order open* (law 9), *Resume* reads it with `GET State` — on two new stub options (`staysOpen`, `statePersists`); a second tab is handed the SAME stub · `502/502` · ⚠ stub only, no live SIS.Api
- [184](184-waived-delivery-fee-says-why.md) — aWaivedDeliveryFeeSaysWhyItWasWaived · **done** · blocked by: 182 · the *free over …* line is gated on `!waived`, so today it **vanishes at the exact instant it would explain itself** — that is the defect, and `deliveryFee.waivedReason` (v1.5) is the fix. 🚩 The console **must not** derive the reason from `gross` vs `thresholdGross`: the rule lives in `CallCenterDeliveryFeePolicy`, the one copy the till, the live quote and the submit all call, and a second implementation here is precisely what 156 was raised to prevent. An unrecognised reason draws waived-with-no-reason, never a guess. Blocked by 182 because that slice decides whether the region exists at all · dep: BackOffice **786 §2** · **DONE 2026-07-29** — most of the spine landed **inside 182's commit** (a region drawn at all forces the question of what its waived arm says), so `feeLine` + the three drive scenarios arrived early. What was left was the one thing the Proof asked for and 182 could not give it: the degrade for an **unrecognised** category lived in `t(key, { defaultValue: '' })` — a §9 rule decided by whether a translation key happened to resolve, unprovable purely and silently changed by one empty string in the JSON. `WORDED_REASONS` moved into `fulfilment-view.ts`, `feeLine` degrades outside it, the receipt's `t()` is unguarded. The anti-derivation guard is **structural**: the waived arm of `FeeLine` carries no `thresholdGross`, so there is no number in scope to compare · 18/18 pure, drive 108/108
- [185](185-search-row-expands-and-quotes-the-real-price.md) — expandingASearchRowQuotesTheRealPrice · **done 2026-07-29** · blocked by: — · ⚠ **a genuinely undrawn surface** — no prototype; a build, not a wiring. The new *about this item* panel plus `priceCheck` (v1.6), replacing an answer currently read off the row's `≈` estimate that runs **~13% under** what the caller pays — a number built to sort a list, read down a phone as a price. **`unitPrice.gross` must equal the basket line's** for the same item under the same header: that equality is the whole point. Renders with `SAR` because it IS engine money; the `≈` estimate never leaves its meta line, so no row changes shape mid-list; a pricing failure is a **typed refusal, never a fallback to the estimate**; the offers region holds **no figure formatted as money at all** (asserted in the narrow form over a fixture carrying a currency word, since the broad *no `SAR`* form fails on server text nobody may edit); `offersComplete: false` says offers were not fully checked and flips true with **no client change**. Gate is `canPriceCheck` = `canAddItem`'s predicate. Takes no claim, so asking the price mid-basket costs the call nothing. 🚩 `Pricing/Simulate` must **not** be reused, route or body · dep: BackOffice **875** 🚩 **Shipped**: `priceCheckPanel` (`price-check-view.ts`) is FOUR states — `shut` · `pending` · `refused` · `quoted` — and three of them have **no money field at all**, so *never a fallback to the estimate* is structural rather than remembered; the module is handed the whole row, estimate included, precisely so its absence is a property of the model. The offers half is literally `guidance-view`'s projection (one vocabulary, one implementation). The equality is asserted off the capture's OWN two legs — fixture 12 price-checked item 200021 and then added it — rather than off two numbers an author made agree. Drive `tools/item-panel-drive.mjs` 31/31 proves on the WIRE what no pure test can: the row's meta line byte-identical across the expansion, the shut gate sending **no `PriceCheck` at all**, `transactionId`+`itemNumber` and nothing else on a bodyless GET, and `Pricing/Simulate` never touched. Reviews moved two real defects: an offer with no `progress` read as *applies to this item* (now branched on class, silent where there is nothing honest to say), and the offer handle keyed on the blank `offerId` (859) instead of `cardId`. The *priced by* run is now a shared `Conditions.tsx` the basket uses too.
- [186](186-panel-names-the-stores-that-have-it.md) — thePanelNamesTheStoresThatHaveIt · **done 2026-07-29** · blocked by: 185 · `stockElsewhere` (v1.7) in the same panel, and the **separation is the design**: the price is a lock-free engine run inside SIS.Api, this is the **only remote HTTP hop on the whole contract**, so they render together and **fail independently** — a stock outage must not cost the agent the price. **Read-only by ruling, not omission** (a rebind re-prices every line and would invalidate the list it was clicked from; the panel may name that path in words). One availability number and it is **ATP**, same definition as the search row — the till shows on-hand beside it, which is how the larger number gets promised. Nearest-first, no-stock dropped, own plant excluded, capped at 10 with `withStock` + `truncated`. 🚩 **Unknown distance is a value, never a missing store**, and `distanceKnown: false` makes the whole list honestly unranked by store code rather than a plausible ranking measured from `(0,0)`. **`available: false` means unknown, not empty** · dep: BackOffice **876** · **built**: `stock-view.ts` + `StockBlock`, a **sibling** of every price state with its own query, own cache key and own model — so the independence is structural: a stock outage leaves the price byte-identical and a price refusal leaves the store list whole, both asserted on the wire, as is the fact that these are TWO calls (if one read ever served both, the independence would be a coincidence). 🚩 The three-way answer is three **states**, not three sentences a component picks — only `listed` carries rows, and `available` **outranks the rows**, because a partial list from a hop that has just reported failure is a claim the server itself withdrew. Read-only is asserted as an **absence**: the drive counts `button`/`a`/`input` inside the whole block and requires nought, and the store-change path is one sentence. ⚠ **No capture and deliberately never one** (876): the second hop is a remote stock service no test host can reach, so the only scenario recordable is an outage and committing it would freeze an outage as the contract's shape — the pure corpus and the drive's stock answers are stubs to §3.5's documented shape, said in the drive's own header. Two review findings moved code: `truncated` was trusted verbatim, so `withStock: 23` with `truncated: false` would have printed *23 stores with stock* above three rows — now the one field reconciled rather than read, and only in the direction that can harm (a count line may never out-claim its own list); and the unranked fallback sorted by `localeCompare`, whose collation of real mixed codes (`BZ01` beside `1102`) is locale-dependent, so it is an ordinal compare. 🚩 185's *the panel holds no figure at all* is narrowed to the **price half** on purpose — the new region's figures are availability (an ATP count, a kilometre), neither money nor anywhere an estimate could go, and *the estimate appears nowhere in the panel* still reads the whole panel. Accepted: the hop fires on every expand (nothing gates it, and gating on the row's own `atp` would make *who else has it* depend on the number the panel exists to go beyond) · dep: BackOffice **876** (done) · **21 pure** + drive **73/73** (was 31/31), 720 pure suite-wide, lint and build green
- [187](187-agent-creates-and-corrects-an-address.md) — theAgentCreatesAndCorrectsAnAddressMidCall · **done 2026-07-29** · blocked by: — · ⚠ **the largest ticket in the spec, and the third undrawn surface** — `AddressPicker` is **pick-only** today; 179 was a decision session, not a prototype. One modal, **two views (list ↔ form)** because `AddressPicker`'s own comment already rejects modal-on-modal. Create collects CC2's nine + label and **auto-applies** via `setAddress` (the number comes back on the create, and the only reason to create mid-call is to deliver to it). District picker is CC2's **one box, not a cascade**, over the ~1,000-district list already cached at `staleTime: Infinity` for Screen 2 — no new read. A store-less district is greyed: the client asks *whether* there is a store, never *which*, so the no-derivation rule stays literally true and the server refusal stays authoritative. 🚩 Two capture rules an endpoint-signature reading would get backwards: the payload **narrows** safely because `UpdateCustomerAddress` is a **null-coalescing merge**, and that merge's flip side is that **a field can never be emptied by omission** — so the client sends `""` and **never `null`**, and the mapper must not tidy blanks to null. ⚠ The book is unreachable before customer attach (§6.3) · dep: BackOffice **878** (✅ the five routes already ship) · **built**: `district-choice.ts` (whether a store delivers, never which — the negative asserted by reading the whole projection back as text) + `address-capture.ts` (the mapper) + `AddressForm.tsx` + `AddressPicker` grown to two views, one dialog. 🚩 **The Proof’s "ten fields" is superseded by the shipped type**: BackOffice 878 is `done` and `CallCenterWebAddressCapture` carries **twelve** values plus `labelCode` — ten was CC2’s field COUNT (city, district and GPS each a pair). ⚠ And the pair is why it matters: `GpsLat`/`GpsLon` are non-nullable decimals the service assigns **unconditionally**, so they are the one omission the merge cannot preserve — the console captures neither and **round-trips both**, or every edit zeroes a real fix. Two review findings fixed: the `HOME` default is a CREATE’s only (defaulting in the mapper let a street correction silently relabel an unlabelled row), and a create **seeds** the book from its own answer rather than re-reading it — no call either way, but the refusal path no longer shows a book missing the address just saved. `NO_DELIVERY_STORE_FOR_DISTRICT` finally has a sentence, worded to claim nothing about the book because it arrives both as a plain pick and as a create’s auto-apply (§6.5’s named consequence, now driven). Adds one ungated reference read (`SdDocument/AddressLabels` — 179 §2.2: labels are server data, not an enum) and caps the ~1,700-row district list at 30 with an honest truncation line. **188 deferred by construction**: edit ABSENT (not disabled) on the order’s own address, no delete control and no delete verb. Proof: **46 pure** + `address-editor-drive` **51/51** + `callcenter-drive` **461/461** unchanged
- [188](188-editing-re-pins-deleting-is-refused.md) — editingTheOrdersAddressRePinsTheStoreAndDeletingItIsRefused · **done 2026-07-29** · blocked by: 187 · split out because these two rules are sharp, independently testable and carry the real risk: **the address book is a CUSTOMER store on a DIFFERENT door and two of its ordinary writes reach into the order.** **(1)** `setAddress` carries only an `addressNumber` and an edit does not change it, so a `PUT` across districts leaves the order on a plant derived from a district the address has left ⇒ **re-issue `setAddress` with the same number**, which already carries the re-derivation, §5.1's preview and `NO_DELIVERY_STORE_FOR_DISTRICT`. ⚠ An edit can leave the book **saved** and the order **refusing** — the honest outcome, drawn rather than prevented, never rolled back over a correction the caller just made. **(2)** Delete is refused `ADDRESS_IN_USE_BY_ORDER`: the sidecar holds a *number* while the submit builder copies *fields* and `GetCustomerAddresses` filters `IsDeleted = 0`, so it breaks the order at its **last** step. The console omits the control too, but **the refusal is the guard and the omission is the courtesy** — so the client handles a code its own UI should never provoke. 🚩 The one server obligation is **NEGATIVE**: a same-number `setAddress` must not be short-circuited as a no-op — the only call on this contract that looks idempotent and is not · dep: BackOffice **878** · **built**: `rePinAfterEdit` — the one predicate that turns a book `PUT` into an order act — asked by `updateAddress.onSuccess`, which on a match fires **the ordinary rebind**, so §5.1's preview, the stale-token re-preview and `REBIND_REFUSED` all arrive through paths that already existed. Rule 2 is `api.del` (new on `core/api.ts`; params not a body, because 801's route names its target on the query string) → `deleteCustomerAddress`, the control **absent** on the order's own row and asking a second time **in place** on every other, and `ADDRESS_IN_USE_BY_ORDER` explained even though this UI cannot provoke it. 🚩 **187's own drive assertion flipped here on purpose** — *the edit control is ABSENT on the address the order is using* became *is offered*, and the absent-control assertion moved to DELETE; that flip IS the shape of the split. ⚠ §6.5's named consequence is drawn as **two facts**: `savedNotMoved` renders beside the refusal, because an agent shown only *no store delivers there* would conclude the correction was lost and key it twice — and only beside a refusal, since alone it would announce what the rail already shows. Proof: **8 new pure** (18 green in the file) + `address-editor-drive` **80/80** (was 51/51); `callcenter-drive` at HEAD re-run **460/461**, its one failure the chip-row order belonging to 183's uncommitted note chip
- [189](189-coupon-names-itself-and-comes-off.md) — theCouponNamesItselfAndComesOff · **done** (coupon-view 13 · guidance-view 39 · drive **103/103** against `/callcenter`; typecheck, lint, build green) · blocked by: — · 🚩 **879 landed mid-ticket and the map was re-captured at v1.10**, so what this ticket called a stub is now the wire's own bytes (new `14-coupon-on-and-off.json`) — only the REVERSAL refusal and the second coupon's money are still stubbed, and the drive says so in its own output. The re-capture arrived **red**: five pure tests in the two Proof modules were already failing on inputs that had changed underneath them (capture 01 now carries a real shut `canApplyCoupon` with reason `NO_CUSTOMER`; **both** of capture 03's near-misses now answer `kind: "coupon"`) — the answers changed, the rules did not. Answers [181](181-console-drive-green-on-clean-tree.md)'s finding: a coupon-kind near-miss draws no **card** by design (a card is expandable and carries an Add), it draws a **stated row** — `needsCoupon >= 1` and `couponCards === 0` both asserted. 🚩 The server fixed its half too: `addItem("COUPT173")` is now `ITEM_NOT_SELLABLE`. The *"last chip in the row"* line is **stale** — 183 landed the note beside it under the same rule, so the drive asserts the closing PAIR rather than the position. Review finding applied: `couponRemoveFailure` is a pure, tested rule returning the key **and whether it outranks the server's own message**, because the first cut let the *nothing changed* sentence reach `network`/`unknown` failures where the void may well have landed · `applyCoupon` shipped in v1.0 and the projection deliberately **hides** the `COUP` line it creates — rightly, its money is already flattened onto the product line — but nothing replaced it, so an applied coupon moves the total and **names itself nowhere** (its discount arrives as an `AppliedBonusBuy` with no coupon attribution at all) and the only way to find one was to apply it again and read the refusal. `CouponPicker`/`coupon-view`/the chip exist and `ConsoleShell` accepts the handler; **the page supplies nothing**. Last chip, carrying the **code never the amount**, so the chip row goes on holding no money and the receipt gains no coupon row. Three non-obvious rules: **a shut gate is not a shut chip** (the list projects while applying is refused, and `canRemoveCoupon` is the server's boolean and **never** `coupons.length > 0`); 🚩 **`COUPON_REVERSAL_REFUSED` means NOTHING CHANGED** — the reverse runs before the void, so it is the opposite of what a failed remove normally means, and an agent told *"could not remove"* would reasonably tell the caller the discount had gone (escape: `abandon`); 🚩 **a coupon-gated near-miss is stated, never offered** — capture 02 already offers a coupon SKU as an addable prereq and matching has no line-type filter, so a plain add qualifies the bonus buy **while burning nothing**. A second *different* coupon is accepted (per-code duplicate check). ⚠ Shares `CallCenterConsolePage` with 182 — sequence, don't parallelise · dep: BackOffice **879 (implementing now)**
- [190](190-caller-not-a-member-gets-enrolled.md) — theCallerWhoIsNotAMemberYetGetsEnrolledOnTheCall · **done 2026-07-29 — wired, and the two omissions are structural** · blocked by: — · ✅ **both routes have been on the door since 137**, mounted and gated, so the console cannot find a caller it cannot create; `SignupPanel`/`signup-view` exist and `ConsoleShell` accepts `signup`. **Inline in the caller rail, never a modal** — and the reason is not preference: the wait between *Send code* and the code arriving is **spoken**, the caller is on the line reading digits back, and nothing about a signup is a fact of the *order* — it belongs to the caller, and the caller has a column. Hangs off the not-found lookup on ordinary ground (a miss is not a failure), carries the number already typed, **two fields and no more**, four steps, **no resend and no countdown** (CC2 has neither, and an invented countdown promises an expiry only the loyalty service knows), ending at an **Attach** the agent still has to press. 🚩 Two things this client must deliberately NOT implement: **`BranchId` never leaves the browser** (written to `CreatedByBranchId` forever, validator does not require it, routes are verbatim pass-throughs ⇒ any agent could credit any pharmacy) and **the mobile is not normalised here** (CC2's rule compiled into the WPF client; a second copy over the value the loyalty base keys on is 156's exact failure) — the dialling-code line is a **display preview**, and if the server has not moved yet the console still must not do either locally · dep: BackOffice **879 §4 (implementing now)** · **Built:** `signup-view` gained `codeSent`/`signupCreated` (each legal from one step only) and `signupCapture`/`signupConfirmCapture`; `LoyaltySignupCapture` in core models is what makes `branchId` **unexpressible** rather than merely unsent, and the mobile goes out **as typed** while the dialling-code line stays a display preview. Two routes on `api.ts`, neither a session verb (no `transactionId`, no busy retry, one `requestId` each). Proof: 14 pure + 29 flow assertions against the WIRED console (`/callcenter`, wire stubbed) — the wire rules are asserted on the wire. ⚠ BackOffice 879 §4 still landing server-side; the console is correct either way.
- [191](191-arrow-then-enter-adds-the-highlighted-row.md) — arrowThenEnterAddsTheHighlightedRow · **done** · blocked by: 181 · 🚩 **fixes the map's headline feature being mouse-only** — neither a search row's *Add* nor a guidance card's had **any** keyboard path — and it is the **only slice in the spec with no server dependency at all**, so it is unaffected if 804/879 slip. 153 killed the obvious design first: a Gmail-style *armed when you are not typing* grammar cannot work here because the resting focus is a text box **twice over** (the rail `autoFocus`es the phone field; the search box **re-focuses itself after every landed add**, deliberately) — so the grammar works **from inside the box the agent is already in**. `↓`/`↑` move a highlight; **nothing is highlighted until the first press**, because 131's non-sargable `LIKE '%…%'` makes the top row a relevance **guess** and a one-key add of a guess puts a line on a live order — two keys is the price of every add being aimed. Highlight resets per term, inert while the gate is shut or an add is in flight, and `Enter` calls **the same handler the button calls** — never a second predicate. Built as a module because 192 reuses it · **built**: `highlight.ts` carries the highlight **with the term it was set against**, so a new question drops it by construction, and a shorter answer to the SAME question **clamps** to the last row rather than losing the agent’s aim; it holds no row, no item number and no verb, which is what lets 192 reuse it over the palette. 🚩 Ruled: `↑` from nothing highlights **nothing** — wrapping to the last row would arm `Enter` on the least relevant guess. The gate in `ItemSearchPanel` is ONE expression (`add.onAdd !== null && add.pending === null`) — the same condition the row’s *Add* is drawn and disabled on — and `Enter` calls the same handler with the same two arguments the button passes. A key still finishing a word is ignored (an IME’s own `Enter`/arrows reach the handler as ordinary keys, and half this console searches in Arabic). `GuidanceStrip` deliberately UNCHANGED: 153 gives the card’s *Add* its keyboard path through 192’s palette rows, and a second highlight armed from the same box would be two lists competing for one `Enter`. The aim is **announced, not only painted** — 153’s build note binds it to `aria-activedescendant` over the rows (combobox/listbox/option) and never to a focus move, since the caret must stay in the box. Proof: 13 pure + drive boxes 41–43 (the negative taken first, with the gate wide open; the ARIA aim asserted both ways — it names the row, and names nothing while nothing is highlighted)
- [192](192-ctrl-k-reaches-every-order-act.md) — ctrlKReachesEveryOrderActWithoutEndingTheCall · **done** · blocked by: 182, 183, 187, 189, 191 · **lands last** — a palette row cannot exist for a verb that is not wired, and its five blockers are the five slices that supply its rows. `Ctrl+K` opens from anywhere **including inside any text box** (must `preventDefault()`; it is Chrome's omnibox key and IS interceptable) and is **inert while any `<dialog>` is open** — a palette over a confirmation sheet is two truths on one screen. Built on `core/ui/Modal`'s native `<dialog>`, so trap and restore are free. Rows: **actionable offers first**, read from the strip's own view model so the palette and the top-bar count **cannot disagree** (`Alt+1..3` rejected — cards re-order, so a number is a position not an offer), then the order verbs, then the two terminals. 🚩 **Nothing on the keyboard can end a call**: submit/abandon are palette rows only, sorted **last** and **never auto-highlighted**, so a query matching only `Abandon call` highlights nothing and the agent must press `↓`. 🚩 **A refused verb is a disabled row carrying its reason** — the console's **one deliberate exception** to *a control the door would refuse is worse than no control*, because the law is about a control the hand lands on and the palette is a question the agent **asked**; disabled rows stay highlightable and `Enter` does nothing, since skipping them would hide the very reason the exception exists. The palette is the whole cheat sheet — no `?` (it types into the box the agent is in), no `F1`. Line verbs stay out: the palette is one level deep and its object is the **order**, which keeps *void* aimed at a line the agent is looking at. No server dep · ⚠ name the drive `command-palette-drive.mjs` — `tools/palette-drive.mjs` is taken by the POS colour palette · **built**: `palette-model.ts` takes the strip's `GuidanceView` (read ONCE in `ConsoleShell`, beside the top-bar count, so drift is impossible by construction) and a record of **handlers** — never `capabilities` booleans: a row is enabled exactly when the page handed it the same handler the chip, the rail or the receipt already reads, so an act withdrawn from one surface is withdrawn from both, and `ConsoleShell` needed **no new prop**. 🚩 An **offer row hands off to the item search** (172's `onSearchRest`) rather than adding — a near-miss names no material, so an add row would need `ResolvePrereq` run for every offer the instant the palette opens; the strip resolves one card on demand and the palette gives it the keyboard path. 🚩 The aim is carried against the query **AND the row ids** (`paletteQuestion`): the rows are rebuilt from the live `SessionState`, so an add landing under an open palette would otherwise slide a carried index — which `highlight.ts` clamps to the LAST row, *Abandon call*. 🚩 `Ctrl+K` is `preventDefault()`ed **before** the inert check — *inert* must mean this console does nothing, not that Chrome takes the omnibox. Refusals are worded from the chip row's OWN `fulfilment.locked.*` / `payment.locked.*` families and a dead submit borrows the receipt's `submitBlockers`; only §6.3's attach-before-address ordering is quoted outright, guarded on `hasCaller` so it can never be a wrong refusal. ⚠ **Done-when vs 153, resolved to 153**: submit has no modal anywhere on this console, so *sorted last* + *never auto-aimed* are the guards (both asserted) and a **deliberate** ↓ then Enter places the order exactly as the receipt's button does — asserted as ONE `Submit` with one `requestId`; gating submit behind a confirmation is a new ticket about the receipt. Proof: 29 pure + `command-palette-drive.mjs` 44/44

## The call center goes to the web — the caller's open request (spec 193)

Phase 3 of the client track of map 126, from owner grilling 2026-07-29/30. A pharmacist's **sales
request** (category `'Q'` / SREQ) becomes a web-placed order: WPF has done this since
`NewOrderController.RequestLookup`, and on the web the `RefDocumentNo` / `DocumentReason` fields exist
all the way down to `AddSdDocumentHeader` with **nothing filling them** — so a web order can never
convert a request and the TMRA picking fee the OMS charges off the reason code never gets charged.
Server half is BackOffice **880** (contract v1.11).

- [193](193-linked-sales-request-spec.md) — The caller's open request becomes this order · **ready** · spec — consumable by /to-tickets · 🚩 The owner's opening proposal — **lock the customer for the life of the order**, change means abandon — is deliberately **not** in the spec: `removeCustomer` clearing the link closes the same vector (link caller A's request, swap to B, submit, and the 055b spine completes A's request against B's order) while 871 §6's ruling stands that "the ordinary *wrong caller, same items* correction must not cost the basket". **Linkable only on an empty basket** is the restriction the rest of the design hangs off — it kills the plant re-price, the copied-meets-typed merge and every quantity ambiguity at once — and it is what forces **unlink to be a full undo** rather than WPF's stamp-drop, since a basket left un-emptied could never be re-linked. Store + items copy for every reason; **TMRA additionally forces pickup and paid-online**, because the caller is standing in the pharmacy and Tamara settles online. 🚩 The picker deliberately does **not** reuse `DocumentDetailsPage` (no view-only mode — WPF had `ViewOnly = true`; it would put change-store/close-request under an agent's hand mid-call — and `callcenter → oms` is a boundary violation): the console needs the lines anyway to copy them, so the picker shows what it is about to copy. Scoped by **loyalty id only** — one household number is one household, and phone scoping surfaces a relative's request · dep: BackOffice **880**
- [194](194-the-callers-open-request-becomes-the-order.md) — theCallersOpenRequestBecomesThisOrder · **done 2026-07-30** · blocked by: — · ⚠ **wholly undrawn** — no picker, no chip, no model, and the server routes are being built alongside, so this is a build against a contract rather than a wiring. **The count is drawn, the modal is not**: attaching a caller with open requests puts a count in the rail (an unnoticed request is the failure the spec exists to prevent) but nothing opens by itself, because the agent is mid-greeting and a picker over the basket takes the call away from them. **One press, one act** — `linkRequest` carries one `requestId` and the console must **not** loop `addItem`, which would be N ids for one agent action. 🚩 The interesting half is the **skipped-line report**: lines the server refused or found below ATP were **not** added, and a below-ATP line may never arrive already-added because `HasBelowAtp` is a fraud signal and a flag nobody saw proves nothing. The linked card holds **no money** (the request is unpriced) · dep: BackOffice **880** · **built 2026-07-30** — 32 pure + drive **61/61**, 783 suite-wide, lint and build green. 🚩 **The offer reads `canLinkRequest` and inspects the basket ONLY where that capability is absent** (a pre-v1.11 server), so *linkable ⇔ empty basket* has one home — the server's — and the count is also silent once `header.linkedRequest` is set, because the card replaces it. The reason renders as its description or as **nothing**: there is no `?? reason` fallback anywhere and `TMRA` is asserted absent from the picker's whole text. 🚩 Review moved real code twice: the skipped row was classified on the figures' mere presence AND had its code dropped, so a refusal that happened to ship stock numbers would have been drawn as an *add anyway* with the server's own reason erased — it is now classified on a real **shortfall** (`requested > available`, `belowAtpAsk`'s own rule, since the handle leads to that very sheet) and the code rides on **every** row; and a landed add now **prunes its row** (`reportWithout`, on ANY add of that item, the last row taking the banner with it), because *this did not land* over a basket that now holds the line is the console arguing with the receipt. The report is the console's THIRD in-the-flow banner rather than picker furniture — *add anyway* opens the below-availability `<dialog>` and two open dialogs are two truths on one screen; `ConsoleBanner` lost its tone-derived dismiss selector in the process (three banners, two tones). Open question settled **under the caller card**: a request belongs to the CALLER, arrives with them and leaves with them. The picker's shut-gate wording is a **guard, not the normal path** (the count never offers a picker onto a shut gate), so the two unreachable reasons were cut from the i18n rather than left as plausible dead copy. **No re-read on the picker's open** — 195 forbids pre-checking the request, and the guard is the submit refusal. `CONTEXT.md` gained **Sales request** (and why it is not a *request close*), **Linked request**, **Skipped line**. Unlink and `REQUEST_ALREADY_CONVERTED` are 195's · ⚠ dep BackOffice **880 UNBUILT** — both routes and all of v1.11 stubbed to §3/§4's documented shape, said in the drive's own header; against today's server the read 404s and the rail is silent, which is the same silence a caller with no requests gets
- [195](195-unlinking-and-the-request-that-went-away.md) — unlinkingIsAFullUndoAndTheRequestThatWentAwayIsNamed · **done 2026-07-30** · blocked by: 194 · split out because these two rules are sharp and carry the real risk. **Unlink removes the copied lines and re-shuts the store gate**, so it asks first and its confirm says *the caller stays attached* — the two acts sit next to each other in the rail. `removeCustomer` clearing the link is a **server** change with a **client obligation to stay quiet**, so the client's correctness is an absence and the drive proves it by reading the whole screen for the request number rather than checking one element. 🚩 `REQUEST_ALREADY_CONVERTED` is reachable in normal operation — another agent or the till can convert the request mid-call, and 055c then refuses the order; without the mapping it arrives as the transient `SUBMIT_UNAVAILABLE` and the agent retries forever on an order that will never post. ⚠ **No pre-submit re-check**: it would narrow the window and invite a reader to believe it was closed · dep: BackOffice **880** · **built 2026-07-30** — 9 new pure cases (41 in `linked-request.test.ts`, 792 suite-wide) + drive **92/92**; typecheck, lint and build green. 🚩 **BackOffice 880 was BUILT mid-ticket** and capture 15 (`136-cc-contract/15-linked-sales-request.json`, v1.11) landed with the whole scenario on the wire — which caught 194 calling the wrong door (`CustomerRequests` needs `?transactionId=`, the session row being what holds the loyalty id the scope comes from; fixed here, and still no customer id on the wire) and pinned the unlink's own bytes: `linkedRequest: null`, `lines: []`, `plantSource: seededAtOpen`, `canAddItem` **false** with `STORE_NOT_CHOSEN` back, `canLinkRequest` **true** again, caller untouched. One surprise from the capture: the prefilled `sourceReference` **survives** both the unlink and the caller's removal — it is the ORDER's field from §4.4 onward, so the drive's whole-screen sweep exempts the chip row rather than the console scrubbing the header behind the server's back. `unlinkCost(state)` takes the state and NOTHING else, re-derived each render, so a link that landed four of six offers to remove four and a link that vanishes under the open sheet closes it; 🚩 review moved real code once — the confirm read its store off the REQUEST's stamp, which 880 (*what shipped* §3, a request naming no store copies no store) would have drawn as a blank claiming a gate re-shut that never opened, and which spec 193's *the address still wins the plant* would have made name a branch the order had left; it now reads `header.plant` + `plantSource`, silent where there was no chosen store to lose; `keepsCustomer: true` is a FIELD, like `SubmitFailure.orderOpen`, because the sheet must not be trusted to remember the one fact that makes the control pressable. `REQUEST_ALREADY_CONVERTED` needed no new failure kind — `readSubmitFailure` already answers it `retryable: false`, asserted from both directions since the two SWAPPING is the defect — so 195 adds only the naming and a closed set of one escape, silent where the order holds no link, and it **drops** the server's sentence (which names the same request) on 189's `COUPON_REVERSAL_REFUSED` precedent. The sheet is `ConfirmSheet` + an `UnlinkConfirm` body (a third body, not a third mechanism), no `reissue` because there is no token. ⚠ Named not done: the palette (192) still lists neither *view requests* nor *unlink* — `VERB_ORDER` predates the request surface, and both belong to whoever revisits it

## UA users at scale (map 139)

Ayed's three asks for `admin/ua-users`, which all press on the same seam — the `UaAdminWeb/*` read
contract. A seventh report card counting people who **completed activation**; real paging past the
50-row wall (the server already takes `skip`/`take`, the client hardcodes `skip: 0`); and a CSV the
Excel double-click opens intact. Destination is a `ready` spec for the client plus a written server
addendum for SIS.Api — the C# lands elsewhere.

- [139](139-ua-users-scale-and-export.md) — MAP: UA users at scale: the activation counter, paging, and export · **done 2026-07-27** · all 7 tickets resolved, no fog left — ready for `/to-spec`. One server change in the whole effort (141's `completedActivation` card field); audit-tab paging ruled out of scope
- [140](140-uaadminweb-contract-as-built.md) — What the UaAdminWeb contract already gives us · **done 2026-07-27** · map 139 · blocked by: — · research · 🎯 **paging needs zero server change** — both endpoints already bind `skip`, `ClampToCap` caps only `take`, every path orders deterministically before paging, `isCapped` already means "beyond *this* page"; the whole change is the hardcoded `skip: 0` at `api.ts:17`. **Nothing today means "finished activating"** (`awaitingActivation` = `Seeded ∧ ¬phoneGap ∧ no credential row`); a 7th card is ~4 lines in 3 files and its count agrees with its worklist **by construction**. 🚩 the cards **already don't sum** — three universes, and `mustChange` alone skips the legacy-`[User_]` join and shared-account exclusion. Estate **~6,000 identities**, ~6 DB queries per page ⇒ client full export of *All people* ≈ **120 round trips / 720 queries** (narrowed cards trivial: `phoneGap` ≈ 8) — the fork 144 must rule on is `all` alone; `MaxSearchRows = 50` is a **product clamp, not a technical limit**. Every route grant-gated fail-closed, **no read audited anywhere**, audit rows are single-target only · [research asset](assets/140-uaadminweb-contract-as-built.RESEARCH.md)
- [141](141-completed-activation-predicate.md) — What counts as a completed activation · **done** · map 139 · blocked by: 140 · grilling · **HITL** · legacy-backed ∧ ¬shared ∧ credentialState=='active'; no active clause, no phone clause; server field only
- [142](142-seventh-card-label-and-placement.md) — Naming and placing the seventh card · **done 2026-07-27** · map 139 · blocked by: 141 · grilling · **AFK (recommendations taken)** · "Activation done" (keeps the noun, never spends "active"); Disabled unchanged; position 5 beside Awaiting activation; untoned; row becomes `auto-fit minmax(8rem,1fr)` so 6 and 7 cards both look right
- [143](143-pager-shape.md) — The pager: page size, controls, and what a page change disturbs · **done** · map 139 · blocked by: 140 · grilling · **HITL** · fixed 50, prev/next + "Page N of M" in a footer; selection survives paging; cap note retires
- [144](144-export-scope-and-cost.md) — How much the export exports, and what that costs · **done** · map 139 · blocked by: 143 · grilling · **HITL** · everything exportable, confirm above 500 rows; walk at 50 (no server change); cancel or error ⇒ no file
- [145](145-csv-shape-and-excel-fidelity.md) — What is in the file, and whether Excel opens it right · **done 2026-07-27** · map 139 · blocked by: 144 · grilling · **AFK (recommendations taken)** · 13 columns = the row unpacked + Status; labels not codes (so the file is localised); `="…"` wrapper on employeeId/phone, `'` guard on free text, BOM + `sep=,` + CRLF; `null` last-login is an empty cell; `ua-users-{scope}-{date}.csv`; pure `csv.ts`; no server change
- [146](146-export-gate-and-audit.md) — Should bulk export be gated, and should it leave a trace · **done 2026-07-27** · map 139 · blocked by: — · grilling · **AFK (recommendation taken)** · **no separate grant, no audit row** — 144 made export = the pager in a loop, so the server can't tell them apart: a second grant has no endpoint to enforce at (client-only ⇒ hide-the-button) and inverts least privilege (the same grant already carries SetPassword/Deactivate/RevokeSession); the separation already exists as the standalone `UA_USERS_ADMIN` role. Button carries **no permission check of its own**. Addendum gains a 2nd deferred note: if ever required, a *server* export endpoint that streams+audits+carries its own grant — never a client `canExport`. 🚩 144's >500 confirm is a wait-time device, not a control
- [147](147-ua-users-scale-spec.md) — UA users at scale: the activation counter, paging, and export (spec) · **ready** · spec — consumable by /to-tickets · dep (7th card only): BackOffice 805 `completedActivation` count + worklist case; pager and export need no server change

## UA users at scale — build (spec 147)

Tracer tickets sliced from spec 147. Two independent chains: **paging + export** (148→{149, 150→151})
and **the seventh card** (152, parallel). Only 152 needs a server change — BackOffice 805
(`completedActivation` count + worklist case); the pager and export ship against `UaAdminWeb/*`
as-built.

- [148](148-ua-users-pager.md) — theGridWalksPagesInsteadOfStoppingAt50 · **done 2026-07-27** · blocked by: — · Slice 0 · fixed 50-row page, prev/next + "Page N of M" footer shown only past 50 rows, Next off `isCapped`, page becomes a field of the query (so a new search/card resets to page 1 by construction), `keepPreviousData` + dim-while-fetching. Retires the cap note (`grid.capped` **deleted**, `search.hint`/`grid.emptyHint` reworded) and fixes the latent bug: the header count must read `totalMatches`, not `rows.length`. No size chooser (server clamps `take` down), no numbered pages (120 of them). Creates `tools/ua-users-scale-drive.mjs` (28/28). Built: pure `pager.ts` + `GridPager.tsx`; needed **`staleTime: 30_000`** on top of `keepPreviousData` — the global default is `0`, so a step back to a visited page would refetch and dim, which is the stutter the ticket set out to kill. `CONTEXT.md` gained a **Page** entry recording that `isCapped` now reads "another page exists", not "truncated"
- [149](149-ua-users-page-holds-through-mutations.md) — actingOnAPersonHoldsThePageAndClampsAnEmptyLastPage · **done 2026-07-27** · blocked by: 148 · a reset/disable/clear-TOTP refetches without leaving the page; if that empties a page above 1, clamp to `ceil(totalMatches/50)` so success never looks like a broken screen. Selection surviving paging is **asserted, not built** (the pane fetches by id) — recorded so nobody "fixes" it by clearing selection on page change. Built as `clampToLastPageWhenCurrentPageEmpties` in `pager.ts` + one effect on the **list result** rather than in an action handler, which is what makes it cover **every** caller of `refreshLists` (pane actions, set-password, new identity) — gated on a *settled* read, because `keepPreviousData` means mid-flight `data` is the previous page's rows. "The page holds" needed no code (the page is a field of the query; a mutation only invalidates) and is asserted so nobody breaks it. Drive now 41/41, its stub gaining live worklist membership + counts/mutation ledgers. Review caught one real hole, fixed here: an **uncached** landing page arrives behind the emptied one as `keepPreviousData`'s placeholder, so "No people match" would render for that round trip — the very screen the clamp exists to prevent; a placeholder with no rows now reads as a first load. `CONTEXT.md` gained **Worklist** (a *card* is the count you click; a *worklist* is what you work down, and it has live membership)
- [150](150-ua-users-csv-export.md) — exportingANarrowedCardDownloadsACsvExcelOpensCleanly · **done 2026-07-27** · blocked by: 148 · the routine export path: button + walk over the current query's **full match set** (not the visible page) + pure CSV writer. 13 columns = the wire row unpacked + derived Status, channel pill split back into codes-via + reachable; every classification is **the screen's `t()` label, not the code** (so the file reconciles by construction — accepted price: the export is localised). Excel guards: `="…"` on employeeId/phone (quoting alone loses a leading zero), `'` on formula-leading free text, BOM + `sep=,` + CRLF. `null` last-login is an **empty cell, not "Never"** (a word kills date sorting). Filename `ua-users-{scope}-{YYYY-MM-DD}.csv` off the card **code**. 🚩 **No permission check of its own** — renders behind the existing access probe; do NOT add `canExport`. Built: pure `csv.ts` (writer + filename, takes a **label resolver** so its suite drives it with the real locale file — a missing key fails as a raw key in a cell) + `export.ts` (walk + download). Column 4 `phoneClass` is the one fact with **no on-screen word**, so *Mobile status* + `csv.phoneClass.*` is the file's only new vocabulary; everything else resolves through the pills' own keys. The runaway bound **throws** rather than returning a short list — returning would be the silent early stop, and it would write a file. Drive now 50/50 (its `phoneGap` pool became 400 rows = 8 pages, so 402 lines out proves whole-match-set)
- [151](151-ua-users-export-at-scale.md) — exportingEverybodyWarnsFirstAndCancelsWithNoPartialFile · **done 2026-07-27** · blocked by: 150 · everything exportable incl. `all`, but >500 rows raises a confirm naming rows + wait (~45 s for ~6,000); `phoneGap` ~400 lands just under deliberately. Cancellable sonner progress (one toast id re-fired per page, cancel flips a **ref** so the running walk sees it without a render), walk never touches the mounted query's cache. 🚩 **The governing rule both ways: cancel or any `ApiError` ⇒ no file at all** — the walk **throws** rather than returning what it had, which is that rule expressed where it can be tested. Built: `needsConfirm` + `estimateWalkSeconds` as pure functions (the threshold is one testable fact, not a `>` on a screen) and two named errors, `ExportCancelledError` + `ExportRunawayError` — the runaway needs its own because `apiErrorMessage` would render it as a generic "unexpected", and "the walk never ended" is different news from "the server refused". 🚩 Review caught a real hole: the confirm read `list.data?.totalMatches ?? 0`, so an export fired before the first read settled saw **0 matches**, skipped the dialog and walked 6,000 people with no progress and no Cancel — the button is now dead until a count lands. `exporting` is taken **before** the confirm too (the dialog is awaited; a live button could start a second walk). Drive now 70/70, its new sections delaying the stub's `all` pages so a mid-walk cancel is reachable, and letting one run finish to prove 6,000 identities land exactly once. ⚠ the >500 confirm is a wait-time device, **not** a control — never write it up as one
- [152](152-ua-users-activation-done-card.md) — theCardRowRendersRightAtSixAndSevenCards · **done 2026-07-27** · blocked by: — · "Activation done" at position 5 beside Awaiting activation, **untoned** (colour on this row means *there is work here*; this is the one card whose rows need nothing done). Disabled neither moves nor is renamed. Card list moved out of the page into pure `cards.ts` — **an absent field ⇒ the card is not rendered, never `0`** — with a `conditional` flag so "which card may be absent" is stated once, and `ROW as const` giving a `CardCode` union. 🚩 **`auto-fit` was the obvious reach and is wrong**: it derives its column count from the width, so at ~1000px seven cards become six plus a full-width orphan — the row-with-a-hole the ticket exists to avoid. Track is now **one column per card** (`repeat(var(--card-count),minmax(0,1fr))`), and the drive measures one row of equal widths at 1600px *and* 1024px, in both arrangements. The drive runs its whole length on the SIX-card envelope and flips the field on at the end with a reload — which is how the card reaches production when 805 deploys, with no client release. Drive 85/85. **dep: BackOffice 805**. 🚩 Do NOT add a cards-sum check — they already don't sum (three universes), and this card overlaps Disabled on purpose (a leaver who activated still counts)

## Nphies to web — map 196

Charting the port of the WPF Nphies module (`C:\Work\DMSCO\BackOffice\Sartawi.POS\Nphies\`) to
oms-react. Destination: **a sized estimate + a `/to-spec`-ready spec**. **MAP CLOSED 2026-08-01 —
both halves exist: the estimate is 204 (35–46 developer-days, two teams) and the spec is
[209](209-nphies-web-spec.md), `status: ready`. Twelve tickets, no fog left. Next step is
`/to-tickets` against 209.** The Nphies service's **own source** is on disk at
`C:\Work\DMSCO\nphies\Service\NphiesService` — read it before charting anything further.
Four premise-changing finds: the Nphies
integration already lives server-side, **the Nphies service's own source is on disk** at
`C:\Work\DMSCO\nphies\Service\`, **v1 is one claim type with no mode selectors and no hand-typed
identity** (199) — deliberately the smallest thing the team can react to — and, latest and largest,
**an authorization is an engine document on the Till Submission Platform, not a JSON post** (208),
so the web screen is a live engine session and the map's cheapest term grows a session surface.

- [196](196-nphies-to-web-map.md) — Nphies to web · **done 2026-08-01** · map · **destination reached: estimate [204](204-nphies-the-estimate.md) + spec [209](209-nphies-web-spec.md)** · Destination: sized estimate backed by a ready spec. Key fact from charting: `NphiesService.cs` is pure `HttpClient` against a standalone REST service at `172.23.27.40:8065` — **this is a UI port, not an integration build**. Counterweight: `BuildAuthRequestForSubmit()` builds the auth line items out of the **live POS basket** (engine-computed price, VAT, discount and the three deductible buckets), so the cost is reproducing what the till gave WPF for free. Dispensing/invoicing/GS1/robot ruled out of scope — the pharmacist keeps the till
- [197](197-nphies-pricing-machinery.md) — What pricing and deductible machinery already exists server-side · **done** · map 196 · blocked by: — · research · **AFK** · the money is the **SIS.Pricing engine's, not the till's**, and its insurance pass is already reachable over HTTP both ways — 15 line fields split **5 free / 10 need a server change / 0 unsourced**, and nine of the ten collapse into two changes; `NphiesDeductibleManager`'s second overload is already pure so lifting it server-side is a move not a rewrite; the call-centre price-check path is *not* reusable (open transaction, qty-1, insurance explicitly off); whether the Nphies service checks the money is unknowable from source → 206 · [findings](assets/196-nphies/pricing-machinery-research.md)
- [198](198-nphies-proxy-contract.md) — The shape of the SIS.Api passthrough · **done** · map 196 · blocked by: — · grilling · **HITL** · **the passthrough is half-built already** (SIS.Api ships `Nphies/CheckEligibility` + a registered `NphiesHttpService`; the Nphies service's own source is on disk at `C:\Work\DMSCO\nphies\Service\`) · **split by kind** — acts+lookups pass through, the 2 lists re-modelled in SIS.Api (forced: `Take(20000)` with `OrderByDescending` commented out) · **14 endpoints**; dispense/payment/claim/HIDP/multipart-upload never ported; attachments are base64-in-JSON so **the whole surface is JSON in/out** · env **already server config** (staging `:8077` planned; `Nphies:BaseUrl` missing from source appsettings) · error taxonomy is **three-way not two** (a payer rejection is `Success=true` *data*) and SIS.Api owns all translation · explicit 100 s sync, **timeout means in-flight not failed** (`SaveNAuth` runs after the round-trip, no `CancellationToken`) · port is **async all the way down** · **5–7 developer-days, SIS.Api team**
- [199](199-nphies-scope-of-acts.md) — Which acts and which claim types the web actually raises · **done 2026-07-31** · map 196 · blocked by: — · grilling · **HITL** · v1 = **claim type 0 only, request type `WithReferenceToEligibility` as a constant, 9 acts** — so **both mode dropdowns leave the screen** and **no identity is ever hand-typed**. Eligibility: search/display/new with a patient-id **Fill** (`LastEligibility/{patientId}`) replacing new-with-reference. Auth: search, display, status check, retry, cancel, clinical-edit validate (a **submit**-time gate, not dispense-time). Later: types 2/4/6, follow-up, communication, update-from-eligibility, update-advance. Never: claims (1), direct dispense (3). Bahrain + Vitality out of scope; **exception prescription in** (= one item group per request). A `NeedComm` auth **stalls on the web** by explicit ruling. Adds `Auth/Retry` → 198 becomes **15** endpoints
- [200](200-nphies-identity-and-context.md) — Provider, staff and store · **done** · map 196 · blocked by: — · grilling · **HITL** · the acting store plays NO part — `ProviderCode` is a free per-act pick from `core/providers` (no default, no memory, inherited read-only onto the auth, a filter defaulting to *all* on the lists); channel is the constant `"20"` because `"21"` is Bahrain; `UserId`==`StaffId`==session user id, server-stamped; `SourceCode` = `'WEB'`; **one grant** for the whole screen, and the existing ungated `Nphies/CheckEligibility` gets it too
- [201](201-nphies-rejection-detail.md) — What a status is, and what the rejection detail shows · **done** · map 196 · blocked by: — · grilling · **HITL** · **two columns** — Request (did we get an answer) + Verdict (what they said, blank until Complete) — same pair on both lists, with `NeedComm`/`IsDispensed` as markers; **no "ready to dispense" flag** (the service's `Dispense()` owns it and its `HasFollowUp` clause isn't on the list DTO); **no rejection view** — `BenefitReason` is already decoded display text inside the already-proxied `auth/AuthResponse/{id}`, so the ordinary detail always shows it. Estimate moves down
- [202](202-nphies-attachments-in-a-browser.md) — Attachments in a browser · **done** · map 196 · blocked by: 199 · prototype · **HITL** · file picker only, no type dropdown, canvas-downscale to JPEG, fixed 7-value title select; no cap exists anywhere (column is NVARCHAR(MAX)); ~1 day, zero server work
- [203](203-nphies-screen-shape.md) — The shape of the screens: four modals become what? · **done 2026-07-31** · map 196 · blocked by: 197, 199 · prototype · **HITL** · **six routes in one new area `features/nphies/`** — two lists, each with a detail + a form route. Seam = **route transition carrying an id** (`/nphies/authorizations/new?from=<eligId>`), because the auth is often raised days after the check: WPF's *carried object* becomes a fetch by id. Items land on an **inline add-row** that prices in place; **no modal opens anywhere in the flow**. The eight visibility booleans need **no replacement** — 199/200 dissolved them and `ViewOnly` was just one dialog doing duty as both form and detail; one checkbox + one conditional block survive. Principal diagnosis = **a radio in the row** (uniqueness structural), **morphology appears with its cause**. 🚩 Two corrections of record: **`Failed` = NPHIES refused on validation before the payer saw it** ⇒ a *form* state fixed in place, and **Retry belongs to `Pending`, not `Failed`** (`RetryAuth` re-POSTs the stored JSON verbatim). Acts state-driven, reason on hover. Lists default to **last 7 days as a removable chip** — what actually tames `Take(20000)`. **No browser polling**: the service's own `PollRequestWorker` polls NPHIES every 15 s, so Pending resolves itself; manual refresh only. Surfaced 207 · [prototype](assets/196-nphies/screen-shape-prototype.html)
- [204](204-nphies-the-estimate.md) — The estimate · **done 2026-08-01** · map 196 · blocked by: — · grilling · **HITL** · **35–46 developer-days engineering-complete, two teams** — ~15–20 server (SIS.Api) + ~19–25 web (oms-react); one dev end to end 7–9 weeks, two in parallel **≈ 5 weeks elapsed**. **Zero Nphies-service days** — not one ticket found work for the service team, and the money term the map opened calling "a multiple" closed as **three session verbs and one column**, both its candidate server changes priced at zero. Riskiest by far is 208's **engine-session surface at 8–12 d (could be 6, could be 18)** — a *parallel* build, since the call-centre services are doc-type-aware but call-centre-**bound**; a ~2-day de-risking spike was priced and declined. Staging `:8077` is a **schedule dependency, not an effort figure**. QA/UAT/deploy deliberately excluded (a 30–50% tail, quote it as a tail not a discount); View JSON, camera capture and before/after audit on the two line-meta codes priced and left out. Requester's four rulings: per-team + total, engineering-complete only, final-value-only audit accepted, session term carried as a range. **Spec not written here** — `/to-spec` against 196 is the map's closing act and its own session
- [205](205-nphies-who-computes-the-money.md) — Who computes the money on a web-raised authorization line · **done 2026-08-01** · map 196 · blocked by: — · grilling · **HITL** · graduated from fog by 197, shrunk by 208 · **nobody computes money on the web — the engine computes all of it and the agent supplies five inputs**: header rates, header paid-outside, line qty, line Max Coverage, line Days Supply (+ Selection Reason, a code not an amount). **Q3 dissolves at zero cost** — `InsuranceItemCategory` rides the engine line and `ResolveDeductibleGroupForLine` already maps category→G1/G2/G3 server-side, so 197's picker change is **priced at zero and dropped**. 🚩 **`DeductibleGroupName` IS `InsuranceItemCategory`** (same value, two names) — Q3 and a fifth of Q4 were one question. **`Factor` is omitted** — the service overwrites it (`Amount/ExtendedPrice`). **`MaxCoverage`/`DeductibleG`/`DeductibleGroupName` never reach NPHIES** — stored on `NAuthLine`, read back by the *dispensing till*; only `ActualPatientShare` is adjudicated. 208's override list was short by three: Max Coverage (editable cell, via `MaxPayerShare`), Selection Reason (WPF rule exactly — disabled on `Generic` only), Days Supply (both levels, **one 1–100 rule** replacing WPF's 180/90/100, deleting the submit-time sweep). Paid-outside **included** and **persisted** — new `PaidAmount` column on `PosTransactionDeductibleGroup`, because a stored cap can't distinguish 300 from 500-less-200. **Cost: 3 session verbs (8→11) + 1 column**; the auth header was already modelled on the transaction (migration 015). Audit is complete for money, **final-value-only** for `DaysSupply`/`SelectionReason`. 🚩 3 defects reported not fixed: div-by-zero at `AuthService.cs:450`, stray-semicolon parse in both `NphiesDeductibleManager` copies, SIS.Pos silently ignoring `MaxPayerShare <= 0`
- [206](206-nphies-does-the-service-check-the-money.md) — Does the Nphies service check the money it is sent · **done 2026-08-01** · map 196 · blocked by: — · task · **AFK in the end** · **no — not one guard touches an amount**, and the probe was never needed: resolved from the service's own source at `C:\Work\DMSCO\nphies\Service\NphiesService`. `Auth/Auth` is a **transcription layer** — copies every money field onto `NAuthLine` (`AuthService.cs:423-448`), rounds seven to 2dp away-from-zero, recomputes exactly one (`Factor = Amount/ExtendedPrice`, `:450`), overrides `SelectionReason` from the item master, POSTs. **What the caller sends is what the national exchange sees.** A wrong `ExtendedPrice` is accepted **silently** (never leaves the service; only skews the sent `Factor` ⇒ a wrong factor on a live claim, not a rejection); a header/line deductible mismatch is **unobservable** — `DeductibleG*`/`MaxCoverage` have no NPHIES mapping and the outbound `Claim` carries **no `Total`**. 🚩 **The case that could have cut the estimate does the opposite**: zeroed money **throws div-by-zero** at `:450`, caught blanket at `:743` ⇒ `Success=false` + an orphan `NAuth` header with no lines — so real money is required from day one and **205's engine-computes-everything becomes load-bearing**, no downstream second opinion existing. Adds **one required guard** (SIS.Api refuses `ExtendedPrice = 0`) and **one rounding rule** (`ActualPatientShare` is the only *adjudicated* field and the service does **not** round it — web rounds 2dp, parity with the till). NPHIES-side validation stays unprobed (staging `:8077` unreachable, prod `:8065` off-limits) but is **no longer a risk** — the production formula is empirically valid, so the web reproduces it rather than discovering it; a violation is 203's `Failed`. Incidental for 207: **`GET auth/AuthJson/{id}` exists** (`AuthController.cs:590`) · [findings](assets/196-nphies/service-money-checks.md)
- [208](208-nphies-the-auth-is-an-engine-document.md) — The authorization is an engine document, not a form post · **done 2026-07-31** · map 196 · blocked by: — · grilling · **HITL** · 🚩 **premise-changing** · **yes — the web drives a live `NphiesAuth` engine transaction like the till; the screen is a SESSION, not a form** (open on entry → `ScanAsync`/`ChangeQty`/`VoidLine` → `StartAsync`→POST→`MarkSubmittedAsync(authId)`; `Abandon` on leaving). Reason is the owner's own from spec 301: audit **what the engine landed vs what the agent changed** — "added then voided" can't be rebuilt from a payload of survivors. Shift-less per ADR-0001/0005 (a browser is the call-centre-device case, path proven). **A parallel Nphies session, ~8 verbs vs call centre's 25** — `CallCenter*` is doc-type-aware but call-centre-**bound** (guard at `CallCenterEngineSession.cs:103`; id hard-coded `:336/:373/:413/:500`), so the recipe is reused, not the code. Agent overrides **header deductible rates + line qty + void**, never an item swap / price / discount ⇒ **203's rate block becomes EDITABLE**. **Duplicate item refused at the SCAN** (WPF's submit-time `ValidateDuplicateItems` moved forward). **No resumable drafts** — abandon + sweeper, because 249's OPEN-claim litter is what spec 301 retired. 🚩 **Corrects 200: the acting store IS the pricing plant**, bound once at open (`CallCenterEngineSession.cs:491-493`) — invisible in the payload, decisive in the money; no store↔provider mapping exists. Shrinks **205** to its picker/fields half and **unblocks** it; grows **198** by the session surface; sharpens **207**
- [209](209-nphies-web-spec.md) — **Nphies on the web: eligibility checks and prior authorizations** · **ready** · spec · from map 196 (all 12 tickets) · **the map's destination, reached** — consumable by `/to-tickets`. Two screens in a new `features/nphies/` area, six routes, two i18n namespaces; the auth form is a **live engine session, not a form post** (11 verbs), the acting store is the pricing plant, nobody computes money in the browser. Four `/to-spec` rulings: **pure vitest + Playwright drives** (RTL still not bootstrapped, matching the call-centre console); the call-centre **session guard graduates to `core/`** and goes first, alone, since features may not import features; the **SIS.Api work is spec'd as a contract but not ticketed here** (~15–20 of the 35–46 days is another team's); **one spec**, not two. 90 user stories. Suggested slice order: core guard → eligibility list as the area tracer → check + detail → auth list + detail (no session needed) → the auth form last, since it alone blocks on the server's session verbs
- [207](207-nphies-reopening-a-refused-request.md) — Reopening a refused request: what can the form be prefilled from? · **done 2026-08-01** · map 196 · blocked by: — · research · **AFK** · surfaced by 203 · **path 3 — SIS.Api's own store — and it is already paid for.** 208 put the web on spec 301's orchestration, whose **write-ahead journal row already carries the whole request**: `IntegrationAttemptLog.StartAsync` inserts `RequestJson = SerializeObject(request)` into `PosIntegrationAttempt` on a fresh connection **before** the payer is called, `StringMaxType` (202's attachments ride inside), findable by `SubmissionReference == authId` which 301 stamps on **any** response carrying an id, refusals included. **No new table, column or write.** **Path 1 is richer than suspected and is the free fallback** — `auth/AuthResponse/{id}` returns **`AuthHeaderDto`**, eagerly fetching `AuthLines` + `AuthSupportingInfos`: all nine deductible rates, policy, every header flag, per-line qty/days-supply/selection-reason/diagnosis-index/deductible-group, attachments as base64. Two gaps: **`MaxCoverage` is on `NAuthLine` but missing from `AuthLineDto`** (one of 205's five agent inputs — priced, not taken) and **`NAuthDiagnosis` is dead code** (entity present, both DTOs commented out, never written) so diagnoses round-trip as the header `Diagnosis` string + per-line `Diagnosis`/`DiagnosisIndex`. **Path 2 dropped as a prefill seam** — `auth/AuthJson/{id}` exists and **WPF already calls it**, straight into a raw-text viewer (`NphiesAuthListController.cs:487`); nothing anywhere parses FHIR back into a form, and writing that parser recovers a payload we hold twice in flat form. **A reopen is a replay, not a restore** — 208 banned drafts and the row is terminal `SUBMITTED`, so it opens a **fresh** session and replays through existing verbs (**no new session verb**), reporting what did not come back rather than restoring silently. **~1.5 developer-days, zero Nphies-service change.** 🚩 **Corrects 203 on `Failed`**: NPHIES validation is only *one* of two sources — the service's own guards (unknown item `:402`, no Nphies category `:514`, unconfigured provider `:576`/payer `:581`, prescription ref >40 `:219`) throw before the POST and produce an identical `Failed`, *more* fixable in the form; but lines are built at `:562`, **after** them, so those leave a header-only row path 1 can't prefill — the second reason path 3 wins. 🚩 **The list must send `showAll=true`** (`AuthService.cs:1377` filters `!c.Error`) or refused rows never appear · [findings](assets/196-nphies/reopening-a-refused-request.md)

## Nphies build — spec 209

Building spec [209](209-nphies-web-spec.md) out of map [196](196-nphies-to-web-map.md). Twelve
vertical slices, **frontier 210 + 211**. ⚠ **Eleven of the twelve depend on SIS.Api work** — exactly
ONE endpoint ships today (`POST Nphies/CheckEligibility`, API key, **no grant filter**) and
`NpihesHttpService` has three methods. ✅ **The server set now exists too: BackOffice
[912](C:\Work\DMSCO\BackOffice\.issues\912-nphies-web-door.md)–[922](C:\Work\DMSCO\BackOffice\.issues\922-nphies-conformance-fixtures.md)**,
eleven tickets, frontier 912, each citing the contract by section — so 210's the frontier here and
912's the frontier there, and they can start the same morning. ✅ **The contract:
[Nphies web API v1.0](assets/209-nphies-contract/CONTRACT.md)** — frozen 2026-08-01, 16 passthrough
endpoints + 11 session verbs, the session-state projection and its ordering rule, the 19-field line
ownership table, a three-way error taxonomy with codes, and 12 conformance fixtures; every field name
read from the service source, not inferred. **The server tickets cite it by section.** Blocking edges
below are in-repo only; each ticket names its server dependency under Boundaries.

- [210](210-core-owns-the-latest-state-guard.md) — `core` owns the latest-state guard, and the console still drives an order unchanged · **done** · spec 209 · blocked by: — · **prefactor, the only ticket startable today** · the version/etag/contract-version guard moved out of `features/callcenter/console` into `core/engine-session/` with its suite, because 217 is the second consumer and features may not import features. **Zero behaviour change** — assertions unchanged, `applyState` generic over `{version, etag}` so 217 need not widen `core/`. Open q **answered: `session-fault.ts` travelled** — the Nphies contract §6 names the same two codes with the same three closed reasons. 814 tests green, boundary gate clean, `callcenter-drive.mjs` 508/508
- [211](211-an-eligibility-check-answers-in-two-axes.md) — A granted agent runs an eligibility check and gets an answer in two axes · **done 2026-08-02** · spec 209 · blocked by: — · **SLICE 0, the tracer** · the whole spine landed — new `features/nphies/` area, `eligibility` namespace, `/nphies/eligibility/new`, a top-level **Nphies** nav group whose leaf **fails closed** on the area's one `Nphies/Access` probe (in `core/`, since both features share the single grant), and the check act through `core/api`. Provider is a free per-act pick with **no default and no memory** — and **Fill deliberately does not choose one** though the last check names one. Both axes render, with the site qualifier **inside** the verdict badge ("Eligible · outside network") and the verdict **blank** on a non-Complete request even when the row carries `isEligible:true`. 🚩 **the two-axis module landed in `core/nphies/status.ts`, not in the feature** — 214 is a second feature and features may not import features, so 212 takes it as-is. 🚩 **contract gap logged:** §5 names `NAuth`'s columns; an eligibility derives Request from `Outcome`/`Success` and is **never `Cancelled`**. 🚩 **`success:false` outranks `outcome`** — the service fills `Outcome` before the exchange's validation errors throw, so a refusal can arrive saying `complete`; 214 owes the same ordering. 🚩 **law 10's `contractVersion` deliberately neither sent nor checked** — §1.1/§3.1 make this a passthrough of a DTO that has no such field, so the gap is named for a §8 revision rather than invented; 217 needs the answer. 849 tests green, drive **41/41** against mocked envelopes (SIS.Api down, all four server deps unbuilt), lint + build clean · ten decisions in `.afk/HITL-211.md`
- [212](212-the-eligibility-list-opens-on-a-visible-window.md) — The eligibility list opens on the last 7 days and says so with a removable chip · **open** · spec 209 · blocked by: 211 · the chip IS the ticket — the underlying read is an unordered bulk take and a silent window reads as "that's everything". Five filters, provider defaulting to **all** (the opposite of a till, and therefore *not* what narrows the read). Introduces the **shared two-axis status module** 214 reuses unchanged. **server dep:** the re-modelled eligibility list (sort/page/total — the only genuinely new logic in the proxy)
- [213](213-two-coverages-force-a-pick.md) — A patient with two coverages must pick one before an authorization can be raised · **open** · spec 209 · blocked by: 212 · every coverage listed; **one auto-selects — expired included**, because the Verdict column is what says so; two or more force a pick with no default. Choosing a policy is choosing which **member id** the auth is raised under (there is no coverage id in the request). Builds the seam — a route transition carrying eligibility id + member id — which is what makes the two features independently buildable and what serves the raise-it-days-later case. **server dep:** eligibility response by id
- [214](214-an-authorization-row-states-both-facts.md) — An authorization row states whether we got an answer and what the payer said · **open** · spec 209 · blocked by: 212 · second feature + namespace. Both axes from 212's module, plus the two **markers** that are neither axis: payer query (**required** — such a row stalls on the web and must be visibly WPF's) and dispensed. **The row never asserts "ready to dispense"** — the real predicate has a follow-up clause this data lacks, so a copy could only lie. **No browser polling** (the service polls every 15 s); manual Refresh with the load time beside it. 🚩 **must request refused rows explicitly** or 221's reopen sits on invisible rows — the easiest thing here to get silently wrong. **server dep:** the re-modelled auth list
- [215](215-a-row-offers-only-the-acts-its-state-permits.md) — A pending authorization can be status-checked or retried, and a completed undispensed one cancelled · **open** · spec 209 · blocked by: 214 · state-driven acts, each withheld one **disabled with its reason on hover**. 🚩 carries the correction of record: **Retry belongs to `Pending`, not `Failed`** — it re-POSTs the stored payload verbatim, which is meaningless for a request never accepted. Server stays authoritative; a refusal renders as a **business outcome**, never a crash. **server dep:** status check · retry · cancellation
- [216](216-the-detail-shows-the-payers-reason-in-words.md) — The authorization detail shows the payer's reason per line, in words · **open** · spec 209 · blocked by: 214 · **there is no rejection view to build** — the reason arrives already decoded, so per-line verdict/approved-qty/rejected/reason renders **always**, which also covers the **partial** the brief forgets. Attachments-as-submitted cost nothing (already on the response). Defuses the dual-meaning field: **the Request state picks both label and source** — failure label on Failed/Pending, **never rendered on Complete**. **server dep:** auth response by id — one of the three methods that already exists, the cheapest ask in the effort
- [217](217-a-live-engine-session.md) — An agent builds a request in a live engine session, and a duplicate item is refused as it is added · **open** · spec 209 · blocked by: 210, 213 · **THE CORE.** The form is a **session, not a form** — open/add/qty/void/abandon on a real engine transaction, because a posted payload carries only the survivors and the audit exists to show engine-landed vs agent-changed. Identity read-only (not disabled controls). Inline add-row prices in place — **no counterpart in the old screen**, which read the till's basket. **Duplicate refused at the add**, remedy named. The **acting store is the pricing plant**, bound at open ⇒ resolve before the first item, no mid-request switch. **No resumable drafts** ⇒ warn before leaving. First consumer of 210's `core/` guard. **server dep:** six session verbs — **the largest and riskiest server term (8–12 d, could be 6 or 18)**, a parallel build since the call-centre services are doc-type-aware but call-centre-**bound**
- [218](218-five-money-inputs-and-nothing-else.md) — The agent's five money inputs reach the request, and nothing else is editable · **open** · spec 209 · blocked by: 217 · editable: header rates+caps, header paid-outside, line qty, line Max Coverage, line Days Supply (+ Selection Reason, a code not an amount, **disabled on `Generic` only**). Everything else read-only — the agent corrects the *insurance* terms, never the merchandise. **Days Supply 1–100 at the cell** ⇒ the old submit-time sweep and its dialog are **deleted, not ported**. **A zero cap warns** (the engine silently ignores it — inherited asymmetry, not ours to fix). 🚩 carry the brand-IR selection-reason overwrite **deliberately**, or someone "fixes" it and changes what reaches the payer. **server dep:** three insurance verbs + one schema column for paid-outside
- [219](219-supporting-material-diagnoses-and-attachments.md) — Morphology appears with its cause, and a 6 MB photo becomes a 250 KB attachment · **open** · spec 209 · blocked by: 217 · merged from two proposed tickets. **Principal is a radio** (uniqueness structural, not validated) and **morphology exists only while the principal is a neoplasm** — the rule stated forward instead of refused after. Exception prescription = a checkbox. Attachments: canvas-downscale 2000 px/q0.85, PDFs untouched and refused over 5 MB, **type dropdown dissolves** (MIME derives it), **closed 7-value title select with duplicates ALLOWED** — the opposite of 217's duplicate refusal, and correctly so. Mandatory-at-least-one is a **form state**, not a submit-time throw. **Attachments have zero server dependency** — the only such half-ticket in the effort. **server dep:** diagnosis + morphology lookups only
- [220](220-a-refused-submit-keeps-the-agent-on-the-form.md) — A refused submit keeps the agent on the form, with the reasons on the rows that caused them · **open** · spec 209 · blocked by: 218, 219 · submit is a **gated path, not a button**: the blockers module names every unmet condition; clinical edit is **one dialog in two shapes** (warning overridable, fatal not). 🚩 **`Failed` = refused before the payer saw it**, from **two** sources (the exchange's validation, and the service's own guards which throw before the lines are built) ⇒ a **form state the agent fixes in place**. 🚩 **a timeout is IN FLIGHT, never failed** — offer status check, never resubmit; this rule is the whole of what prevents a duplicate authorization. Three-way envelope: rejection renders as data, refusal explains from its code, only transport is an error. **server dep:** clinical-edit validate · submit (long explicit timeout) · assumes the zero-price guard
- [221](221-reopening-replays-and-reports.md) — Reopening a refused request replays it, and reports what did not come back · **open** · spec 209 · blocked by: 220, 216 · **a replay, not a restore** — a fresh session through existing verbs, **no new session verb**. **It must not pretend to be silent**: an item since blocked or repriced is *the information the agent needs*, so the screen reports what did not come back. Prefills from the **write-ahead journal row**, written before the payer is called ⇒ survives refusal/rejection/transport failure, and is the only source that covers a **header-only** refusal (guards throw before lines are built). **No new table, column or write.** Dropped: parsing the stored FHIR back (no parser exists anywhere, and it recovers what we hold twice in flat form) and **View JSON** (~0.5 d, post-v1). **server dep:** one journal-row read
