# Issues index

`oms-react`'s local issue tracker. Conventions: `docs/agents/issue-tracker.md`.
One line per issue under its effort heading. Numbers start fresh at `001`.

<!-- Format:
- [NNN](NNN-short-slug.md) — <title> · **open**/**done** · blocked by: NNN, NNN | —
-->

## Active Sessions admin screen (map 001)

- [001](001-active-sessions-admin-screen.md) — Active Sessions admin screen · **open** · wayfinder map
- [002](002-active-sessions-endpoint-gap-and-contract.md) — Active-sessions endpoint gap & contract · **done** · blocked by: —
- [003](003-active-sessions-access-grant.md) — Which access grant gates the Active Sessions screen? · **done** · blocked by: —
- [004](004-active-sessions-design-mock.md) — Interactive design mock for the Active Sessions screen · **done** · blocked by: 002
- [005](005-active-sessions-screen-spec.md) — Lock the Active Sessions screen spec · **done** · blocked by: 003, 004
- [006](006-active-sessions-screen-spec.md) — Active Sessions admin screen (spec) · **ready** · spec — consumable by /to-tickets
- [007](007-active-sessions-screen-access-spine.md) — activeSessionsScreenGatesOnItsOwnGrant · **done** · blocked by: — · dep: BackOffice Sessions/Access + grant seed
- [008](008-active-sessions-search-list.md) — searchingLiveSessionsListsMatchingRowsCappedAt50 · **done** · blocked by: 007 · dep: BackOffice GET Sessions
- [009](009-active-sessions-chips-counts.md) — channelAndIdleChipsFilterWithServerCounts · **done** · blocked by: 008 · dep: BackOffice Sessions/Counts
- [010](010-active-sessions-revoke-one.md) — revokingASessionSignsTheDeviceOut · **done** · blocked by: 008 · reuses Sessions/Revoke
- [011](011-active-sessions-revoke-all-for-user.md) — revokeAllForUserSignsAPersonOutEverywhere · **done** · blocked by: 008, 010 · dep: BackOffice Sessions/RevokeAllForUser
- [012](012-active-sessions-freshness.md) — theMonitorAutoRefreshesAndShowsFreshness · **done** · blocked by: 008

## Web POS Simulation screen (BackOffice map 484 / spec 503)

Client port of the WPF POS Simulation pricing harness. Server slice is BackOffice 509 (`Pricing/Simulate`
+ `Pricing/Access` + `POS_SIMULATION_ADMIN` grant) — **done**. Moved here from BackOffice `.issues/510–513`.

- [013](013-web-sim-screen-tracer.md) — POS Simulation screen: enter a basket, Process, see priced results · **done** · blocked by: — · dep: BackOffice 509 Pricing/Simulate + Access (done)
- [014](014-web-sim-condition-cards.md) — Pricing detail with expandable condition cards + statistical toggle · **done** · blocked by: 013
- [015](015-web-sim-bonusbuy-elements.md) — Bonus-buy tabs + pricing elements · **done** · blocked by: 013
- [016](016-web-sim-editable-grids.md) — Editable items + manual-conditions grids · **open** · blocked by: 013

## POS Simulation stale-BBY cache reset (map 017)

Prevent false pricing feedback from SIS.Api's server-side `"Pricing"` FusionCache serving a stale
BBY after re-download. Destination is a decision/spec (server + client). WPF cleared it in-process;
React needs a new `Pricing/*` endpoint.

- [017](017-sim-stale-bby-cache-reset.md) — POS Simulation stale-BBY cache reset · **done** · wayfinder map — spec ready for /to-tickets
- [018](018-cache-bust-endpoint-feasibility.md) — SIS.Api cache-bust feasibility: endpoint + FusionCache API + download path · **done** · blocked by: —
- [019](019-cache-bust-scope.md) — Whole-cache clear vs targeted BBY eviction · **done** · blocked by: 018
- [020](020-cache-bust-trigger-surface.md) — Where the cache-bust is triggered · **done** · blocked by: 018
- [021](021-cache-bust-blast-radius-auth.md) — Shared-cache blast radius & who may clear it · **done** · blocked by: 019
- [022](022-cache-reset-spec-lock.md) — Lock the stale-BBY cache-reset spec · **done** · blocked by: 019, 020, 021 · [spec](022-cache-reset.SPEC.md)

## Cache reset — client build (spec 022)

Client slice 3 of spec 022 (the simulator Clear-cache button). Server slices 1–2 (`Pricing/ClearCache`,
the `PricingCache` cache-admin grant, `GET Pricing/CacheAccess`, BBY auto-evict) are **BackOffice**
work, tracked in `C:\Work\DMSCO\BackOffice\.issues\` — the tickets below consume those endpoints.

- [051](051-sim-clearcache-button-gated.md) — theSimulatorShowsAClearCacheButtonOnlyToCacheAdmins · **done** (typecheck/build green; drove gating 3/3 via mocked envelope — live-drive pending SIS.Api) · blocked by: — · dep: BackOffice GET Pricing/CacheAccess + PricingCache grant
- [052](052-sim-clearcache-confirm-clear-toast.md) — clickingClearCacheConfirmsClearsAndToasts · **done** (typecheck/build green; drove confirm/clear/toast + rate-limit 5/5 via mocked envelope — live-drive pending SIS.Api) · blocked by: 051 · dep: BackOffice POST Pricing/ClearCache

## Web Back-Office Notification Center (map 023)

Bell + badge + list + deep-link + banner + claim (receive) and a channel-targeted broadcast/compose
screen (send), over `SIS.Api Notifications/*` polling. Backend NC (issues 164–172) already ships the
contract; the web BO caller (no `registerid`) is already a `User+All` audience. Destination = ready spec.

- [023](023-web-notification-center.md) — Web Back-Office Notification Center · **done** · wayfinder map — destination reached (spec 031 ready)
- [024](024-nc-backend-contract-for-web.md) — Notification Center backend contract for the web client · **done** · blocked by: — · [research](024-nc-backend-contract-for-web.RESEARCH.md)
- [025](025-web-identity-session-fit.md) — Web app identity & session fit for the NC · **done** · blocked by: — · [research](025-web-identity-session-fit.RESEARCH.md)
- [026](026-receive-side-parity-scope.md) — Receive-side parity scope for the back-office · **done** · blocked by: 024, 025
- [027](027-broadcast-channel-model.md) — Broadcast channel/audience model · **done** · blocked by: 024
- [028](028-access-gating-and-grants.md) — Access gating & grants (bell + compose screen) · **done** · blocked by: 024
- [029](029-nc-bell-panel-compose-ux.md) — Bell / panel / compose UX prototype · **done** · blocked by: 026, 027 · [prototype](029-nc-bell-panel-compose-ux.PROTOTYPE.html)
- [030](030-nc-spec-shape-and-lock.md) — Lock the NC spec shape & hand off to /to-spec · **done** · blocked by: 026, 027, 028, 029
- [031](031-web-notification-center-spec.md) — Web Back-Office Notification Center (spec) · **ready** · spec — consumable by /to-tickets

## Web Notification Center — build (spec 031)

Tracer tickets sliced from spec 031. Two independent chains: **Receive** (032→033→{034,035}, layout/
chrome) and **Send** (036→{037,038}, features/admin). AFK plan: [NC-AFK-HANDOFF.md](NC-AFK-HANDOFF.md);
overnight blockers: [NC-MORNING-REPORT.md](NC-MORNING-REPORT.md).

- [032](032-nc-bell-poll-badge.md) — theBellPollsAndShowsAnUnreadBadge · **code-complete** (typecheck green; runtime-blocked, SIS.Api down) · blocked by: — · dep: GET Notifications/Poll (+ api.get header passthrough)
- [033](033-nc-panel-list.md) — theBellOpensAPanelListingAnnouncementsNewestFirst · **code-complete** (typecheck green; runtime-blocked) · blocked by: 032
- [034](034-nc-read-state.md) — readingAnItemDropsTheUnreadCount · **code-complete** (typecheck green; runtime-blocked) · blocked by: 033 · dep: POST Notifications/{id}/Read
- [035](035-nc-arrivals-sonner.md) — aFreshArrivalRaisesAToastAndBumpsTheBadge · **code-complete** (typecheck green; runtime-blocked) · blocked by: 033
- [036](036-nc-compose-send-store.md) — composingABroadcastSendsItToAStore · **code-complete** (typecheck green; runtime-blocked) · blocked by: — · dep: POST Notifications
- [037](037-nc-fleet-confirm.md) — sendingToTheWholeFleetAsksForConfirmation · **code-complete** (typecheck green; runtime-blocked) · blocked by: 036
- [038](038-nc-compose-access-gate.md) — theComposeScreenIsHiddenWithoutTheBroadcastGrant · **code-complete** (client + backend built; runtime-blocked) · blocked by: 036 · dep: GET Notifications/Access (backend ADDED — BackOffice pricing2 dc73ba1f)

## Simulation applied-promotion visibility rework (map 039)

Rework how the POS Simulation screen shows per-line results + the promotions that fired: promo
visible without clicking each line, buy→get shown as relationships (1+1 free, 50%-off-2nd), today's
full detail preserved for advanced users via progressive disclosure. Destination = **approved
sketches** (spec + build are a later effort). Buy→get data is present-but-unused; no backend work.

- [039](039-sim-promo-visibility-rework.md) — Simulation applied-promotion visibility rework · **done** · wayfinder map — destination reached (B+C hybrid approved)
- [040](040-sim-promo-shape-taxonomy.md) — Promo-shape taxonomy & response-field mapping · **done** · blocked by: — · [taxonomy](040-sim-promo-shape-taxonomy.TAXONOMY.md)
- [041](041-sim-results-promo-sketch.md) — Sketch the reworked results-and-promo surface · **done** · blocked by: 040 · [prototype](041-sim-results-promo-sketch.PROTOTYPE.html) · chose B+C hybrid
- [042](042-sim-promo-hybrid-lock.md) — Consolidate the B+C hybrid & lock the direction · **done** · blocked by: 041 · [sketch](042-sim-promo-hybrid-lock.PROTOTYPE.html) · APPROVED
- [043](043-sim-promo-visibility-spec.md) — Simulation applied-promotion visibility rework (spec) · **ready** · spec — consumable by /to-tickets

## Simulation promo-visibility — build (spec 043)

Tracer tickets sliced from spec 043. Two chains meeting at the pure view model: **backend projection**
(044, own repo) + **frontend surface** (045 pure `promoView` → 046 grid column / 047 blocks →
{048 missed, 049 disclosure} → 050 responsive). Slice 0 = 045 (pure, in-memory, degradation path first).

- [044](044-sim-applied-bby-projection.md) — appliedBonusBuysSplitBuyFromGetByConditionKey · **in-progress** (condition-projection half — IsPrerequisite/IsCondition/ConditionKey/BbyItemIndex — landed in BackOffice `SIS.Pricing.Services`, builds & sim tests pass; unblocks the client "applied N×" count after SIS.Api restart. Structural `Applications[]`/`DiscountKind` split still blocked on the SIS.Pricing.Core package repo) · blocked by: — · dep: SIS.Pricing/BackOffice projection pass-through
- [045](045-sim-promoview-model.md) — promoViewDerivesLinesBlocksAndMissedFromAResult · **done** · blocked by: — · Slice 0 (pure, graceful degradation) · harness 11/11
- [046](046-sim-grid-promo-column.md) — theResultsGridShowsPromoKindAndRolePerLine · **done** (typecheck/build/transform green; live-drive pending SIS.Api) · blocked by: 045
- [047](047-sim-promo-blocks.md) — firedPromotionsRenderAsBuyGetBlocks · **done** (typecheck/build green; drove real component via Playwright — live-drive of split path pending 044) · blocked by: 045
- [048](048-sim-could-have-applied.md) — aMissedPromotionShowsWhyItDidNotFire · **done** (typecheck/build green; drove real component via Playwright 13/13 — found-vs-required meter, would-save, reason, expand, absent-when-empty) · blocked by: 047
- [049](049-sim-progressive-disclosure.md) — aPromotionBlockRevealsTodaysConditionAndPricingDetail · **wontfix** (superseded by map 097 → [103](103-sim-deep-layers-placement.md)) · blocked by: 047
- [050](050-sim-responsive-hybrid.md) — theHybridLaysOutResponsivelyByWidth · **wontfix** (superseded by map 097 → [105](105-sim-responsive-arrangement.md)) · blocked by: 046, 047, 048, 049

## BBY Inquiry screen (map 053)

React rebuild of the WPF `Sartawi.Retail.Pricing.BbyInquiry` screen: read-only Bonus Buy inquiry —
summary grid defaulting to currently-active BBYs, searchable by number or validity-overlap date
range, with a per-row Details modal mirroring SAP "Display Bonus Buy". Destination = **ready spec**
(+ two prototypes). Two backend contracts (list-search + detail-by-number) designed here, built on
SIS.Api separately.

- [053](053-bby-inquiry-map.md) — BBY Inquiry screen · **done** · wayfinder map — destination reached (both prototypes approved; ready for /to-spec)
- [054](054-bby-domain-glossary.md) — Bonus Buy domain model & glossary · **done** · blocked by: —
- [055](055-bbymodel-detail-shape-research.md) — Research: full BbyModel detail shape for the Details modal · **done** · blocked by: —
- [056](056-bby-feature-placement-access.md) — Feature placement, nav & access gate · **done** · blocked by: —
- [057](057-bby-list-search-endpoint-contract.md) — List/search endpoint contract · **done** · blocked by: 054
- [058](058-bby-detail-endpoint-contract.md) — Detail-by-number endpoint contract · **done** · blocked by: 055
- [059](059-bby-list-search-ux-prototype.md) — List + search UX prototype · **done** · blocked by: 054, 057 · [prototype](assets/059-bby-list-search-prototype.html)
- [060](060-bby-detail-modal-prototype.md) — Details modal prototype (SAP "Display Bonus Buy" mirror) · **done** · blocked by: 055, 058 · [prototype](060-bby-detail-modal-prototype.PROTOTYPE.html)
- [061](061-bby-inquiry-spec.md) — Bonus Buy (BBY) Inquiry screen (spec) · **ready** · spec — consumable by /to-tickets

## BBY Inquiry — build (spec 061)

Tracer tickets sliced from spec 061. Tracer = 062 (gate + list-active grid); frontier after it is
{063, 064} (parallel). Every slice ships **code-complete / runtime-blocked** until the SIS.Api `Bby/*`
endpoints (`Bby/Access`, `Bby/List`, `Bby/Detail`, `Bby/GroupingMembers`) are built separately.

- [062](062-bby-inquiry-scaffold-gate-list.md) — bonusBuyInquiryGatesAndListsActiveBonusBuysByDefault · **done** (typecheck/build green; pure harness 10/10; drove gate + active-list + empty/fail-open/denied 14/14 via mocked `Bby/*` envelopes — live-drive pending SIS.Api) · blocked by: — · dep: SIS.Api Bby/Access + Bby/List
- [063](063-bby-inquiry-full-grid.md) — theGridShowsAllTwentyEightHeaderFieldsGroupedWithChipsStickyIdentityAndDetailsAction · **done** (pure harness 23/23; drove full grid 23/23 via mocked `Bby/*` — live-drive pending SIS.Api) · blocked by: 062
- [064](064-bby-inquiry-search-toolbar.md) — searchingByNumberOrDateClearsActiveOnlyAndFiltersByValidityOverlap · **done** (typecheck/build green; pure harness 8/8; drove search toolbar — chip, cap banner, date-error title, Reset — 35/35 via mocked `Bby/*`; live-drive pending SIS.Api) · blocked by: 062
- [065](065-bby-inquiry-csv-export.md) — exportingTheGridWritesAllTwentyEightRawFieldsToCsv · **done** (typecheck/build green; drove Export CSV — 28-col header + raw code/date cells — via mocked `Bby/List`; live-drive pending SIS.Api) · blocked by: 063
- [066](066-bby-inquiry-details-modal.md) — openingDetailsShowsTheHeaderRecapBuyGetOrTotalDiscount · **done** · blocked by: 063 · dep: SIS.Api Bby/Detail
- [067](067-bby-inquiry-grouping-drilldown.md) — groupingRowsOpenAPagedMembersDrilldown · **done** · blocked by: 066 · dep: SIS.Api Bby/GroupingMembers

## POS palette + Document Details rework — wayfinder map 068

Adopt the `Sartawi.POS/View/Themed/PosTheme.xaml` palette as oms-react's standard (retiring the
claude.ai warm neutrals), then rebuild Document Details to match the POS "OMS Detail — reworked"
prototype. Destination is two ready specs. Palette first, screen second — owner ruling.
Reference capture: [assets/068-pos-detail-reference.html](assets/068-pos-detail-reference.html).
All 13 tickets done — wayfinding complete. **Both specs published: 082 (design system) and 083
(Document Details rework).** The map is reached; 083 consumes 082 and must not start until it lands.

- [068](068-pos-palette-and-document-detail-rework.md) — POS palette as the app standard + Document Details rework · **done** · wayfinder map — destination reached, both specs ready
- [069](069-token-surface-inventory.md) — Token surface & call-site inventory · **done** · blocked by: —
- [070](070-pos-token-remap-light.md) — The POS token remap (light) · **done** · blocked by: 069 · [prototype](assets/070-pos-token-remap.PROTOTYPE.html)
- [071](071-pos-token-dark-twin.md) — The derived dark twin · **done** · blocked by: 070 · [prototype](assets/071-pos-dark-twin.PROTOTYPE.html)
- [072](072-command-family-taxonomy.md) — The command-family taxonomy · **done** · blocked by: — · [prototype](assets/072-command-families.html)
- [073](073-detail-layout-with-our-data.md) — The reworked layout, filled with our real fields · **done** · blocked by: 070 · [prototype](assets/073-detail-layout.PROTOTYPE.html)
- [074](074-ag-grid-theme-mapping.md) — AG Grid theme mapping · **done** · blocked by: 070 · [research](assets/074-ag-grid-theme-mapping.RESEARCH.md)
- [075](075-brand-surfaces-reconciliation.md) — What survives of al-dawaa gold & navy · **done** · blocked by: 069
- [076](076-action-bar-grammar.md) — The action-bar grammar for our eight commands · **done** · blocked by: 072, 073
- [077](077-severity-colour-layer.md) — The severity colour layer and the raw-palette sweep · **done** · blocked by: 070, 071, 075
- [078](078-live-document-payload-capture.md) — Capture live document payloads · **done** · blocked by: — · [payloads](assets/078-document-payloads/)
- [079](079-status-severity-mapping.md) — Status value → severity mapping for the pill rail · **done** · blocked by: 078
- [080](080-rtl-mirroring-of-the-reworked-layout.md) — RTL mirroring of the reworked layout · **done** · blocked by: 073 · [prototype](assets/080-rtl-mirroring.PROTOTYPE.html)
- [081](081-rail-card-field-rules.md) — The rail cards' field rules against live data · **done** · blocked by: 079
- [082](082-pos-design-system-spec.md) — The POS design system · **ready** · spec (map 068, 1 of 2)
- [083](083-document-details-rework-spec.md) — The Document Details rework · **ready** · spec (map 068, 2 of 2) · consumes 082

### Design-system build — spec 082

Expand → migrate → contract. 084 expands (tokens; zero `.tsx` churn), 085–088 migrate the consumers,
089 contracts (the gates are green only when the sweep is complete).
**084 done** — the tokens are in and the contrast gate runs on every `npm run lint`; the app now
renders steel-blue neutrals against warm status badges, the expected intermediate state.
**085 done** — every grid now paints from the tokens out of one params block, so the grid can no
longer diverge from `global.css`.
**086 done** — the badge idiom is one component taking a severity; the two feature tone maps (and
the duplicate third) are gone, and `go` found its first consumer in the BBY validity marker.
**087 done** — al-dawaa gold/navy now live only inside the mark: the login Editorial Split swaps its
ground to `--brand-panel` (kickers lose their gold, marks stay gold), the home hero becomes a plain
`--card` tool (watermark + gold kicker dropped), both `text-[#FDC801]` classes and the four brand
`text-white` sites are gone, and the dead `auth.json` `subtitle` key is deleted. `-\[#` is zero across
`src/`.
**089 done — the design-system build is closed.** `npm run lint` runs three gates; the third,
`tools/check-palette.mjs`, fails on a raw palette class, an arbitrary colour value, a `white`/`black`
utility or a bare hex anywhere in `src/` outside `global.css` and the mark. The ticket's open question
was resolved as it recommended: the hex pattern is widened past D-12's `-\[#` to `#[0-9a-fA-F]{3,8}`,
so a colour hidden in a `.ts` string — the one place this app ever hid one — is caught by lint rather
than by review. The four deliberate literals (three `bg-black/50` scrims, the login QR quiet zone)
are file-scoped exclusions carrying their reason inline.

- [084](084-pos-tokens-both-themes.md) — theAppRendersOnThePosTokensInBothThemes · **done** · blocked by: — · slice 0 · + contrast gate
- [085](085-grid-theme-reads-tokens.md) — theGridReadsTheAppTokensInsteadOfItsOwnHexCopy · **done** · blocked by: 084 · one params block · + `tools/grid-theme-drive.mjs`
- [086](086-status-badge-takes-a-severity.md) — aStatusBadgeTakesASeverityRatherThanAClassString · **done** · blocked by: 084 · new `core/ui` primitive · + `tools/status-badge-drive.mjs`
- [087](087-brand-colour-lives-in-the-mark.md) — brandColourLivesOnlyInTheMark · **done** · blocked by: 084 · both kickers + 4 brand `text-white` retired, hero → card
- [088](088-raw-palette-sweep.md) — noScreenSpellsARawPaletteClass · **done** · one pass, 35 files
- [089](089-colour-literal-lint-gates.md) — lintFailsOnAReintroducedColourLiteral · **done** · blocked by: 085, 087, 088 · `tools/check-palette.mjs`, hex gate widened
- [107](107-identity-band-dark-separation.md) — theIdentityBandReadsAsASlabOnTheDarkPage · **open** · blocked by: — · follow-up from [096](096-document-detail-drive.md)'s manual pass · `--brand-panel` separates at 13.54:1 in light but 1.18:1 in dark and is the page's only borderless slab; D-9 sized that step deliberately, but for the login half, not for 091's full-width header

### Document Details build — spec 083

Region by region, each slice taking one region of Screen 2 the whole way down (payload → pure rule →
component → i18n → test). **090 is slice 0**: it bootstraps vitest against the five captured payloads
and cuts the pill rail, retiring the spec's biggest unknown — that rules derived from real data hold
against that data. 093 needs only the runner, so it runs in parallel with 091/092. The action bar is
094 rather than earlier because it is the only region whose change deletes state elsewhere in the
page (`pendingNote`). 095 lands the RTL respellings byte-identical under LTR; 096 is the acceptance
drive.
**096 done — the Document Details rework is closed, and with it map 068.** The rebuilt screen drives
end to end (39/39) with the six region drives still green beside it, and the both-theme pass across
all five captured documents found no arrangement defect. Its one recorded finding is a *design-system*
question, not a screen one: `--brand-panel` is fixed in both themes, so the identity band separates
from the page at 13.54:1 in light and 1.18:1 in dark.

- [090](090-pill-rail-and-vitest.md) — theRailRendersOnlyTheStatusesThatCarryAValue · **done** (vitest bootstrapped, `npm test` 17/17 on the five captured payloads; typecheck/lint/build green; drove the rendered rail 25/25 via `tools/document-rail-drive.mjs`) · blocked by: — · slice 0 · + Status tab removed, header Status group retired early
- [091](091-identity-band.md) — theDocumentNumberIsTheLargestThingOnTheScreen · **done** (`npm test` 28/28; typecheck/lint/build green; drove the rendered band 32/32 via `tools/document-band-drive.mjs` in both themes) · blocked by: 090 · `isExpressDelivery` binds — no contract bug; `DocumentHeader` retired, provenance moved into the disclosure
- [092](092-summary-rail-cards.md) — theScatteredFieldsBecomeFiveCardsOnASummaryRail · **done** (`npm test` 40/40; typecheck/lint/build green; drove the rendered rail 45/45 via `tools/document-cards-drive.mjs` in both themes and across the 900px unstack) · blocked by: 091 · `FieldGroup`/`ShippingAddress` retired; `courierCode` is set on 5/5 so the driver card does NOT collapse on pick-in-store — the rule ships as written, the gloss was wrong; the address panel's GPS + Google-Maps link left the screen with it
- [093](093-items-grid-and-jobs-count.md) — theItemsGridSumsItselfAndFlagsASignedDiscount · **done** (`npm test` 56/56; typecheck/lint/build green; drove the rendered grid 23/23 via `tools/document-items-drive.mjs`) · blocked by: 090 · parallel with 091/092 · counts land on ALL four tabs (a lone number on Jobs reads as an anomaly), no count until a deferred collection resolves; the footer label is two pluralised fragments — `1 lines` would have shipped on all five captures; totals round off float dust; the `cellClass` bidi wrap is 095's
- [094](094-action-bar-grammar.md) — theActionBarReadsAsThreeClustersAndATerminalTier · **done** (`npm test` 68/68; typecheck/lint/build green; drove the rendered bar 38/38 via `tools/document-actions-drive.mjs`) · blocked by: 092 · `pendingNote` dies with the standing textarea; the note is captured in each command's own dialog (`NoteDialog` + `NoteField`, `ChangeStoreResult.note`); a reasoned command carries `aria-disabled` (not `disabled`) so it stays focusable enough to explain itself; four new `core/ui/Button` variants carry 072's families; "command cluster" added to `CONTEXT.md`
- [095](095-rtl-mirroring-and-bidi.md) — theLayoutMirrorsCorrectlyBeforeAnyDirSwitchExists · **done** (`npm test` 68/68 unchanged; typecheck/lint/build green; drove both directions 33/33 via `tools/document-rtl-drive.mjs`, and the four region drives re-run green untouched) · blocked by: 091, 092, 093, 094 · new `core/ui/Ltr` on six fields; F1/F2/F4 were already correct from 085/091/093 and are now measured rather than assumed; **F5 has no counterpart in this build** — 073's notched frame became an underline tablist, so there is no notch to respell; `↗` flips to `↖`; `Undo2`/`Reply` on the action bar were never ruled on and stay unflipped for the `dir`-switch effort
- [096](096-document-detail-drive.md) — theRebuiltScreenDrivesEndToEndInBothThemes · **done** (`tools/document-detail-drive.mjs` 39/39; the six region drives re-run green untouched — rail 25, band 32, cards 45, items 23, actions 38, rtl 33; `npm test` 68/68, typecheck/lint/build green) · blocked by: 095 · **no source change was needed** — the pass found no defect; the one finding is recorded, not fixed: `--brand-panel` separates from the page at 13.54:1 in light but **1.18:1 in dark**, and the band is the page's only borderless surface, so the "one dark band" reads as a faint slab in dark — an 082 token decision, not a one-line correction · 090's fold-in note answered: the corpus table folded in, `document-rail-drive.mjs` stays as region evidence

## POS Simulation screen rework — wayfinder map 097

The Simulation twin of map 068: the whole screen — input side and results side — rearranged on the
shipped POS design system (082) into a denser, chip-led device that reclaims the space eleven
bordered cards spend on chrome. **Fresh design, no external reference**; evidence-first (098 before
every design ticket); **arrangement only**, no behaviour change. Destination = one **ready spec**.
Supersedes the two open 043 build tickets (049, 050).

- [097](097-simulation-screen-rework.md) — The POS Simulation screen rework · **done** · wayfinder map · all 11 tickets resolved, fog clear → hand off to `/to-spec`
- [098](098-simulate-payload-capture.md) — Capture live Simulate payloads · **done** · blocked by: — · task · 11 payloads under assets/098-simulate-payloads/
- [099](099-sim-region-question-inventory.md) — What the analyst reads the screen for, region by region · **done** · blocked by: — · grilling · arrangement D approved; 9 frames → 3
- [100](100-sim-chip-vocabulary.md) — The chip vocabulary: what earns a chip · **done** · blocked by: 098, 099 · grilling · two kinds, two hues, chips never act
- [101](101-sim-screen-device-prototype.md) — The reworked screen as one device · **done** · blocked by: 098, 099, 100 · prototype · quiet strip, 3 frames, near-misses in
- [102](102-sim-input-chip-bar.md) — The input chip bar: collapse, expand, edit, stale run · **done** · blocked by: 101 · prototype · one status slot, three states; defaults chip
- [103](103-sim-deep-layers-placement.md) — Where the deep layers live · **done** · blocked by: 101 · supersedes 049 · one idiom + the modal; only trace hides
- [104](104-sim-results-line-anatomy.md) — The anatomy of a result line · **done** · blocked by: 098, 101
- [105](105-sim-responsive-arrangement.md) — How the arrangement behaves across widths · **done** · blocked by: 101, 103, 104 · supersedes 050 · one breakpoint at 900 of work area; floor 1024
- [106](106-sim-rtl-mirroring.md) — Mirroring and bidi for the reworked arrangement · **done** · blocked by: 101, 104 · mirroring clean, 13 Ltr wrappers, 2 icons to flip
- [108](108-sim-bby-details-affordance.md) — The bonus-buy details affordance on a promo card · **done** · blocked by: 101, 103 · both cards · modal in place · ships dark until Bby/Detail lands
- [109](109-sim-i18n-churn-and-test-seams.md) — The i18n churn and the testing seams · **done** · blocked by: 108 · research · 22 retired / 6 renamed / 17 new keys, 6 pure seams, 5 drives
- [110](110-sim-screen-rework-spec.md) — The POS Simulation screen rework · **ready** · spec · synthesized from map [097](097-simulation-screen-rework.md); 80 stories, 6 pure seams, 4 drives to build; sliced into 111–122
- [111](111-sim-aggregate-conditions-under-test.md) — `aggregateConditions` holds its grouping contract under test · **done** · blocked by: — · spec 110 · prefactor · safety net before 116 grows its blast radius
- [112](112-bby-detail-modal-to-core.md) — The bonus-buy detail modal answers from `@/core/` · **done** · blocked by: — · spec 110 · prefactor · file move, zero behaviour; i18n namespace unchanged
- [113](113-sim-run-strip.md) — The run strip collapses the determination into chips and processes from there · **done** · blocked by: 123 · spec 110 · **slice 0** · + the work-area `@container` · 9 pure + 29 drive assertions
- [114](114-sim-status-slot.md) — The status slot marks a stale run and an in-flight one · **done** · blocked by: 113 · spec 110 · the rework's only new component · 30 pure + 22 drive assertions; no new i18n key
- [115](115-sim-result-line.md) — The result line reads its money in the corrected order · **done** · blocked by: 113, 123 · spec 110 · 7 columns; blank not 0.00; W ⇒ not priced · 20 pure; rendered check throwaway (the drive is 119’s)
- [116](116-sim-line-expansion.md) — A result line expands its rules and elements in place · **done** · blocked by: 111, 115, 123 · spec 110 · dissolved 2 components, the last AG Grid and 11 keys · 33 drive assertions on port 5200
- [117](117-sim-promotions-rail.md) — The promotions rail shows fires and near-misses beside the lines · **done** · blocked by: 115, 123 · spec 110 · near-misses reinstated; card prints its line list · 6 pure + 31 drive assertions (port 5201) · the split is between Results and the rail, so Items goes full-width above it and each frame is as tall as its own content; the near-miss's hue correction is the **meter**, not the tile (088 had already neutralised `KIND_CHIP`), so `○` marks the card and the condition type is named in words; no capture carries a `prerequisites[]` array, so the meter is unprovable on the corpus and the drive asserts the fallback; 114's stale-note check re-based on geometry
- [118](118-sim-bby-details-affordance.md) — A promotion card opens its bonus buy details · **done** · blocked by: 112, 117, 123 · spec 110 · gate `probed && screenAllowed`; ships dark · 22 drive assertions (port 5202) · the probe had to MOVE to `@/core/` to be consumed at all (112's spillover), exported cache key with it; four probe states driven, the fourth HELD so “the rail never waits” is proven rather than inferred; the control is a full-width card footer, never a chip, and the drive now checks that rule both ways
- [119](119-sim-responsive-arrangement.md) — The arrangement stacks the rail above the results below 900 px · **done** · blocked by: 116, 117 · spec 110 · container queries; pure shed order · 10 pure + 47 drive assertions (port 5203) · the work-area container is NAMED (`@container/work`) so the rail's card row can query the same 900 px breakpoint that stacks it — beside, a card still fills its column; the `order` lives on the RESULTS so source order stays results-then-rail (the reading and tab order at the width the screen is worked at); the trace measures its own wrapper because a pure shed order needs a number, and at 780 stacked it is WIDER than at 960 inside the 66% column; the two-row rule is measured across the strip's leaf parts, which caught a real three-row fragmentation at 780 — fixed by tightening spacing, hiding nothing
- [120](120-sim-non-result-states.md) — The screen's non-result states: pre-run, manual conditions, and the whole-run failure · **done** · blocked by: 113, 123 · spec 110 · manual conditions self-open; 400 replaces the work area · `fault-route.ts` reads the route off the code · drive port 5204, 27/27
- [121](121-sim-rtl-mirroring.md) — The reworked screen mirrors, spends two hues, and carries no retired key · **done** · blocked by: 115, 116, 117, 119 · spec 110 · absorbs 122 · `tools/sim-rtl-drive.mjs` re-pointed from the prototypes at the REAL app (29/29, 8 states × both directions, port 5205) + new `tools/check-sim-keys.mjs` (8/8) · the mirroring half stayed empty (zero physical utilities, asserted by source sweep); the bidi half landed on **9 runs + 5 composed values**, not 13 sites — two audit sites don't exist in the build (the strip's money has no literal space; `Rate: … | Base:` is W7-immune) and the raw server title turned up in THREE places, including the fired card's own title, because the classified branch never renders on the corpus; the audit's own measurement had to be **sharpened** (a leading/trailing neutral relocates per N1/N2 without scrambling the value, so runs are measured first-to-last ALPHANUMERIC — otherwise English prose reads as broken) and its composed pass **narrowed** to figure+unit (a label and its amount SHOULD swap under RTL — that is the mirroring); a Tailwind v4 flip is invisible to `transform` (it lives on the `scale`/`rotate` longhands), which is what makes the double-mirror check meaningful; the `buy→get` arrow is unprovable on the corpus (no split path until 044) so the drive asserts the spelling and says so; hue: the flag cell's `success` went neutral, 122's dead `met` branch had already left with 116, and the 400's `danger` is core `ErrorBanner`'s own (120's ruling), asserted by name rather than folded into the census; the ledger swept 11 held-back keys + one orphan the ledger never named (`summary.placeholder`)
- [122](122-sim-hue-and-key-audit.md) — The screen spends two hues and carries no retired key · **wontfix** · blocked by: 116, 121 · spec 110 · merged into [121](121-sim-rtl-mirroring.md) to remove a build wave
- [123](123-sim-i18n-key-expand.md) — The simulation namespace carries every new key before any slice needs it · **done** · blocked by: — · spec 110 · **expand half** · 27 keys minted, nothing retired; vitest key-ledger test

## OMS access gate + the empty-menu state

The one nav group with no probe (`menu-model.ts:23-29` names deliveries as *the* "always visible"
example) turns out to be an open **write** door: every `SdDocument/*` endpoint carries
`ApiKeyEndpointFilter` and no grant filter, so any authenticated session — including a store employee who
has just activated and holds no role — can reschedule or update a delivery document. Closing it makes the
empty menu common, so the empty state ships first. Server halves are BackOffice
[749](C:\Work\DMSCO\BackOffice\.issues\749-oms-web-screen-grant-seed.md) (grant seed) and
[750](C:\Work\DMSCO\BackOffice\.issues\750-sddocument-web-door.md) (the cookie-only `SdDocumentWeb/*` door).

- [124](124-no-visible-menu-empty-state.md) — A signed-in user with nothing to open is told so, not shown a blank page · **done** · blocked by: — · frontend-only, no backend dependency, ships first · the `noAccess` card in the `home` namespace (three keys: title / diagnosis / action — "contact IT support", never "your administrator") plus the part that is not cosmetic: `useVisibleMenu` grows a `settled` flag, because it hides a gated item while its probe is *pending* and the moment 125 gates the last ungated group the menu is empty on **first paint for everyone** — a naive `length === 0` check would flash the card on every load. Two call sites, so the return type changes rather than adding a parallel hook that can drift. Root cause it explains: `UpsertEmployeeAsync` writes `UaEmployee` only; the `UaUser` shell is minted by **first role assignment in Authz Admin**, and until then `AuthorizationSnapshotLoader` answers every probe with an empty list, silently
- [125](125-oms-behind-the-access-probe.md) — OMS goes behind an access probe, and its write doors stop being open · **code-complete** (client built, drive 28/28 on a stubbed wire; runtime-blocked on 750 being deployed + the server-refuses-a-reschedule check) · blocked by: 124 ✔, BackOffice 750 (**deployed**, not merely merged) · repoint `features/oms/*` to `SdDocumentWeb/*`, fold the bare `searchDeliveries` into a `deliveriesApi` object with `access()`, `accessProbe` on the leaf keyed `['oms','access']` (same key as the page guard ⇒ one call), and **two** guards — `DeliveriesPage` on `canOpenList`, `DocumentDetailsPage` on `canOpenDetail`. The second is the easy one to skip and the one that matters: `/oms/document/:no` is deep-linkable, `router.tsx` carries no per-route permission metadata, and a list-only guard leaves the write screen reachable to anyone who knows a document number. ⚠ this probe **fails closed** — no 404-tolerant catch like `Notifications/Access`/`Bby/Access`, because the endpoint ships with 750 and the thing behind it is a write door, which is exactly why it cannot merge before 750 is deployed. OQ1 is the likeliest way it goes wrong: the current OMS team must be *bound to the role* first, or the gate locks out the people it admits. Built: the probe landed in `@/core/oms/api` (`OMS_ACCESS_KEY` + `omsAccessApi.access()`) rather than on `deliveriesApi` — both OMS pages guard on it and a feature may not import a feature, the ticket-118 move. Two fixes beyond the letter: the guards carry the menu's `staleTime: Infinity` too (a mismatched one refetched on mount and could empty the OMS group under a working screen), and a FAILED probe now says the check is unavailable instead of telling an entitled operator to ask for a grant they hold. Eight sibling document/grid drives were repointed to the new door and re-run green

## The call center goes to the web (map 126)

Phase 1 replaces the WPF call center (CC1 `NewOrderController` + CC2) for the **plain CLCN cash
order only**, on an **engine-as-a-service** substrate: SIS.Api opens a real server-side
`CallCenterOrder` `PosTransaction` (it already ships `SIS.Pos 26.4.113`), resumed per request off
the persisted snapshot, and the client sends **intent, never money**. Backend halves are minted as
BackOffice issues (`C:\Work\DMSCO\BackOffice\.issues\`) — the engine and SIS.Api ship to the tills
too. Stands on BackOffice map [244](C:\Work\DMSCO\BackOffice\.issues\244-newpos-callcenter-order-entry.md)
+ spec [267](C:\Work\DMSCO\BackOffice\.issues\267-cc-order-entry-submission-spec.md), which already
shipped the doctype, the once-only `CallCenter/SubmitOrder` mint, and the submit orchestration.

- [126](126-web-call-center.md) — MAP: The call center goes to the web · **open**
- [127](127-engine-session-lifecycle.md) — What a web agent's engine session is, precisely · **done** · map 126 · blocked by: — · grilling · `register = WEB-<loginId>` (per agent), `Origin C000` a server-side constant, shiftless stays doc-type-keyed (no new flag), **strict claim** + 15 s lease + no heartbeats, second `open` refused with the existing id, sweeper at 12 h, **no `IPosEnvironment`**, live rows in the HQ DB → BackOffice [785](C:\Work\DMSCO\BackOffice\.issues\785-web-cc-engine-session.md)
- [128](128-origin-c000-and-coupon-parity.md) — Origin becomes a real field, and coupons move with it · **done** · map 126 · blocked by: — · grilling · **two permanent layers**: `TransactionOpenOptions.Origin` engine-**defaulted** (never required, so a till is right by construction), and the `BbyProcess` Plant-fallback **kept forever** — owner ruling — because it serves `PcHeader`s built outside the transaction engine (simulation/billing). `Origin` is **sticky**, restored from the snapshot, NOT claimant-derived. Coupons match `request.Origin ?? request.StoreCode` — the same expression as `BbyProcess` — with **one pairing test** proving same-filter template + BBY give both-or-neither. Physical CC device **out of scope** (already a `Store` row); **no blast-radius survey** (new engine not live); no `Origin` on the CLCN document. Server work minted as BackOffice [788](C:\Work\DMSCO\BackOffice\.issues\788-origin-seat-axis-and-coupon-parity.md)
- [129](129-rebind-store-door.md) — The plant-rebind door and its five re-derivations · **done** · map 126 · blocked by: — · grilling · 🚩 found a **till bug** on the way: `ResumeAsync` never sets `PcHeader.Plant`, so anything scanned after a suspend/resume takes the **national** price, silently (BackOffice 797, blocks the door). Door = `RebindPlantAsync` (BackOffice 798): takes the new plant's **whole identity**, re-prices with `"C"` `NewPricingAndKeepManual` (NOT `RecalculateAsync`'s rescale-only `"A"`), **refuses atomically** if a line no longer prices, **re-freezes ATP** (amends note 8), coupons **untouched**, promotion *selection* unchanged (BBY keys on Origin, not Plant). Derivation **pinned at the address act** ⇒ 132's ops-flip trigger out of scope. Preview is free: rebind and don't persist
- [130](130-potential-bby-prerequisites.md) — Can the server tell an agent *which items* would fire a missed promotion? · **done 2026-07-27 — yes for the buy side, no for the get side.** `prerequisites[]` is **not** dead (filled on every candidate); the captures read `[]` because both promotions carry **zero prereq rows**. BackOffice already shipped the guidance projection (spec 574/579 `AvailableOffersBuilder`), and `PricingContext.BuildSimulationResult` projects a **live** transaction with no re-price — that *is* `getState()`'s promotion seam. Eligible items are already exploded one-row-per-material and are the **same set** evaluation matches, but both builders **collapse to `[0]`** (also mis-typing a grouping as a single material — a live WPF defect). 🚩 **The real blocker is discovery**: BBY lookup keys on the *condition-side* access tables only, so a basket with only the buy-side item of "buy X get Y" **never loads that promotion**. ATP filtering → SIS.Api beside 131. 128 shifts which promos appear but doesn't block 136 (typed skip category). **No `wouldSave` on the wire** — this repo prints a percent as money today. → BackOffice [787](C:\Work\DMSCO\BackOffice\.issues\787-web-cc-promotion-guidance-engine.md), design → [138](138-near-miss-guidance-design.md) · map 126 · blocked by: — · research
- [131](131-item-search-endpoint.md) — The item search the agent actually types into (owner supplied the SQL) · **done** · map 126 · blocked by: — · task · **HITL** · contract minted as BackOffice [799](C:\Work\DMSCO\BackOffice\.issues\799-cc-item-search-endpoint.md) · `GET CallCenter/ItemSearch` behind door 137 · 🚩 supplied SQL is missing the composite key's `Client` predicate · ⚠ **amends note 9** — the row price is an **ex-VAT estimate** off the material master, not a store list price, and reads ~13% under the VAT-inclusive basket line (`MWST` is a separate 15% condition), so under-quoting is the failure mode; label lands on 135 · search gains **Arabic** (WPF never searched `Description2`) + item-number prefix, barcode out · eligibility = CC1's own whitelist, which the WPF lookup applies nowhere · **ATP needs no new contract** — `Stock/ItemPlant` already exists, folded in server-side at the order's plant, degrades to `atp: null` never non-200 · no paging, cap + `truncated` · 🚩 non-sargable match left deliberately unmeasured (p95 ≤ 500 ms target, full-text as measured fallback)
- [132](132-header-capture-inventory.md) — What header capture actually requires (CC1 + CC2 inventory) · **done** · map 126 · blocked by: — · research · **no new server data** — 11 of 12 header fields already have live endpoints, 5 already wired here; the one gap is a **door** (→ 137). Slot rule + `1283`/`1154` are Wasfaty-only, cut from CLCN. CC2's district→store rule wins and this repo already has it. Two new silent rebind triggers for 129
- [133](133-submission-path-server-side.md) — How the web submit reuses 278's orchestration, server-side · **done 2026-07-27 — in-process, not HTTP-to-self** (once-only lives entirely below the HTTP boundary; the till's endpoint stays byte-identical). The builder needs **no move** — its WPF-resident **input**, the issue-198 line mirror, is what moves, and it already exists server-side as a private test helper to promote. Once-only shipped as **(OrderNo, DocumentType), OrderNo := TransactionId**. Operator identity needs **nothing new**; `EntryStore` ruled = the agent's switcher store. 🚩 **`RecoverStaleAsync` has zero production callers**; 🚩 127's 12 h sweeper would **auto-void a transaction whose order was actually minted** ⇒ hosted reconciler + sweeper guard. 🚩 The delivery-fee **rule** is WPF-only and must become shared code (the `DFEE` condition itself is unchanged). Server work: BackOffice [786](C:\Work\DMSCO\BackOffice\.issues\786-web-cc-submission-path.md) · map 126 · blocked by: — · research
- [134](134-access-and-authorization.md) — Who may open the call center, and what the door refuses · **done** · map 126 · blocked by: — · grilling · **one grant admits the whole console**: `CallCenterConsoleView` = `BackOfficeScreen[CallCenter,03]`, role `CALL_CENTER_AGENT`, covering every verb incl. submit (749's own ruling reused; note 4 already removed every price-affecting power, so no dangerous verb is left to single out) ⇒ the probe answers **one boolean**, `{ canOpenConsole }`, and **fails closed**. **No store dimension** — the store is derived and pinned at the address act, 129's atomic refusal constrains an override materially, and there is nothing to constrain against (`StoreDetails` is documented broader than real permissions; no per-user `Auth/Stores`). 🚩 The filter must require the **cookie branch explicitly** — `ApiKeyEndpointFilter` stamps a never-empty *service* `UserId`, so an `IsNullOrEmpty`-only guard grant-checks a service account; that check is what keeps the WPF `CallCenter/SubmitOrder` alive and **unedited** beside the web door. Both Bby-gated surfaces dissolved instead of minting a second grant: grouping members ship **resolved + ATP-filtered inside `getState()`** (a 136 contract ruling), the SAP detail modal is **out of phase 1** ⇒ the console touches no `Bby/*` route. 🚩 Chrome-less console ⇒ its refusal is a **dead end** (`ProtectedLayout` renders `AppShell` unconditionally), so it carries its own way home in the five-key copy form — a state 135 must draw. 🚩 **Cutover**: seeds bind no holder and the only path minting the `UaUser` shell is first role assignment in Authz Admin, so an activated agent is refused **silently** until bound — per user, query-verified, before the SIS.Api deploy. Server work minted as BackOffice [800](C:\Work\DMSCO\BackOffice\.issues\800-call-center-console-grant.md) (the 749 analogue; deploys before 137's door)
- [135](135-agent-console-prototype.md) — What the agent console looks like · **done 2026-07-27** · map 126 · blocked by: — · prototype · **variant A, three fixed columns** — rails never move; ex-VAT estimate leaves the money column (`SAR` = engine money only); offer strip wraps, count mirrored in the top bar; both confirmations stay modal; keyboard grammar graduates to 153. Variants on branch `prototype/135-callcenter-console`
- [136](136-session-api-contract.md) — The session API contract that lets both tracks run free · **done** · map 126 · blocked by: 127, 129, 130, 133 · grilling · **frozen v1.0** → [CONTRACT.md](assets/136-cc-contract/CONTRACT.md) + 8 provisional fixtures. Every mutating verb returns the **whole `SessionState`** (the server has just resumed and persisted — the projection is in hand); **`requestId` on every mutation** makes an ambiguous retry safe; **explicit, ownership-validated `transactionId`** stops a forgotten tab writing onto the agent's *new* order; **"are you sure" rides the success path** as `pendingConfirmation` + a token that **pins** the previewed diff — one pattern for both the rebind and the below-ATP ack, whose token *is* the fraud-flag audit record; **near-misses inline, eligible items on demand** (`ResolvePrereq`, off `Bby/*`); `SESSION_BUSY` retried in `features/callcenter/api.ts`, **never** `core/api.ts`; header fields live in a SIS.Api **`CallCenterSession` sidecar** and `SessionState` is the join. 🚩 The two-store **write ordering** is the fragile part (reserve requestId *with the pre-mutation version* before the engine mutation, or a crash makes the retry double-apply). Server work minted as BackOffice [804](C:\Work\DMSCO\BackOffice\.issues\804-cc-session-contract-server-obligations.md). **Both tracks now unblocked to `/to-spec`**
- [137](137-callcenter-web-door.md) — The header-capture routes need a web door · **done** · map 126 · blocked by: 134 · grilling · 🚩 **132's premise was wrong**: `ApiKeyEndpointFilter` is *cookie-session OR api-key* (`:40-79`), not api-key-only, so all ~15 routes are **already browser-reachable today** (`lookups.ts` drives five live). The door is about **gating**, not reachability ⇒ **eight leave it entirely** (Cities · Districts · AddressLabels · DocumentSources · DocumentTypes · StoreDetails · AvailableSlots · SlotIsActive — reference data, zero server work; 750 OQ2's slot ruling re-confirmed on this door's own terms) and **nine stay**, gated for PII + writes. **One tag `CallCenterWeb/*` hosts everything** — header siblings, 131's `ItemSearch`, 136's session verbs — behind one filter and one `{ canOpenConsole }` probe, so **136 inherits it**. 🚩 **'Delegates verbatim' breaks twice, deliberately**: `DocumentSourceUsers/{userId}` → session-derived `MyDocumentSources` (no path param), and the five `CustomerAddresses` routes → **scoped to the session's attached customer**, because the originals are unscoped (`DeleteCustomerAddress` takes **only an `addressNumber`**, no customer at all) ⇒ a verbatim sibling would let any agent delete any address in the estate. ⚠ Address book now **unreachable before customer attach** — a new ordering constraint for 135 + 136. `Touch` confirmed off the door. 🚩 Exposed a **live PII hole predating the call center**, **ruled out of scope** → BackOffice [802](C:\Work\DMSCO\BackOffice\.issues\802-callcenter-pii-routes-ungated.md). Door minted as BackOffice [801](C:\Work\DMSCO\BackOffice\.issues\801-callcenter-web-door.md)
- [138](138-near-miss-guidance-design.md) — What actionable promotion guidance looks like, and what it promises · **done 2026-07-27** · map 126 · blocked by: 135 · prototype · **HITL (ruling taken)** · graduated from fog by 130: three near-miss classes in one list, a grouping is a set not an item, and the card may promise a discount **definition** — never a savings total. Ruled: **variant 1, the wrapping strip** — against the write-up's recommendation, which stays recorded; it costs 72 px over variant 2 and buys **scanning**, every actionable offer a card with no stepper. Safe only because both its corrections were already measured in (open card spans both columns; strip body clamped at 18rem with the outcome banner pinned outside). **Qualifying rows carry Arabic** (131's reason applies harder — this is the row read aloud), fixed at the shared `ItemRow`, whose doc comment had promised Arabic while rendering none. Two findings from landing it: 🚩 **`<bdi>` implies `dir="auto"`** and flips the block RTL, detaching the Arabic from its English line — `<bdi dir="ltr">` keeps the isolation without the flip, the mirror case of 121's `core/ui/Ltr` and a **build-level** constraint; 🚩 **a clamped region hides the cost of new content** — all nine heights stayed identical while the third item and `Search the other 37` (part 1's required route to the rest) went below the fold, and the 45%-of-centre budget left no room to raise the clamp, so Arabic moved to the **meta line** and every height came back byte-identical: **the ruling costs zero pixels**. Drive 91/91, typecheck + 3 lint gates green
- [153](153-console-keyboard-grammar.md) — The console's keyboard grammar · **open** · map 126 · blocked by: — · prototype · **HITL** · graduated by 135 amendment 3: `Ctrl+K` verb list, what gates single letters when the resting focus IS a text box, what `?` shows, and whether submit/abandon get keys at all

### Owner gap review, 2026-07-27 — six features the charting missed (154–159)

Raised on reviewing 138's prototype: delivery type · payment type · delivery fees · stock in other
stores · price check · redeem coupon · add new loyalty. They split three ways — **already in scope
but never drawn** (159), **missing from the frozen contract** (154, 155), and **never charted at all**
(156's waiver half, 157, 158). Two owner rulings taken at review time: the delivery fee is
**rule-driven with no manual waiver**, and price check + stock-in-other-stores are **both phase 1**.

- [154](154-fulfilment-mode-and-store-choice.md) — Fulfilment mode, and how the store gets chosen when nothing derives it · **open** · map 126 · blocked by: — · grilling · `Cc2DocumentHeaderBuilder.cs:29-31` writes `DeliveryType = Delivery|PickInStore` and CC1 `NewOrderController.cs:447-481` collapses/shows the store picker on it — but contract v1.0 has **no fulfilment-mode axis at all**, and it is not on 126's out-of-scope list either: it fell between 132 (which established the fact) and 136 (which froze without it). The mode is cheap; **pick-in-store unwinds four settled rulings** — 129's derivation-at-the-address-act (nothing to derive from), note 6's store-before-first-item (a different opening move than 135's caret-on-phone), 137's attach-before-address ordering, and slot+fee, which both presuppose delivery. ⚠ *"address optional, store chosen not derived"* changes frozen meanings ⇒ **major** under §9, owner ruling as a dated amendment
- [155](155-payment-type-cod-or-online.md) — Payment type: cash on delivery, or paid online · **open** · map 126 · blocked by: — · grilling · `Cc2DocumentHeaderBuilder.cs:36-38` `PaymentType = Online|CashOnDelivery`, CC1 defaults COD. 🚩 the map's own phrase *"plain CLCN **cash** order"* hid this for the whole charting — that "cash" is the order **kind** (not Wasfaty/Nphies/insurance/P2E), a **different axis** from COD-vs-paid-online. Probably a plain header field, not a confirmed verb (it classifies collection, it does not change an amount) — but state that reading explicitly, because "payment" sits close enough to note 4 to be worth ruling on
- [156](156-delivery-fee-shared-rule.md) — The delivery fee stops living in WPF · **open** · map 126 · blocked by: — · research · 133's one-line flag, read properly, is worse twice over. **The amount rule** (`POSCommon.cs:394-435`) contains a **hardcoded free-shipping window, 20–27 June 2026** — a campaign compiled into the till binary — plus NUPCO/Wasfaty 15 vs standard 10 and a P2E per-doctype config; meanwhile the contract quotes `totals.deliveryFee.thresholdGross` and **nothing computes any of it**, so either the threshold lives elsewhere or the contract invented a field. **The waiver**: `NewOrderView.xaml:192-200` is an agent-facing radio pair *"Add Delivery Fees" / "No Fees"*. 🚩 **map note 4's justification is false as written** — it claims removing price-affecting power *"removes nothing agents have today"*, verified by grepping five names (`ChangePrice`/`ManualCondition`/`Discount`/`OpenPrice`/`SetPrice`) none of which is the fee radio, so the search could not have found it. **Owner ruling: rule-driven only, no manual waiver** — `waived` is an outcome shown, never a control; note 4's principle holds and its supporting claim gets corrected instead
- [157](157-price-check.md) — Price check: what an item costs, without adding it · **open** · map 126 · blocked by: — · grilling · **owner-added, ruled into phase 1**. A till feature, not a port: `ItemInfoLookupController` (UnitPrice + Stock) is constructed in exactly one place, `POSController.cs:16403`, and no CC1/CC2 path reaches it. 🚩 the whole difficulty is **131's note 9 made worse** — an ex-VAT estimate is tolerable on a search row where the agent is *choosing*, and intolerable here, because a price check exists to be **read out loud** with no basket line beside it to contradict it. So the question is not where the button goes but **what number it may return**: engine truth or estimate, VAT-inclusive always, does it see promotions (a number that ignores the offer 138 is simultaneously advertising is a contradiction on one screen), and what it says before a plant is bound
- [158](158-stock-in-other-stores.md) — Stock for an item in other stores · **open** · map 126 · blocked by: — · research · **owner-added, ruled into phase 1**. `StockInOtherStoresCommand` → `StockByDistanceController` → `StockV2Service.GetCurrentStockByDistance(item, lat, long)` — the first thing on this map needing **geo**, and the first item read deliberately **not** scoped to the order's plant (131 folded `Stock/ItemPlant` in *at* that plant — the opposite reach). 🚩 the dangerous half is not the read, it is **what the agent may do with it**: seeing stock at 1204 invites moving the order there, which is 129's plant rebind with its atomic refusal and whole-basket re-price. Rule read-only vs rebind-entry explicitly; if the latter it rides 129's confirm path and nothing else
- [159](159-coupon-and-loyalty-signup-drawn.md) — The console draws its coupon and its loyalty signup · **open** · map 126 · blocked by: — · prototype · a **drawing** job, no contract question open: both are already settled and simply absent from 135/138. `applyCoupon` is verb #10 with `COUPON_REJECTED`/`COUPON_ALREADY_APPLIED` in the taxonomy, yet `grep -i coupon` over `VariantA.tsx` returns nothing; loyalty signup is 132's ruled-in two-step OTP (country + mobile → OTP, no name/email, `CustomerCreateService`) with *"nothing to build server-side"*, and the console has no answer to "this caller isn't in the system". Draw the coupon beside a fired promotion without it reading as a fourth near-miss class, draw the OTP wait as a **spoken** one, and 🚩 check that the **signup** routes are on 137's door — it gated loyalty *lookup*, and a console that can find a caller it cannot create is the wrong half

## UA users at scale (map 139)

Ayed's three asks for `admin/ua-users`, which all press on the same seam — the `UaAdminWeb/*` read
contract. A seventh report card counting people who **completed activation**; real paging past the
50-row wall (the server already takes `skip`/`take`, the client hardcodes `skip: 0`); and a CSV the
Excel double-click opens intact. Destination is a `ready` spec for the client plus a written server
addendum for SIS.Api — the C# lands elsewhere.

- [139](139-ua-users-scale-and-export.md) — MAP: UA users at scale: the activation counter, paging, and export · **done 2026-07-27** · all 7 tickets resolved, no fog left — ready for `/to-spec`. One server change in the whole effort (141's `completedActivation` card field); audit-tab paging ruled out of scope
- [140](140-uaadminweb-contract-as-built.md) — What the UaAdminWeb contract already gives us · **done 2026-07-27** · map 139 · blocked by: — · research · 🎯 **paging needs zero server change** — both endpoints already bind `skip`, `ClampToCap` caps only `take`, every path orders deterministically before paging, `isCapped` already means "beyond *this* page"; the whole change is the hardcoded `skip: 0` at `api.ts:17`. **Nothing today means "finished activating"** (`awaitingActivation` = `Seeded ∧ ¬phoneGap ∧ no credential row`); a 7th card is ~4 lines in 3 files and its count agrees with its worklist **by construction**. 🚩 the cards **already don't sum** — three universes, and `mustChange` alone skips the legacy-`[User_]` join and shared-account exclusion. Estate **~6,000 identities**, ~6 DB queries per page ⇒ client full export of *All people* ≈ **120 round trips / 720 queries** (narrowed cards trivial: `phoneGap` ≈ 8) — the fork 144 must rule on is `all` alone; `MaxSearchRows = 50` is a **product clamp, not a technical limit**. Every route grant-gated fail-closed, **no read audited anywhere**, audit rows are single-target only · [research asset](assets/140-uaadminweb-contract-as-built.RESEARCH.md)
- [141](141-completed-activation-predicate.md) — What counts as a completed activation · **done** · map 139 · blocked by: 140 · grilling · **HITL** · legacy-backed ∧ ¬shared ∧ credentialState=='active'; no active clause, no phone clause; server field only
- [142](142-seventh-card-label-and-placement.md) — Naming and placing the seventh card · **done 2026-07-27** · map 139 · blocked by: 141 · grilling · **AFK (recommendations taken)** · "Activation done" (keeps the noun, never spends "active"); Disabled unchanged; position 5 beside Awaiting activation; untoned; row becomes `auto-fit minmax(8rem,1fr)` so 6 and 7 cards both look right
- [143](143-pager-shape.md) — The pager: page size, controls, and what a page change disturbs · **done** · map 139 · blocked by: 140 · grilling · **HITL** · fixed 50, prev/next + "Page N of M" in a footer; selection survives paging; cap note retires
- [144](144-export-scope-and-cost.md) — How much the export exports, and what that costs · **done** · map 139 · blocked by: 143 · grilling · **HITL** · everything exportable, confirm above 500 rows; walk at 50 (no server change); cancel or error ⇒ no file
- [145](145-csv-shape-and-excel-fidelity.md) — What is in the file, and whether Excel opens it right · **done 2026-07-27** · map 139 · blocked by: 144 · grilling · **AFK (recommendations taken)** · 13 columns = the row unpacked + Status; labels not codes (so the file is localised); `="…"` wrapper on employeeId/phone, `'` guard on free text, BOM + `sep=,` + CRLF; `null` last-login is an empty cell; `ua-users-{scope}-{date}.csv`; pure `csv.ts`; no server change
- [146](146-export-gate-and-audit.md) — Should bulk export be gated, and should it leave a trace · **done 2026-07-27** · map 139 · blocked by: — · grilling · **AFK (recommendation taken)** · **no separate grant, no audit row** — 144 made export = the pager in a loop, so the server can't tell them apart: a second grant has no endpoint to enforce at (client-only ⇒ hide-the-button) and inverts least privilege (the same grant already carries SetPassword/Deactivate/RevokeSession); the separation already exists as the standalone `UA_USERS_ADMIN` role. Button carries **no permission check of its own**. Addendum gains a 2nd deferred note: if ever required, a *server* export endpoint that streams+audits+carries its own grant — never a client `canExport`. 🚩 144's >500 confirm is a wait-time device, not a control
- [147](147-ua-users-scale-spec.md) — UA users at scale: the activation counter, paging, and export (spec) · **ready** · spec — consumable by /to-tickets · dep (7th card only): BackOffice 805 `completedActivation` count + worklist case; pager and export need no server change

## UA users at scale — build (spec 147)

Tracer tickets sliced from spec 147. Two independent chains: **paging + export** (148→{149, 150→151})
and **the seventh card** (152, parallel). Only 152 needs a server change — BackOffice 805
(`completedActivation` count + worklist case); the pager and export ship against `UaAdminWeb/*`
as-built.

- [148](148-ua-users-pager.md) — theGridWalksPagesInsteadOfStoppingAt50 · **done 2026-07-27** · blocked by: — · Slice 0 · fixed 50-row page, prev/next + "Page N of M" footer shown only past 50 rows, Next off `isCapped`, page becomes a field of the query (so a new search/card resets to page 1 by construction), `keepPreviousData` + dim-while-fetching. Retires the cap note (`grid.capped` **deleted**, `search.hint`/`grid.emptyHint` reworded) and fixes the latent bug: the header count must read `totalMatches`, not `rows.length`. No size chooser (server clamps `take` down), no numbered pages (120 of them). Creates `tools/ua-users-scale-drive.mjs` (28/28). Built: pure `pager.ts` + `GridPager.tsx`; needed **`staleTime: 30_000`** on top of `keepPreviousData` — the global default is `0`, so a step back to a visited page would refetch and dim, which is the stutter the ticket set out to kill. `CONTEXT.md` gained a **Page** entry recording that `isCapped` now reads "another page exists", not "truncated"
- [149](149-ua-users-page-holds-through-mutations.md) — actingOnAPersonHoldsThePageAndClampsAnEmptyLastPage · **done 2026-07-27** · blocked by: 148 · a reset/disable/clear-TOTP refetches without leaving the page; if that empties a page above 1, clamp to `ceil(totalMatches/50)` so success never looks like a broken screen. Selection surviving paging is **asserted, not built** (the pane fetches by id) — recorded so nobody "fixes" it by clearing selection on page change. Built as `clampToLastPageWhenCurrentPageEmpties` in `pager.ts` + one effect on the **list result** rather than in an action handler, which is what makes it cover **every** caller of `refreshLists` (pane actions, set-password, new identity) — gated on a *settled* read, because `keepPreviousData` means mid-flight `data` is the previous page's rows. "The page holds" needed no code (the page is a field of the query; a mutation only invalidates) and is asserted so nobody breaks it. Drive now 41/41, its stub gaining live worklist membership + counts/mutation ledgers. Review caught one real hole, fixed here: an **uncached** landing page arrives behind the emptied one as `keepPreviousData`'s placeholder, so "No people match" would render for that round trip — the very screen the clamp exists to prevent; a placeholder with no rows now reads as a first load. `CONTEXT.md` gained **Worklist** (a *card* is the count you click; a *worklist* is what you work down, and it has live membership)
- [150](150-ua-users-csv-export.md) — exportingANarrowedCardDownloadsACsvExcelOpensCleanly · **done 2026-07-27** · blocked by: 148 · the routine export path: button + walk over the current query's **full match set** (not the visible page) + pure CSV writer. 13 columns = the wire row unpacked + derived Status, channel pill split back into codes-via + reachable; every classification is **the screen's `t()` label, not the code** (so the file reconciles by construction — accepted price: the export is localised). Excel guards: `="…"` on employeeId/phone (quoting alone loses a leading zero), `'` on formula-leading free text, BOM + `sep=,` + CRLF. `null` last-login is an **empty cell, not "Never"** (a word kills date sorting). Filename `ua-users-{scope}-{YYYY-MM-DD}.csv` off the card **code**. 🚩 **No permission check of its own** — renders behind the existing access probe; do NOT add `canExport`. Built: pure `csv.ts` (writer + filename, takes a **label resolver** so its suite drives it with the real locale file — a missing key fails as a raw key in a cell) + `export.ts` (walk + download). Column 4 `phoneClass` is the one fact with **no on-screen word**, so *Mobile status* + `csv.phoneClass.*` is the file's only new vocabulary; everything else resolves through the pills' own keys. The runaway bound **throws** rather than returning a short list — returning would be the silent early stop, and it would write a file. Drive now 50/50 (its `phoneGap` pool became 400 rows = 8 pages, so 402 lines out proves whole-match-set)
- [151](151-ua-users-export-at-scale.md) — exportingEverybodyWarnsFirstAndCancelsWithNoPartialFile · **open** · blocked by: 150 · everything exportable incl. `all`, but >500 rows raises a confirm naming rows + wait (~45 s for ~6,000); `phoneGap` ~400 lands just under deliberately. Cancellable sonner progress, walk never touches the mounted query's cache. 🚩 **The governing rule both ways: cancel or any `ApiError` ⇒ no file at all** — a partial CSV is indistinguishable from a complete one in Excel and this file's use is spotting who's *missing*. Terminate on `isCapped` + 200-page runaway guard + dedupe by `employeeId` (a 45 s walk can be shifted by a concurrent SAP insert). ⚠ the >500 confirm is a wait-time device, **not** a control — never write it up as one
- [152](152-ua-users-activation-done-card.md) — theCardRowRendersRightAtSixAndSevenCards · **open** · blocked by: — · parallel frontier · "Activation done" at position 5 beside Awaiting activation, **untoned** (colour on this row means *there is work here*; this is the one card whose rows need nothing done). Disabled neither moves nor is renamed. Card list moves out of the page into a pure module; row becomes an auto-fit track because **an absent field ⇒ the card is not rendered — never `0`** (a confident zero reads as "the cutover hasn't started"). The six-card state is a real state, not a temporary hack. 🚩 Do NOT add a cards-sum check — they already don't sum (three universes), and this card overlaps Disabled on purpose (a leaver who activated still counts). **dep: BackOffice 805**; stubbed-envelope drive until it ships, and the card appears in prod the moment 805 deploys with no client release
